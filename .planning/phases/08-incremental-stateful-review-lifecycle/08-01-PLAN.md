---
phase: 08-incremental-stateful-review-lifecycle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/prevue/fingerprint.py
  - tests/test_fingerprint.py
  - tests/fixtures/compare_ahead.json
  - tests/fixtures/compare_diverged.json
  - tests/fixtures/compare_identical.json
  - tests/fixtures/graphql_review_threads.json
  - tests/fixtures/graphql_resolve_ok.json
  - tests/fixtures/graphql_forbidden.json
autonomous: true
requirements: [LIFE-02]
must_haves:
  truths:
    - "fingerprint(path, title) is deterministic — same path+title always yields the same digest"
    - "Rephrasing severity, line, or suggestion does NOT change a finding's fingerprint (identity = path|normalize(title) only)"
    - "normalize_title is unicode-correct (casefold, NFKC) so visually-identical titles fingerprint equally"
    - "Shared compare-API and GraphQL JSON fixtures exist for downstream plans to mock against"
  artifacts:
    - path: "src/prevue/fingerprint.py"
      provides: "normalize_title + fingerprint pure functions (D-04)"
      contains: "def fingerprint"
    - path: "tests/test_fingerprint.py"
      provides: "determinism + unicode + exclusion-of-line/severity/suggestion tests"
    - path: "tests/fixtures/compare_ahead.json"
      provides: "PyGithub compare response: status=ahead, merge_base==lastSHA, incremental file set"
    - path: "tests/fixtures/compare_diverged.json"
      provides: "PyGithub compare response: status=diverged (force-push fallback)"
    - path: "tests/fixtures/graphql_review_threads.json"
      provides: "GraphQL reviewThreads query response (id/isResolved/isOutdated/path/line)"
    - path: "tests/fixtures/graphql_forbidden.json"
      provides: "GraphQL 403/FORBIDDEN errors body for best-effort-skip tests"
  key_links:
    - from: "src/prevue/fingerprint.py"
      to: "hashlib.sha256"
      via: "stdlib digest of path|normalize(title)"
      pattern: "hashlib\\.sha256"
---

<objective>
Create the content-addressed finding-identity pure unit (D-04) and the shared
HTTP fixtures every downstream plan in this phase mocks against.

Purpose: The fingerprint is the deterministic dedupe/carry-forward backstop
(LIFE-02). It is a pure function with a precise I/O contract — the prime TDD
candidate flagged by RESEARCH.md and VALIDATION.md. Building it (and the shared
compare/GraphQL fixtures) first unblocks Waves 2–5 with zero file conflicts.
Output: `src/prevue/fingerprint.py`, `tests/test_fingerprint.py`, and six JSON
fixtures under `tests/fixtures/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (the convergence source-grounding pass
must treat these as new, not drift):
- `src/prevue/fingerprint.py` — NEW module: `normalize_title(title: str) -> str`,
  `fingerprint(path: str, title: str) -> str` (D-04).
- `tests/test_fingerprint.py` — NEW test module.
- `tests/fixtures/compare_ahead.json`, `compare_diverged.json`, `compare_identical.json` — NEW REST fixtures.
- `tests/fixtures/graphql_review_threads.json`, `graphql_resolve_ok.json`, `graphql_forbidden.json` — NEW GraphQL fixtures.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — fingerprint/normalize determinism + exclusion contract</name>
  <files>tests/test_fingerprint.py</files>
  <read_first>
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"NEW: fingerprint / normalize_title (D-04)" — exact normalize steps + casefold/NFKC pitfalls)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§"Pattern 3: Content-addressed finding fingerprint" — pitfalls to encode as tests)
    - tests/test_prompt.py (pure-fn class-grouped test layout to mirror)
  </read_first>
  <behavior>
    - normalize_title lowercases via casefold (NOT .lower()): "�straße" and "STRASSE" normalize equal
    - normalize_title applies NFKC: visually-identical composed/decomposed forms normalize equal
    - normalize_title strips punctuation and collapses whitespace: "SQL  injection!" == "sql injection"
    - fingerprint(path, title) is stable across repeated calls (determinism)
    - fingerprint is INVARIANT to severity/line/suggestion — those are not inputs at all; assert identity for the same (path, title) regardless of any other finding field
    - Different path OR different normalized title → different fingerprint
    - A fully reworded title yields a NEW fingerprint (by design, D-04) — assert two semantically-equal but textually-different titles differ
    - fingerprint output is a 16-char lowercase hex string
  </behavior>
  <action>Write failing pytest cases in tests/test_fingerprint.py importing `normalize_title` and `fingerprint` from `prevue.fingerprint` (module does not yet exist → RED). Group as a class mirroring test_prompt.py. Cover each behavior bullet above with a named test (e.g. test_casefold_unicode, test_nfkc_equivalence, test_punctuation_whitespace_collapse, test_determinism, test_excludes_line_severity_suggestion, test_reworded_title_is_new_fingerprint, test_digest_is_16_hex). Run the suite to confirm collection-time ImportError / failure (RED).</action>
  <verify>
    <automated>uv run pytest tests/test_fingerprint.py -x -q 2>&1 | grep -qE "(ImportError|ModuleNotFoundError|No module named 'prevue.fingerprint'|error|failed)" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fingerprint.py exists and imports from prevue.fingerprint
    - Running `uv run pytest tests/test_fingerprint.py -x -q` fails at collection or assertion (module/functions absent) — RED state confirmed
    - At least one test asserts severity/line/suggestion are excluded from identity
    - At least one test asserts casefold (not lower) and one asserts NFKC equivalence
  </acceptance_criteria>
  <done>Failing test module pins the D-04 contract before any implementation exists.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement fingerprint.py to pass the contract</name>
  <files>src/prevue/fingerprint.py, tests/test_fingerprint.py</files>
  <read_first>
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"NEW: fingerprint / normalize_title (D-04)" code shape + analog: pure-fn style of positions.commentable_lines and gate.SEVERITY_RANK constants)
    - src/prevue/gate.py (lines 1-16 — module-level constant + pure-fn style to mirror)
  </read_first>
  <action>Create src/prevue/fingerprint.py with `normalize_title(title: str) -> str` performing, in order: unicodedata.normalize("NFKC", title), casefold(), re.sub(r"[^\w\s]", "", t, flags=re.UNICODE) to strip punctuation, then re.sub(r"\s+", " ", t).strip() to collapse whitespace. Add `fingerprint(path: str, title: str) -> str` returning hashlib.sha256(f"{path}|{normalize_title(title)}".encode("utf-8")).hexdigest()[:16]. Stdlib only (hashlib, re, unicodedata) — no new dependency. Do NOT take severity/line/suggestion as parameters (D-04 excludes them by construction).</action>
  <verify>
    <automated>uv run pytest tests/test_fingerprint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_fingerprint.py -x -q` exits 0 (all GREEN)
    - `uv run pytest -k fingerprint` exits 0
    - src/prevue/fingerprint.py imports only stdlib (grep shows no third-party import)
    - `uv run ruff check src/prevue/fingerprint.py` passes
  </acceptance_criteria>
  <done>Deterministic fingerprint/normalize_title pass the full D-04 contract; commit as feat(08-01).</done>
</task>

<task type="auto">
  <name>Task 3: Shared compare-API + GraphQL JSON fixtures</name>
  <files>tests/fixtures/compare_ahead.json, tests/fixtures/compare_diverged.json, tests/fixtures/compare_identical.json, tests/fixtures/graphql_review_threads.json, tests/fixtures/graphql_resolve_ok.json, tests/fixtures/graphql_forbidden.json</files>
  <read_first>
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§"Pattern 1" compare fields: status/ahead_by/behind_by/merge_base_commit/files; §"Pattern 2" GraphQL query/mutation/threads shape)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"Test Conventions" — new fixtures list)
    - tests/fixtures/pulls_files.json (existing REST file-payload shape to mirror for compare .files)
  </read_first>
  <action>Create six JSON fixtures matching GitHub's wire shapes. compare_ahead.json: a `/compare` response with "status":"ahead", "ahead_by":1, "behind_by":0, "merge_base_commit":{"sha":"<lastSHA>"}, and "files":[...] (two files, mirror pulls_files.json field names: filename/status/additions/deletions/patch) where merge_base sha equals the stored last-reviewed SHA. compare_diverged.json: same shape but "status":"diverged" with a merge_base sha that differs from lastSHA (force-push case). compare_identical.json: "status":"identical","ahead_by":0,"behind_by":0,"files":[]. graphql_review_threads.json: the `data.repository.pullRequest.reviewThreads` shape with nodes carrying id/isResolved/isOutdated/path/line and comments(first:1).nodes[].body (include one already-resolved thread for idempotency tests). graphql_resolve_ok.json: `data.resolveReviewThread.thread.{id,isResolved:true}`. graphql_forbidden.json: an `{"errors":[{"type":"FORBIDDEN","message":"Resource not accessible by integration"}]}` body for the scope-failure path. Use realistic 40-hex SHAs consistent with test_diff.py constants where a value must match downstream (document the chosen lastSHA in a top-level comment-free convention note in the SUMMARY).</action>
  <verify>
    <automated>uv run python -c "import json,glob; [json.load(open(p)) for p in glob.glob('tests/fixtures/compare_*.json')+glob.glob('tests/fixtures/graphql_*.json')]; print('FIXTURES-VALID')"</automated>
  </verify>
  <acceptance_criteria>
    - All six fixtures parse as valid JSON (verify command prints FIXTURES-VALID)
    - compare_ahead.json has status=="ahead" and merge_base_commit.sha equal to its files' parent SHA convention; compare_diverged.json has status=="diverged" with a differing merge_base sha
    - graphql_review_threads.json contains at least one node with isResolved=true and one with isResolved=false (idempotency coverage downstream)
    - graphql_forbidden.json contains an errors array with a FORBIDDEN entry
  </acceptance_criteria>
  <done>Downstream plans (08-03 compare/scope, 08-04 GraphQL) can register these via `responses` without inventing payloads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output → finding title | Engine-derived `title` text becomes a fingerprint input |
| (no network in this plan) | Pure functions + static fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `normalize_title` on adversarial unicode title | mitigate | NFKC + casefold normalization is the documented contract; no eval/exec — pure string transform. Tested with unicode adversarial inputs. |
| T-08-02 | Spoofing | sha256 collision merging two distinct findings | accept | 16 hex = 64 bits; collision-free at PR scale (Assumptions Log A2). sha256 is the stdlib primitive — never hand-rolled (ASVS V6). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed (RESEARCH §Package Legitimacy Audit: N/A). Stdlib only. |
</threat_model>

<verification>
- `uv run pytest tests/test_fingerprint.py -q` green
- `uv run pytest -q` full suite still green (no regression from the new module)
- All six fixtures are valid JSON
</verification>

<success_criteria>
- normalize_title/fingerprint are deterministic, unicode-correct, and exclude line/severity/suggestion from identity (LIFE-02 / D-04)
- Six shared fixtures exist and parse, unblocking Waves 2–5 HTTP mocking
- No new third-party dependency introduced
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-01-SUMMARY.md` when done
</output>
