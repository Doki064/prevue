---
phase: 08-incremental-stateful-review-lifecycle
plan: 11
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/prevue/github/comments.py
  - src/prevue/review.py
  - tests/test_comments.py
  - tests/test_review_flow.py
autonomous: true
requirements: [LIFE-05]
must_haves:
  truths:
    - "D-13: on a FULL-scope review, a prior Prevue thread whose fingerprint the engine did NOT re-report this run is RESOLVED even when its line region did not change (engine silence over a fully re-read file = finding gone)"
    - "D-13: an INCREMENTAL run keeps the conservative D-09 region gate unchanged — a partially-read file's silence is not authoritative"
    - "D-13 (Pitfall 3): on a full run, a prior on a token-budget-skipped file (NOT in reviewed_paths) is NOT resolved — its silence is not authoritative because the engine never saw it"
    - "D-13: a still-present finding (engine re-emits same fingerprint after the code shifted to a new line) is NEVER resolved — resolution keys on fingerprint absence, and fingerprint excludes line number (D-04)"
  artifacts:
    - path: "src/prevue/github/comments.py"
      provides: "resolve_outdated_prior_findings authoritative flag that skips finding_region_changed on full runs (D-13)"
      contains: "authoritative"
    - path: "src/prevue/review.py"
      provides: "run_review passes authoritative=(scope=='full') and in_scope_paths=reviewed_paths on the full path"
      contains: "authoritative"
  key_links:
    - from: "src/prevue/review.py"
      to: "resolve_outdated_prior_findings"
      via: "authoritative=(scope == 'full') with delta_paths scoped to reviewed_paths"
      pattern: "authoritative"
---

<objective>
Land the machine half of breaking the false-positive treadmill (D-13): on a
FULL-scope review, resolve any prior Prevue thread the engine did not re-report,
dropping the D-09 region-change gate for full runs only. Incremental runs keep
the conservative gate untouched.

Purpose: This is the smallest, lowest-risk change in the added scope (a pure
predicate relaxation on a shipped, tested function plus a one-argument wiring
change in run_review). It must land FIRST (§L6) so the truly-fixed and flaky
findings clear themselves before the dismiss store and command surface build on
the same resolve seam.
Output: an `authoritative` flag on `resolve_outdated_prior_findings` + its
wiring from `run_review`'s scope decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH-LIFE0305.md
@src/prevue/github/comments.py
@src/prevue/review.py
</context>

<artifacts_this_phase_produces>
- `resolve_outdated_prior_findings` extended with a new keyword-only parameter
  `authoritative: bool = False` (`src/prevue/github/comments.py`) — when True,
  the `finding_region_changed` gate (line 293) is skipped; the other three
  conditions (in-scope path, fingerprint absent from current, thread exists and
  not already resolved) are unchanged.
- `run_review` wiring (`src/prevue/review.py`): the existing
  `resolve_outdated_prior_findings(...)` call (≈679-688) gains
  `authoritative=(scope == "full")`; `in_scope_paths` remains `delta_paths`
  which on the full path already equals `reviewed_paths` (packed files only,
  Pitfall 3).
- New/extended tests in `tests/test_comments.py` and `tests/test_review_flow.py`.
No new public symbols, no new module.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: authoritative flag on resolve_outdated_prior_findings (D-13 predicate)</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py (lines 247-302 resolve_outdated_prior_findings — the four current conditions: path in in_scope_paths (275), fingerprint not in current_fingerprints (282), finding_region_changed gate (293), thread exists + not already resolved (296); the PriorFinding/_thread_id_by_location helpers it relies on)
    - src/prevue/github/positions.py (finding_region_changed signature, C=3 — the gate D-13 relaxes for full runs)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH-LIFE0305.md (§"D-13 — Full-review-authoritative auto-resolve" — the exact relaxation: skip region check when authoritative; §Pitfall 3; §Landmine L2 — parameter change, not a parallel resolve path)
    - tests/test_comments.py (existing resolve_outdated tests + MagicMock PR / responses-mocked GraphQL fixtures: graphql_review_threads.json / graphql_resolve_ok.json)
  </read_first>
  <behavior>
    - authoritative=False (default): identical to today — resolve only when in-scope AND fingerprint-absent AND finding_region_changed True AND thread exists/not resolved
    - authoritative=True: resolve when in-scope AND fingerprint-absent AND thread exists/not resolved — the finding_region_changed check is skipped entirely
    - authoritative=True does NOT resolve a prior whose fingerprint IS in current_fingerprints (engine still emits it — line shifted, same fingerprint) — guaranteed by the unchanged fingerprint-absence condition
    - authoritative=True still respects in_scope_paths (a prior on a path absent from the set is never resolved)
    - An already-resolved thread (isResolved) is still skipped (idempotency unchanged)
  </behavior>
  <action>Add a keyword-only parameter `authoritative: bool = False` to `resolve_outdated_prior_findings` in comments.py. In the loop body, change the region gate at line 293 so the `if not finding_region_changed(stub, regions): continue` branch is taken only when `not authoritative` (i.e. `if not authoritative and not finding_region_changed(stub, regions): continue`). Leave the in_scope_paths check (275), the fingerprint-in-current check (282), the thread-exists/already-resolved check (296), and the resolve_review_thread best-effort call (298) untouched. Do NOT add a second resolve function (Landmine L2). Add tests to test_comments.py: a prior on an in-scope file whose region did NOT change and whose fingerprint is absent from current is resolved when authoritative=True but NOT resolved when authoritative=False; a prior whose fingerprint IS in current_fingerprints is NOT resolved under authoritative=True (line-shifted same-finding case); a prior on an out-of-scope path is NOT resolved under authoritative=True; an already-resolved thread is skipped.</action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "authoritative_resolve or resolve_keeps_present" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_comments.py -k authoritative_resolve -x` exits 0
    - With authoritative=True and an unchanged region, resolve_review_thread is invoked for a fingerprint-absent in-scope prior; with authoritative=False it is NOT invoked (assert mock call counts)
    - A prior whose fingerprint is in current_fingerprints is NOT resolved even when authoritative=True (line-shift case)
    - A prior on an out-of-scope path is NOT resolved when authoritative=True
    - All existing resolve_outdated_prior_findings tests still pass (authoritative defaults False — no regression)
  </acceptance_criteria>
  <done>resolve_outdated_prior_findings honors authoritative; D-13 predicate isolated and tested; commit as feat(08-11).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire authoritative resolve into run_review's full path (D-13)</name>
  <files>src/prevue/review.py, tests/test_review_flow.py</files>
  <read_first>
    - src/prevue/review.py (lines 553-556 reviewed_paths/delta_paths derivation — note delta_paths == reviewed_paths on the non-incremental/full path; lines 673-688 current_fps + regions_by_path + the resolve_outdated_prior_findings call gated by review_cfg.resolve_outdated; line 692 _open_set_findings consuming resolved_fps)
    - src/prevue/github/comments.py (resolve_outdated_prior_findings authoritative param from Task 1)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH-LIFE0305.md (§"D-13" caller wiring — on scope=='full' pass authoritative=True and in_scope_paths=reviewed_paths; §Pitfall 3 — do NOT pass the whole PR file set; §"Open-set consistency" — feeding the larger resolved_fps into the existing call makes the gate/sticky reflect cleared threads automatically)
    - tests/test_review_flow.py (existing full-review + incremental responses-mocked harness; graphql fixtures)
  </read_first>
  <behavior>
    - On scope=='full' (first run / diverged / force), resolve_outdated_prior_findings is called with authoritative=True; engine-silent in-scope priors are resolved without a region change
    - in_scope_paths for the full-run resolve is reviewed_paths (packed files only) via the existing delta_paths value — a budget-skipped file's prior is NOT resolved (Pitfall 3)
    - On scope=='incremental', authoritative=False (existing conservative D-09 behavior preserved)
    - resolved_fps flows into _open_set_findings unchanged, so the gate and sticky reflect the cleared threads (D-11 consistency)
    - review.resolve_outdated=false → no resolve calls on either scope (LIFE-04 opt-out preserved)
  </behavior>
  <action>In run_review, pass `authoritative=(scope == "full")` to the existing resolve_outdated_prior_findings call (≈679-688). Do not change in_scope_paths — it is already `delta_paths`, which equals `reviewed_paths` on the full path (553-556), satisfying Pitfall 3 (only files actually packed/reviewed are authoritative). Keep the `if review_cfg.resolve_outdated:` guard and the reused `threads=fetched_threads`. Make no change to the _open_set_findings call at 692. Add integration tests to test_review_flow.py: a full run where the engine omits a prior finding on a reviewed file resolves that thread even though no region changed (assert resolve_review_thread called); a full run does NOT resolve a prior on a file that was packed-out / not in reviewed_paths (assert resolve not called for it); an incremental run with an unchanged region still does NOT resolve (authoritative stays False).</action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py -k "full_resolve_scope or authoritative" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_review_flow.py -k full_resolve_scope -x` exits 0
    - On a full run, an engine-silent prior on a reviewed file is resolved with no region change (resolve_review_thread invoked)
    - On a full run, a prior on a budget-skipped file (absent from reviewed_paths) is NOT resolved
    - On an incremental run, an unchanged-region prior is still NOT resolved (D-09 conservative path intact)
    - `uv run pytest -q` full suite green
  </acceptance_criteria>
  <done>Full-review-authoritative resolve wired and false-resolve-safe on budget-skipped files (D-13); commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| full-run engine silence → thread resolve | Dropping the region gate must not resolve a still-valid finding |
| reviewed_paths scope → resolve authority | Only files the engine actually re-read are authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 | Tampering | authoritative resolve hiding a still-valid finding | mitigate | Resolution keys on fingerprint ABSENCE from current findings; a still-present finding re-emits the same fingerprint (line excluded, D-04) and is excluded — line-shift fixture proves it. |
| T-08-20 | Tampering | resolving a thread on a budget-skipped (not-reviewed) file on a full run (Pitfall 3) | mitigate | in_scope_paths = reviewed_paths (packed files only); a skipped file's prior is carried, not resolved — integration test asserts it. |
| T-08-21 | EoP | GraphQL resolve 403 stalling the run | mitigate | resolve_review_thread is best-effort (shipped); gated by review.resolve_outdated; run completes regardless. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH-LIFE0305 §Standard Stack: zero new installs). |
</threat_model>

<verification>
- `uv run pytest tests/test_comments.py -k authoritative_resolve -q` green
- `uv run pytest tests/test_review_flow.py -k "full_resolve_scope or authoritative" -q` green
- A full run clears an engine-silent prior with no region change; a budget-skipped file's prior survives
- `uv run pytest -q` full suite green (no regression in incremental D-09 path)
</verification>

<success_criteria>
- Full runs resolve engine-silent priors regardless of region change (D-13)
- Incremental runs keep the conservative D-09 gate (D-13 / LIFE-04)
- Budget-skipped files on full runs are never falsely resolved (Pitfall 3)
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-11-SUMMARY.md` when done
</output>
