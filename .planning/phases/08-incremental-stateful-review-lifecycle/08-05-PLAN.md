---
phase: 08-incremental-stateful-review-lifecycle
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - src/prevue/github/comments.py
  - tests/test_comments.py
autonomous: true
requirements: [LIFE-02, LIFE-04]
must_haves:
  truths:
    - "On an incremental run, prior Prevue comments on files NOT re-reviewed this run are left fully untouched (no edit, no delete)"
    - "A same-fingerprint match keeps the existing comment as-is (no churn) unless severity escalated, in which case it is refreshed in place"
    - "An outdated prior finding (in-scope file AND region changed AND fingerprint not in current findings) has its review thread RESOLVED, not deleted"
    - "Own same-run duplicate comments are still hard-deleted (existing dedupe path preserved)"
    - "Prior findings + their severities are re-derived from live comments — no stored state"
  artifacts:
    - path: "src/prevue/github/comments.py"
      provides: "scoped carry-forward stale set (D-05), escalation-only refresh (D-06), outdated→resolve branch (D-08), prior-finding re-derivation (D-01)"
      contains: "in_scope_paths"
  key_links:
    - from: "src/prevue/github/comments.py"
      to: "resolve_review_thread"
      via: "outdated finding → GraphQL resolve instead of delete"
      pattern: "resolve_review_thread"
    - from: "src/prevue/github/comments.py"
      to: "parse_severity_from_body"
      via: "escalation check + open-set severity re-derivation"
      pattern: "parse_severity_from_body"
---

<objective>
Make the surgical changes to comments.py's reconciliation: scope the carry-forward
stale set to in-scope files only (D-05), refresh a matched comment only on severity
escalation (D-06), resolve (not delete) outdated threads (D-08/D-09), and expose
the re-derived prior findings + severities the gate open-set needs (D-01/D-12).

Purpose: This is the LIFE-02/LIFE-04 behavior change — but per RESEARCH "Key
insight" it is a surgical scoping edit to post_inline_review's stale-set
computation and its delete-vs-resolve branch, NOT a rewrite. It depends on every
Wave-1/2 unit: fingerprint (08-01), severity parse-back + region helper (08-02),
marker SHA (08-03), and the GraphQL resolve helper (08-04). It touches comments.py
(also touched in Waves 1–2) so it must run after them (Wave 3).
Output: scoped/escalation-aware/resolve-aware post_inline_review + a prior-finding
re-derivation helper in comments.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md
@src/prevue/github/comments.py
@.planning/phases/08-incremental-stateful-review-lifecycle/08-02-SUMMARY.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-03-SUMMARY.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan creates/modifies the following symbols (the convergence pass: callers
fingerprint/parse_severity_from_body/regions_changed/resolve_review_thread are
created in 08-01/02/04, not drift):
- `post_inline_review` extended with an `in_scope_paths` parameter and an escalation-only
  refresh branch + outdated→resolve branch (D-05/D-06/D-08/D-09) in `comments.py`.
- A NEW helper `derive_prior_findings(pr) -> list[...]` (or similarly named) re-deriving prior
  Prevue inline findings as (path, line, side, fingerprint, severity, thread-id-key) from live
  comments via `_existing_prevue_inline_by_location` (D-01/D-12) — the source for the gate open-set
  and the known-issues list.
- New carry-forward/escalation/resolve tests in `tests/test_comments.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoped carry-forward + escalation-only refresh (D-05/D-06)</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py (lines 388-470 post_inline_review; specifically 397-406 stale-set computation, 412-428 the upsert/update loop, 437-448 the resilient edit-then-create-then-cleanup ordering; 347-370 _is_prevue_inline_comment + _existing_prevue_inline_by_location re-derivation source; line 20 SEVERITY_BADGES + the new parse_severity_from_body from 08-02)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"comments.py — scoped carry-forward + escalation refresh (D-05/D-06)" — the exact stale_comments scoping snippet keying on key[0] in in_scope_paths; escalation check in the to_update branch)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§Pitfall 2 — two-file fixture where push 2 touches only file A; §D-06 — keep as-is unless severity escalated)
    - tests/test_comments.py (TestPostInlineReview — MagicMock PR/comment helpers, _finding/_gate helpers, capsys for stderr assertions)
  </read_first>
  <behavior>
    - Scoped stale set: post_inline_review takes an in_scope_paths set; a prior comment whose path is NOT in in_scope_paths is never added to to_delete and never edited (carry-forward, D-05)
    - Two-file scenario: push 2 reviews only file A; a prior comment on file B survives untouched (no edit, no delete)
    - Escalation: a same-(path,line,side) prior comment whose parsed severity EQUALS the new finding's severity is kept as-is — no .edit() call (no churn, D-06)
    - Escalation: a prior comment whose severity is LOWER than the new finding's (e.g. warning→error) IS refreshed in place via .edit()
    - Own same-run duplicate (>1 comment at a key) → extras still deleted (existing path preserved)
    - Full-review path (in_scope_paths = all files, or a sentinel meaning "all") behaves exactly as today — no regression
  </behavior>
  <action>Extend post_inline_review with an `in_scope_paths: set[str] | None` parameter (None = full review = today's behavior, all paths in scope). Change the stale_comments comprehension (currently 401-406) to additionally require `key[0] in in_scope_paths` when in_scope_paths is not None — so comments on out-of-scope files are never staled (D-05). In the to_update branch (415-419), before scheduling an edit, parse the prior comment's severity via parse_severity_from_body (from 08-02); compare against the new finding's severity using gate.SEVERITY_RANK — if the prior severity rank <= new rank (same or already worse), SKIP the edit (keep as-is, D-06); only schedule the edit when the new finding's severity is more severe (escalation). Preserve the existing same-run duplicate deletion (418-419) and the resilient edit→create→cleanup ordering + failed-key downgrade set (433-470) unchanged. Add tests to TestPostInlineReview: a two-file MagicMock scenario asserting file B's comment is untouched; an equal-severity match asserting no .edit(); a warning→error escalation asserting .edit() is called; a duplicate-at-key asserting the extra is deleted.</action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "carry_forward or escalation or scoped" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_comments.py -k carry_forward` exits 0: a comment on an out-of-scope file is neither edited nor deleted
    - `uv run pytest -k escalation` exits 0: equal-severity match → no .edit(); warning→error → .edit() called
    - Existing TestPostInlineReview tests (full-review path with in_scope_paths=None) still pass — no regression
    - Own same-run duplicate deletion still works
  </acceptance_criteria>
  <done>Carry-forward is scoped to in-scope files and matches refresh only on escalation (D-05/D-06); commit as feat(08-05).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Outdated→resolve branch + prior-finding re-derivation (D-08/D-09/D-01/D-12)</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py (lines 347-370 _existing_prevue_inline_by_location + inline_location_key — the re-derivation source; 373-385 _delete_prevue_inline_comments best-effort posture)
    - src/prevue/github/graphql.py (fetch_review_threads + resolve_review_thread from 08-04 — thread ids keyed by path/line; isResolved for idempotency skip)
    - src/prevue/fingerprint.py (fingerprint from 08-01); src/prevue/github/positions.py (finding_region_changed from 08-02)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§Shared Patterns "Best-effort GitHub I/O" + "Idempotent upsert"; §"comments.py — scoped carry-forward" outdated→resolve note)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§Pattern 5 conservative D-09 triple condition; §Pitfall 3 idempotency: check isResolved before mutation)
  </read_first>
  <behavior>
    - A NEW re-derivation helper returns prior Prevue inline findings from live comments as records carrying path/line/side, fingerprint(path, title-from-body), parsed severity, and the matching review-thread id
    - Outdated trigger (D-09, all three must hold): file in in_scope_paths AND finding_region_changed True AND fingerprint NOT in the current run's finding fingerprints → the thread is RESOLVED via resolve_review_thread (not deleted)
    - A still-valid prior finding on an untouched (out-of-scope) file is NEVER resolved (conservative)
    - A prior finding whose region did NOT change is NEVER resolved (carry-forward keeps it)
    - An already-resolved thread (isResolved=true from fetch) is skipped — no second resolve call (idempotency)
    - A resolve failure (403/GraphQL error from 08-04) is logged + skipped; the run continues and other reconciliation still completes
  </behavior>
  <action>Add a re-derivation helper to comments.py (e.g. `derive_prior_findings(pr) -> list[PriorFinding]`) built on _existing_prevue_inline_by_location: for each trusted prior inline comment, extract its title from the rendered first line, compute fingerprint(path, title) (08-01), parse severity via parse_severity_from_body (08-02), and pair it with the review-thread id from fetch_review_threads (08-04) matched by (path, line). Add an outdated-resolution step (a function or a branch invoked from post_inline_review / the orchestrator in Plan 06): for each prior finding, RESOLVE its thread only when ALL of: comment path in in_scope_paths, finding_region_changed(prior, regions_for_that_file) is True (regions from the incremental patch — the orchestrator supplies them), and the prior fingerprint is not among the current run's fingerprints (D-09). Skip when the thread isResolved is already true (Pitfall 3). Use resolve_review_thread (best-effort — already returns False without raising on 403). Keep hard-delete ONLY for own same-run duplicates (D-08). Add tests with MagicMock PRs + responses-mocked GraphQL (graphql_review_threads.json / graphql_resolve_ok.json / graphql_forbidden.json fixtures): assert resolve called only when all three D-09 conditions hold; assert an untouched-file prior is not resolved; assert an unchanged-region prior is not resolved; assert an already-resolved thread is skipped; assert a 403 is logged and the run proceeds.</action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "outdated or resolve or prior_finding" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_comments.py -k outdated` exits 0: resolve is invoked ONLY when in-scope AND region-changed AND fingerprint-not-in-current (all three)
    - A prior finding on an out-of-scope file and a prior finding on an unchanged region are both NOT resolved
    - An isResolved=true thread is not re-resolved (idempotency)
    - A FORBIDDEN/403 resolve is logged to stderr and the run continues (capsys assertion); no exception propagates
    - Own same-run duplicates are still hard-deleted (not resolved)
  </acceptance_criteria>
  <done>Outdated threads resolve conservatively and idempotently; prior findings/severities re-derived from live comments only (D-01/D-08/D-09/D-12); commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PR inline comments → prior findings | Comment bodies (untrusted) re-derived into fingerprints + severities |
| in_scope_paths set → stale-cleanup scope | Determines which comments may be deleted/resolved |
| GraphQL resolve → best-effort cleanup | A scope-403 must not strand the run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 (data loss) | DoS (to review signal) | unscoped stale-cleanup wiping valid comments on untouched files | mitigate | Scope stale set to in_scope_paths (D-05). Two-file regression test proves out-of-scope comments survive. |
| T-08-12 | Tampering | over-eager resolve hiding a still-valid finding | mitigate | D-09 conservative triple condition (in-scope AND region-changed AND fingerprint-absent); a false negative leaves a stale comment (safe), a false positive is prevented by requiring all three. C=3 context window biases toward NOT resolving. |
| T-08-13 | EoP | resolve 403 (possible contents:write scope) failing the run | mitigate | resolve_review_thread is best-effort (08-04 returns False on 403); this plan never re-raises. Run continues; scope verification + opt-out gated in Plan 07. |
| T-08-14 | Tampering | malicious comment body forcing a wrong fingerprint/severity | mitigate | fingerprint/parse_severity are pure pattern-matchers over the leading badge/title; no eval. Unknown severity → None handled conservatively by the gate (08-03). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `uv run pytest tests/test_comments.py -q` green (carry-forward, escalation, outdated-resolve, idempotency)
- Two-file fixture proves out-of-scope comments are untouched
- 403 resolve does not fail the run
- `uv run pytest -q` full suite green (no regression in existing post_inline_review behavior)
</verification>

<success_criteria>
- Carry-forward scoped to in-scope files; matched comments refreshed only on escalation (LIFE-02 / D-05/D-06)
- Outdated threads resolved (not deleted), conservatively and idempotently, best-effort (LIFE-04 / D-08/D-09)
- Prior findings + severities re-derived from live comments — zero stored state (D-01/D-12)
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-05-SUMMARY.md` when done
</output>
