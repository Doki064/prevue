---
phase: 08-incremental-stateful-review-lifecycle
plan: 07
type: execute
wave: 5
depends_on: ["08-06"]
files_modified:
  - .github/workflows/prevue-review.yml
  - docs/consumer-setup.md
  - tests/test_reusable_workflow_yaml.py
autonomous: false
requirements: [LIFE-04, LIFE-01]
must_haves:
  truths:
    - "The reusable workflow's token scope (contents: read, pull-requests: write, checks: write) is verified on a LIVE runner against resolveReviewThread — confirmed working OR confirmed 403"
    - "If 403: the run still completes (LIFE-01/02 green) via best-effort skip; review.resolve_outdated documented as the opt-out"
    - "Incremental scoping is confirmed across a real second push on a sandbox PR (only the changed file re-reviewed, marker SHA advances)"
    - "Consumer docs state the resolve_outdated scope caveat and the incremental/resolve_outdated/max_known_issues knobs"
  artifacts:
    - path: ".github/workflows/prevue-review.yml"
      provides: "verified minimal token scope; no silent broadening to contents: write"
      contains: "pull-requests: write"
    - path: "docs/consumer-setup.md"
      provides: "documented incremental + resolve_outdated scope caveat + new config knobs"
      contains: "resolve_outdated"
  key_links:
    - from: ".github/workflows/prevue-review.yml"
      to: "permissions block"
      via: "contents: read + pull-requests: write + checks: write (unchanged unless user approves)"
      pattern: "permissions:"
---

<objective>
Settle the dominant phase risk on a live runner: does `resolveReviewThread`
work with the minimal scope (`pull-requests: write`) or does it require
`contents: write` (RESEARCH Open Question #1, UNRESOLVED publicly)? Confirm the
full incremental lifecycle on a real sandbox PR, and document the consumer-facing
knobs and the scope caveat.

Purpose: The scope question cannot be unit-mocked — it needs the real GraphQL
endpoint on a live runner (VALIDATION.md Manual-Only). The implementation
(08-04/05/06) already fails safe (best-effort 403-skip + review.resolve_outdated
opt-out) so LIFE-01/02 ship regardless. This plan VERIFIES that posture live and,
if contents: write proves mandatory, escalates the scope-vs-feature tradeoff to
the user (Claude has no authority to broaden the scope unilaterally — WKFL-04).
Depends on 08-06 (the resolve path must exist end-to-end to test).
Output: a verified workflow permissions block + consumer docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-VALIDATION.md
@.github/workflows/prevue-review.yml
@docs/consumer-setup.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan modifies the workflow permissions documentation/comments and consumer
docs. No new code symbols. It may add an assertion to
`tests/test_reusable_workflow_yaml.py` that the permissions block stays minimal
(contents: read, pull-requests: write, checks: write).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Assert minimal token scope unchanged + document the new knobs</name>
  <files>tests/test_reusable_workflow_yaml.py, docs/consumer-setup.md, .github/workflows/prevue-review.yml</files>
  <read_first>
    - .github/workflows/prevue-review.yml (the permissions: block — currently contents: read / pull-requests: write / checks: write at lines ~26-29)
    - tests/test_reusable_workflow_yaml.py (existing YAML-structure assertions to extend)
    - docs/consumer-setup.md (existing consumer config + skip≠auto-merge section style to mirror)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§Open Q #1/#2/#3 — defaults: review.incremental true, review.resolve_outdated true, review.max_known_issues 20; the scope caveat)
  </read_first>
  <action>Add a test to tests/test_reusable_workflow_yaml.py asserting the prevue-review.yml permissions block is exactly contents: read, pull-requests: write, checks: write (no contents: write) — this guards against silent scope broadening (WKFL-04). Add a comment in prevue-review.yml above the permissions block noting that resolveReviewThread (LIFE-04) runs under pull-requests: write and degrades gracefully to best-effort skip if the live scope proves insufficient (links the opt-out). Extend docs/consumer-setup.md with: (1) the three new review knobs — incremental (default true; set false to force whole-PR review every push), resolve_outdated (default true; set false to disable LIFE-04 outdated-thread resolution), max_known_issues (default 20; cap on the engine dedupe hint list); (2) a short "Outdated thread resolution scope" note stating LIFE-04 needs no extra token scope beyond pull-requests: write under normal operation, and that if a consumer's environment returns 403 the review still completes and they can set review.resolve_outdated: false. Do NOT add contents: write to the workflow.</action>
  <verify>
    <automated>uv run pytest tests/test_reusable_workflow_yaml.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_reusable_workflow_yaml.py` exits 0 with a test asserting permissions == {contents: read, pull-requests: write, checks: write}
    - prevue-review.yml contains NO `contents: write` (grep returns nothing)
    - docs/consumer-setup.md documents review.incremental, review.resolve_outdated, review.max_known_issues with their defaults and the scope caveat
    - `uv run pytest -q` full suite green
  </acceptance_criteria>
  <done>Minimal scope is test-enforced and the new knobs + scope caveat are documented; commit as docs(08-07).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
  The full incremental lifecycle (08-01..08-06) plus a workflow whose permissions
  remain contents: read / pull-requests: write / checks: write. The resolve path
  is best-effort (403 → skip) and opt-out-able (review.resolve_outdated).
  </what-built>
  <how-to-verify>
  On a sandbox repo where you can push and trigger the Prevue reusable workflow
  (the dogfood review.yml caller is fine):

  1. SCOPE VERIFICATION (the dominant risk — RESEARCH Open Question #1):
     a. Open a PR; let Prevue post at least one inline finding.
     b. Push a second commit that changes the lines under that finding so it
        becomes outdated (in-scope file, region changed, no longer reported).
     c. In the workflow run logs, confirm one of:
        - SUCCESS: the inline thread is RESOLVED (collapsed) on the PR, and the
          logs show no GraphQL error. → resolveReviewThread works with
          pull-requests: write. Record this in the SUMMARY.
        - 403/FORBIDDEN: the logs show "prevue: review thread resolve failed
          (... 403 ...)" AND the run still finishes green for LIFE-01/02 (sticky
          + check published, marker advanced). → confirms the best-effort skip.
          Record that resolve_outdated requires a broader scope in this account.

  2. INCREMENTAL SCOPING (LIFE-01):
     a. On the same PR, note the head=<sha> in the sticky comment marker.
     b. Push a third commit touching only ONE file.
     c. Confirm the new run re-reviews only that file (the Coverage/Metadata
        lines and the engine token count should reflect one file, not the whole
        PR) and the sticky marker head=<sha> advanced to the new head.

  3. NO-OP RE-RUN: re-run the workflow on the same head with no new commits;
     confirm the engine is NOT re-invoked (logs) and no duplicate comments appear.
  </how-to-verify>
  <on-403-escalation>
  If step 1c is 403 (contents: write appears mandatory): this collides with the
  explicit "Out of Scope: requires contents: write" prohibition. Do NOT broaden
  the scope. Surface the tradeoff to the user — (a) ship LIFE-04 disabled by
  default via review.resolve_outdated and document the scope requirement, or
  (b) the user explicitly opts to grant contents: write. The decision is the
  user's, not Claude's (WKFL-04). Record the chosen path in the SUMMARY.
  </on-403-escalation>
  <resume-signal>Type "approved" (and note SUCCESS or 403 for the resolve scope), or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow permissions block | The least-privilege boundary consumers audit and trust |
| live GraphQL endpoint | The real scope behavior of resolveReviewThread (unverified publicly) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-19 (scope escalation) | EoP | broadening token scope to contents: write for LIFE-04 | mitigate | Workflow scope is test-pinned to contents: read / pull-requests: write / checks: write; the live checkpoint confirms whether that suffices; any broadening requires explicit user approval (WKFL-04), never a silent Claude edit. |
| T-08-20 | Information Disclosure | live-run logs leaking the token | accept | Existing token redaction (T-05) applies; the checkpoint reads only status/error-type log lines. No new logging added. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages. |
</threat_model>

<verification>
- `uv run pytest tests/test_reusable_workflow_yaml.py -q` green; permissions block test-pinned minimal
- Live sandbox: resolve scope determined (SUCCESS or 403 with graceful skip)
- Live sandbox: incremental scoping + marker advance confirmed; no-op re-run confirmed
- `uv run pytest -q` full suite green
</verification>

<success_criteria>
- The resolveReviewThread token scope is settled on a live runner; if 403, LIFE-01/02 still ship green and resolve_outdated is the documented opt-out (LIFE-04 / WKFL-04 / Open Q #1)
- Incremental scoping verified end-to-end on a real second push (LIFE-01)
- Workflow scope test-pinned minimal; consumer docs cover the new knobs + scope caveat
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-07-SUMMARY.md` when done
</output>
