---
phase: 08-incremental-stateful-review-lifecycle
plan: 06
type: execute
wave: 4
depends_on: ["08-03", "08-04", "08-05"]
files_modified:
  - src/prevue/review.py
  - tests/test_review_flow.py
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-04]
must_haves:
  truths:
    - "On a second push to an active PR, only files changed since the last-reviewed SHA are classified and reviewed (the dominant token win)"
    - "The new head SHA is written into the sticky marker after each run"
    - "A force-push falls back to a full review and resets the marker to head"
    - "An identical re-run (status=identical / lastSHA==head) is a no-op: marker/check refreshed, engine not re-invoked, threads not re-resolved"
    - "The gate verdict is computed over the union of new + carried-unresolved prior findings (false-green-proof)"
    - "The known-issues list passed to the engine is bound to in-scope-file priors only, capped at review.max_known_issues"
  artifacts:
    - path: "src/prevue/review.py"
      provides: "incremental orchestration: read marker → decide_scope → scoped pack/classify → re-derive priors → known-issues → engine → reconcile/resolve → gate-over-open-set → write marker"
      contains: "decide_scope"
  key_links:
    - from: "src/prevue/review.py"
      to: "parse_marker_sha / decide_scope"
      via: "read sticky marker SHA, classify scope before fetch/pack"
      pattern: "decide_scope"
    - from: "src/prevue/review.py"
      to: "render_marker"
      via: "write new head SHA into the sticky body"
      pattern: "render_marker"
    - from: "src/prevue/review.py"
      to: "apply_gate over open set"
      via: "union(new, carried-unresolved) − resolved-this-run"
      pattern: "carried"
---

<objective>
Wire all the Wave 1–3 units into run_review: read the marker SHA, decide
full-vs-incremental, scope packing/classification to in-scope files, re-derive
prior findings, assemble the known-issues list, run the engine, reconcile
(carry-forward + escalation + resolve outdated), gate over the open set, and
write the new head SHA into the marker.

Purpose: This is the integration seam (type: execute glue, per the planning
brief). All decisions are made by the tested units; this plan sequences them
idempotently. The sequencing must ensure the paid LLM classify fallback only
touches in-scope files (Discretion: mirror P7 packed-set sequencing). Depends on
08-03 (scope/marker/gate/config), 08-04 (known-issues prompt + GraphQL), and
08-05 (reconciliation + prior re-derivation). It owns review.py exclusively.
Output: incremental orchestration in run_review + integration tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md
@src/prevue/review.py
@.planning/phases/08-incremental-stateful-review-lifecycle/08-03-SUMMARY.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-04-SUMMARY.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan modifies `run_review` in `review.py` only (no new public symbols). It
WIRES the symbols created in 08-01..08-05 (parse_marker_sha, render_marker,
decide_scope, the incremental fetch path, fingerprint, the known-issues prompt
helper, fetch_review_threads/resolve_review_thread, derive_prior_findings, the
scoped/escalation/outdated reconciliation, apply_gate-over-open-set). New
integration tests added to `tests/test_review_flow.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read marker → decide_scope → scope the packed/classified set</name>
  <files>src/prevue/review.py, tests/test_review_flow.py</files>
  <read_first>
    - src/prevue/review.py (lines 102-143 setup: load_pr_context, fork guard, load_config, should_skip, fetch_diff; lines 133-356 filter/pack/classify machinery to scope to in-scope files; lines 316-356 classify + LLM fallback — must run only over in-scope files)
    - src/prevue/github/comments.py (parse_marker_sha from 08-03; _existing_prevue_inline_by_location / the sticky body read source); src/prevue/github/diff.py (decide_scope + incremental fetch from 08-03)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"review.py — orchestration sequencing" — insert seam after get_authenticated_pull and skip checks, before fetch_diff; LLM fallback only over in-scope files)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§Architecture diagram — the full ordered seam; §Pitfall 3 idempotency: identical → no-op; §Pitfall 5 file-SET only)
    - tests/test_review_flow.py (existing responses-mocked end-to-end harness + fixtures; tests/fixtures/compare_ahead.json / compare_identical.json from 08-01)
  </read_first>
  <behavior>
    - Read the existing sticky comment body, parse_marker_sha → last_sha (None when no marker / legacy)
    - decide_scope(repo, last_sha, head_sha): full (first run / diverged), incremental (ahead + ancestor), noop (identical / lastSHA==head)
    - Incremental: only files in the in-scope set are filtered/packed/classified — the LLM classify fallback never touches out-of-scope files (gate the fallback on in-scope files only)
    - noop: refresh marker + check, do NOT re-invoke the engine, do NOT re-resolve threads (early idempotent return after marker/check refresh)
    - full: today's whole-PR path unchanged
    - review.incremental=false config → force full review even when a valid ancestor marker exists (opt-out knob honored)
  </behavior>
  <action>Insert the scope-decision seam in run_review after `pr = get_authenticated_pull(ctx)` (118) and the skip checks, before/around `fetch_diff` (133). Read the existing sticky body (reuse the comment-enumeration path), parse_marker_sha → last_sha. Call decide_scope(get_repo(ctx), last_sha if review_cfg.incremental else None, diff head sha) — passing None when review.incremental is False forces full (opt-out). On "noop", refresh the sticky marker (render_marker(head)) + republish the check and return early (no engine spend, no resolve). On "incremental", use the incremental fetch path (08-03) so `reduced`/`packed_files`/classify operate only over the in-scope file set — keeping the existing filter→pack→classify→fallback machinery but feeding it only in-scope files (LLM fallback never sees out-of-scope files). On "full", keep today's fetch_diff path. Add integration tests in test_review_flow.py: a first-run full review (no marker) writes a head= marker; a second push (compare_ahead) reviews only the in-scope file and the engine prompt excludes out-of-scope files; an identical re-run (compare_identical) does not invoke the engine; review.incremental=false forces full.</action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py -k "incremental or noop or scope or full_first_run" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_review_flow.py -k incremental` exits 0: an incremental run packs/classifies only in-scope files (assert the engine prompt / packed set excludes the untouched file)
    - An identical re-run does not call engine.review (assert via the mock adapter call count) and still refreshes the marker/check
    - review.incremental=false forces a full review despite a valid ancestor marker
    - First run with no marker performs a full review and the resulting sticky body contains a head=<sha> marker
  </acceptance_criteria>
  <done>run_review reads the marker and scopes review to the incremental file set; identical re-runs are no-ops; commit as feat(08-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derive priors → known-issues → reconcile/resolve → gate-over-open-set → write marker</name>
  <files>src/prevue/review.py, tests/test_review_flow.py</files>
  <read_first>
    - src/prevue/review.py (lines 316-369 classify→prompt→engine; 369-406 apply_gate + post_inline_review + failed-key downgrade; 407-463 sticky upsert + check publish — the existing write order inline → sticky → check to preserve)
    - src/prevue/github/comments.py (derive_prior_findings + scoped/escalation/outdated post_inline_review from 08-05); src/prevue/engines/prompt.py (known-issues helper from 08-04); src/prevue/github/graphql.py (fetch_review_threads/resolve from 08-04); src/prevue/gate.py (apply_gate open-set input + max_known_issues knob from 08-03); src/prevue/fingerprint.py (08-01); src/prevue/github/positions.py (regions_changed for the outdated trigger, 08-02)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"review.py — orchestration sequencing" — assemble open-set union + resolve-outdated pass before upsert_sticky; §"gate.py — gate over open set" union snippet)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§Architecture diagram steps RE-DERIVE → KNOWN-ISSUES → RECONCILE → GATE → WRITE MARKER; §Pitfall 6 false-green)
  </read_first>
  <behavior>
    - On incremental runs, prior findings are re-derived from live comments (derive_prior_findings); the known-issues list = in-scope-file priors only, capped at review.max_known_issues, passed to build_prompt (fenced)
    - The engine runs over the in-scope packed files with the known-issues list; current findings get fingerprints
    - Reconciliation calls post_inline_review with in_scope_paths and the regions-changed data so carry-forward/escalation/outdated-resolve behave per 08-05; resolve gated on review.resolve_outdated
    - The gate is computed over union(current findings, carried-unresolved priors) − threads resolved this run (D-11) — a clean push over an unresolved prior error does NOT conclude success
    - After the gate/sticky/check, the new head SHA is written into the marker via render_marker on the full-review path AND incremental path; full-review-after-force-push resets the marker to head
    - review.resolve_outdated=false → no resolve calls; LIFE-01/02 still run (LIFE-04 cleanly disabled)
  </behavior>
  <action>After decide_scope yields incremental/full, before the engine call: when incremental, call derive_prior_findings(pr) (08-05), build the known-issues items (path, line, title) bound to in-scope-file priors, cap at review.max_known_issues, and pass them to build_prompt (08-04). After engine.review, compute fingerprints for current findings (fingerprint, 08-01). Replace the existing post_inline_review call (383) with the 08-05 signature passing in_scope_paths and the incremental per-file regions (regions_changed, 08-02) so carry-forward/escalation/outdated-resolve apply; gate the resolve pass on review.resolve_outdated. Assemble the open-set union: carried-unresolved priors (fingerprint not in resolved-this-run, not in current findings) + current findings, then call apply_gate over that union (08-03 / D-11) so the verdict reflects all open findings. Keep the existing failed-inline-key downgrade (384-406), sticky retry (434-450), and write order inline→sticky→check. Write the marker with head=<diff.head_sha> via render_marker in the sticky body (extend the upsert_sticky/render_body path to embed the marker SHA, or upsert the marker after the sticky write) on BOTH full and incremental paths. Add integration tests: a clean incremental push with an unresolved prior error → check conclusion not success (false-green-proof); review.resolve_outdated=false → zero resolve calls; the sticky body after a run contains head=<head_sha>; a force-push run resets the marker and does a full review.</action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py -k "open_set or known_issues or marker_write or resolve_opt_out or force_push" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - A clean incremental push (no new findings) with one carried unresolved error concludes NOT "success" (false-green-proof, D-11) — integration assertion on the check conclusion
    - The engine prompt on an incremental run contains the fenced known-issues list capped at review.max_known_issues (assert substring + cap)
    - review.resolve_outdated=false → resolve_review_thread is never called (assert mock call count 0); LIFE-01/02 still complete
    - After any run, the upserted sticky body matches the head=<head_sha> marker (parse_marker_sha(body) == head_sha)
    - `uv run pytest tests/test_review_flow.py -q` green; `uv run pytest -q` full suite green
  </acceptance_criteria>
  <done>Full incremental lifecycle wired end-to-end and false-green-proof; marker advances each run; LIFE-04 cleanly opt-out-able; commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sticky marker → scope decision | The persisted SHA drives full-vs-incremental; a bogus value must fail safe |
| prior comments → known-issues + open-set | Untrusted live-comment text feeds the prompt and the verdict |
| GraphQL resolve → cleanup | Best-effort; opt-out via review.resolve_outdated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 (false-green) | Tampering/EoP | gate over an incremental run | mitigate | apply_gate is fed union(current, carried-unresolved) − resolved-this-run; integration test proves a clean push over an unresolved error is not green (D-11). HIGH per security_block_on. |
| T-08-16 | Tampering | bogus marker SHA driving a nonsense incremental range | mitigate | decide_scope (08-03) fails safe to full on any non-ahead status; parse_marker_sha returns None on non-hex; review.incremental=false forces full. |
| T-08-17 (prompt injection) | Tampering/EoP | known-issues list from prior comments fed to the engine | mitigate | List built via the 08-04 fenced UNTRUSTED-DATA helper; bound to in-scope priors, capped at N. |
| T-08-18 | EoP | resolve scope-403 stalling the whole run | mitigate | resolve is best-effort (08-04/08-05) and gated by review.resolve_outdated; the run completes LIFE-01/02 regardless. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `uv run pytest tests/test_review_flow.py -q` green (full first run, incremental, noop, force-push, open-set, resolve opt-out, marker write)
- A clean incremental push over an unresolved prior error is not green (D-11 integration proof)
- The marker SHA advances to head after every run
- `uv run pytest -q` full suite green
</verification>

<success_criteria>
- Second pushes review only changed-since-lastSHA files; identical re-runs are no-ops (LIFE-01)
- Known-issues + carry-forward dedupe wired; gate over open set false-green-proof (LIFE-02 / D-11)
- Outdated threads resolved on incremental runs, opt-out-able (LIFE-04)
- Marker advances to head each run; force-push resets to full
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-06-SUMMARY.md` when done
</output>
