---
phase: 08-incremental-stateful-review-lifecycle
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/prevue/github/comments.py
  - src/prevue/github/positions.py
  - tests/test_comments.py
  - tests/test_positions.py
autonomous: true
requirements: [LIFE-04]
must_haves:
  truths:
    - "Severity can be read back from a live inline comment body via its leading badge (🔴/🟡/🔵 → error/warning/info)"
    - "parse_severity_from_body(render_inline_comment(f)) == f.severity for every severity (round-trip contract)"
    - "An unparseable / human / legacy comment body returns None (fail-safe; never silently mislabels)"
    - "A prior finding's line region is 'changed' only when an incremental hunk overlaps [line-C, line+C]"
  artifacts:
    - path: "src/prevue/github/comments.py"
      provides: "BADGE_TO_SEVERITY inverse map + parse_severity_from_body (D-12)"
      contains: "def parse_severity_from_body"
    - path: "src/prevue/github/positions.py"
      provides: "regions_changed + finding_region_changed hunk-overlap helpers (D-09)"
      contains: "def finding_region_changed"
  key_links:
    - from: "src/prevue/github/comments.py"
      to: "SEVERITY_BADGES"
      via: "BADGE_TO_SEVERITY = inverse of SEVERITY_BADGES"
      pattern: "BADGE_TO_SEVERITY"
    - from: "src/prevue/github/positions.py"
      to: "unidiff.PatchSet"
      via: "parse incremental patch hunks for target-line ranges"
      pattern: "PatchSet"
---

<objective>
Build the two remaining pure units for LIFE-04: severity parse-back (D-12, the
tested inverse of SEVERITY_BADGES) and the line-region-changed hunk-overlap
heuristic (D-09).

Purpose: Both are pure functions with precise I/O contracts (TDD candidates per
RESEARCH.md/VALIDATION.md). Severity parse-back lets D-11's gate-over-open-set
recover prior severities from live comments with NO extra storage (consistent
with the SHA-only marker). Region-changed is the middle condition of the
conservative D-09 outdated trigger. They live in different files from Plan 01's
new module, so this plan runs in parallel (Wave 1).
Output: parse_severity_from_body + BADGE_TO_SEVERITY in comments.py; regions_changed
+ finding_region_changed in positions.py.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md
@src/prevue/github/comments.py
@src/prevue/github/positions.py
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols:
- `BADGE_TO_SEVERITY` dict + `parse_severity_from_body(body: str) -> str | None` in `comments.py` (D-12).
- `regions_changed(path: str, incremental_patch: str | None) -> list[tuple[int, int]]` and
  `finding_region_changed(finding, regions, context: int = 3) -> bool` in `positions.py` (D-09).
- New tests in `tests/test_comments.py` (severity parse-back round-trip) and `tests/test_positions.py` (region overlap).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Severity parse-back (D-12) — RED then GREEN</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py (line 20 SEVERITY_BADGES; lines 73-89 render_inline_comment — first rendered line is `{badge} **{title}**`)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"comments.py — severity parse-back (D-12)" — anchor to leading badge emoji only)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§"Pattern 4" + Pitfall 4 — fail-safe on no-match, anchor to leading badge)
    - tests/test_comments.py (existing _finding/_gate helpers and class layout)
  </read_first>
  <behavior>
    - Round-trip: parse_severity_from_body(render_inline_comment(f)) == f.severity for f.severity in {error, warning, info}
    - Leading-badge anchoring: a body whose first line begins with 🔴 → "error", 🟡 → "warning", 🔵 → "info"
    - A human comment with no badge → None
    - A legacy/alternate-template body that still leads with a known badge emoji → correct severity (anchor on emoji, not full line structure)
    - An empty string / None-ish body → None (no exception)
  </behavior>
  <action>Add to tests/test_comments.py a class with the round-trip test (iterate severities, build a Finding via the existing _finding helper, render via render_inline_comment, assert parse_severity_from_body returns the original severity) plus explicit no-badge and empty-body → None cases — RED first (function absent). Then in comments.py add `BADGE_TO_SEVERITY = {badge: sev for sev, badge in SEVERITY_BADGES.items()}` and `parse_severity_from_body(body: str) -> str | None` that scans the first non-empty line's leading character(s) against BADGE_TO_SEVERITY and returns the match or None. Pattern-match only — never eval/exec the body (ASVS V5). Place near SEVERITY_BADGES (line 20) so the inverse sits beside the forward map.</action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "severity_parse or severity_round" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Round-trip test `parse_severity_from_body(render_inline_comment(f)) == f.severity` passes for all three severities
    - No-badge and empty-body cases return None (no exception raised)
    - `uv run pytest -k severity_parse` exits 0
    - parse_severity_from_body contains no eval/exec/compile (grep confirms pattern-match only)
  </acceptance_criteria>
  <done>Severity parse-back is a tested inverse contract anchored to the leading badge; commit as feat(08-02).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Region-changed hunk-overlap helper (D-09) — RED then GREEN</name>
  <files>src/prevue/github/positions.py, tests/test_positions.py</files>
  <read_first>
    - src/prevue/github/positions.py (lines 11-31 commentable_lines — the exact unidiff PatchSet parse + synthesized `--- a/ +++ b/` header trick + is_added/is_context/is_removed extraction to mirror)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-PATTERNS.md (§"NEW: region-changed helper (D-09)" — emit (start,end) ranges, overlap-test with C=3)
    - .planning/phases/08-incremental-stateful-review-lifecycle/08-RESEARCH.md (§"Pattern 5" — conservative C=3, biases toward NOT resolving)
    - tests/test_positions.py (existing patch-fixture test style)
  </read_first>
  <behavior>
    - regions_changed returns RIGHT-side (start,end) line ranges touched in an incremental patch; empty list when patch is None
    - finding_region_changed → True when a finding's line is inside a touched hunk's range
    - finding_region_changed → True when the finding's line is within C=3 of a touched range edge (context window)
    - finding_region_changed → False when the nearest touched range is more than C lines away (distant change on the same file)
    - A malformed/unparseable patch yields no regions → finding_region_changed False (fail-safe: leave the comment, don't resolve)
  </behavior>
  <action>Add tests to tests/test_positions.py covering overlap True, near-edge-within-C True, distant-change False, and unparseable-patch False — RED first. Then add to positions.py `regions_changed(path: str, incremental_patch: str | None) -> list[tuple[int, int]]` reusing the exact PatchSet parse from commentable_lines (synthesize `--- a/{path}\n+++ b/{path}\n` header; catch UnidiffParseError → return []), collecting each hunk's target-line min/max for added+context lines as (start, end). Add `finding_region_changed(finding, regions, context: int = 3) -> bool` returning `any(start <= finding.line + context and finding.line - context <= end for (start, end) in regions)`. Default context=3 (conservative — biases toward NOT resolving, the safe direction per D-09).</action>
  <verify>
    <automated>uv run pytest tests/test_positions.py -k region -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Overlap and within-context-window cases return True; distant-change returns False
    - Unparseable patch and None patch → regions empty and finding_region_changed False (no exception)
    - `uv run pytest -k region_changed` exits 0
    - regions_changed reuses unidiff.PatchSet (grep shows PatchSet import/use, not a regex over `@@`)
  </acceptance_criteria>
  <done>Conservative line-region-changed heuristic is tested and reuses the validated unidiff parser; commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PR comment body → severity | Comment bodies (incl. prior-version / human) are untrusted text read back for the open-set verdict |
| incremental patch text → region ranges | Engine/GitHub-derived diff text parsed by unidiff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | maliciously-crafted comment body forcing false-green via severity mislabel | mitigate | Anchor parse_severity_from_body to the leading badge emoji ONLY; on no-match return None and the gate (D-11) must treat unknown conservatively — never silently drop from the open set (RESEARCH Pitfall 4). Pattern-match only, no eval (ASVS V5). |
| T-08-04 | Tampering | unparseable/oversized incremental patch | mitigate | regions_changed catches UnidiffParseError → [] (fail-safe: do not resolve). Reuses the already-hardened commentable_lines parse. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `uv run pytest tests/test_comments.py tests/test_positions.py -q` green
- Round-trip severity contract holds for all three severities
- `uv run pytest -q` full suite green (no regression in existing comments/positions tests)
</verification>

<success_criteria>
- parse_severity_from_body is a tested inverse of SEVERITY_BADGES, fail-safe to None (LIFE-04 / D-12)
- finding_region_changed is conservative (C=3) and reuses the validated unidiff parser (LIFE-04 / D-09)
- No new third-party dependency
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-02-SUMMARY.md` when done
</output>
