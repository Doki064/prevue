---
phase: 08-incremental-stateful-review-lifecycle
plan: 09
type: execute
wave: 2
depends_on: ["08-08"]
files_modified:
  - src/prevue/github/comments.py
  - tests/test_comments.py
autonomous: true
gap_closure: true
requirements: [LIFE-02]
must_haves:
  truths:
    - "The inline comment footer renders as styled GFM attribution, not a raw <sub> HTML tag"
    - "Inline comments posted with the OLD <sub> marker are still detected as Prevue comments (backward-compatible carry-forward/dedupe/resolve)"
    - "On an incremental run the sticky Review section carries a deterministic disclaimer naming incremental scope and that prior open findings are carried forward"
    - "On a full/first run the sticky Review section has NO incremental disclaimer prefix"
  artifacts:
    - path: "src/prevue/github/comments.py"
      provides: "GFM-safe INLINE_MARKER + backward-compatible marker detection + incremental scope/carried-finding disclaimer in render_body"
      contains: "INLINE_MARKER"
    - path: "tests/test_comments.py"
      provides: "Footer-render, legacy-marker-detection, and disclaimer-prefix tests"
      contains: "posted by Prevue"
  key_links:
    - from: "src/prevue/github/comments.py render_inline_comment"
      to: "INLINE_MARKER footer"
      via: "GFM emphasis _posted by Prevue_"
      pattern: "INLINE_MARKER"
    - from: "src/prevue/github/comments.py _is_prevue_inline_comment"
      to: "old + new marker detection"
      via: "membership check covering legacy <sub> and new GFM marker"
      pattern: "_is_prevue_inline_comment"
    - from: "src/prevue/github/comments.py render_body"
      to: "Review section"
      via: "incremental disclaimer prepended on incremental runs"
      pattern: "### Review"
---

<objective>
Close gap #2 (cosmetic) and gap #5 (minor), both confined to `comments.py`.

Gap #2: `INLINE_MARKER = "<sub>posted by Prevue</sub>"` (comments.py:23) renders as
raw HTML in GitHub inline review comments (those support GFM only), so PR #23 inline
footers showed the literal tag. Replace with a GFM-safe footer (`_posted by Prevue_`)
WITHOUT breaking marker detection — `_is_prevue_inline_comment` (comments.py:585) keys
carry-forward / dedupe / resolve off `INLINE_MARKER`, and live PRs (e.g. #23) already
carry comments stamped with the OLD `<sub>` marker that must remain detectable.

Gap #5: the sticky Review section (`render_body`, comments.py:484) prints
`result.summary_markdown` verbatim — on PR #23 the engine summary described a single
new file while the PR had 2 files with carried errors. Prepend a deterministic
incremental-scope + carried-finding disclaimer on incremental runs so the human-facing
Review narrative reflects the full open set, not just the incremental diff.

Purpose: make inline attribution render correctly and keep the sticky Review honest
about incremental scope and carried findings — both human-facing correctness fixes
under the LIFE-02 "all open findings" narrative.

Output: GFM-safe + backward-compatible inline marker, and a deterministic incremental
disclaimer in the sticky Review section.

Depends on 08-08 because that plan also edits `comments.py`; this plan runs in a later
wave to avoid a write conflict on the same file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-VERIFICATION.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-UAT.md
@.planning/phases/08-incremental-stateful-review-lifecycle/08-CONTEXT.md
@src/prevue/github/comments.py
@tests/test_comments.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: GFM-safe inline marker with backward-compatible detection (gap #2)</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py lines 20-27 (MARKER/INLINE_MARKER/SEVERITY_BADGES),
      303-319 (render_inline_comment — appends INLINE_MARKER at 318),
      583-587 (_is_prevue_inline_comment — detection keyed on INLINE_MARKER)
    - tests/test_comments.py lines 390-471, 537-690, 940 (fixtures + render assertion
      hardcoding `<sub>posted by Prevue</sub>`)
    - 08-VERIFICATION.md gaps[1] (truth + missing) and Anti-Patterns row (comments.py:23)
    - 08-UAT.md §Gaps first entry (root_cause: GitHub inline comments only support GFM)
  </read_first>
  <action>
    Change `INLINE_MARKER` (comments.py:23) to the GFM-safe footer `_posted by Prevue_`
    (markdown emphasis, no HTML). Introduce a backward-compatible detection constant
    (e.g. `LEGACY_INLINE_MARKER = "<sub>posted by Prevue</sub>"`) and update
    `_is_prevue_inline_comment` (comments.py:585) to detect a comment whose body
    contains EITHER the new `INLINE_MARKER` OR `LEGACY_INLINE_MARKER` — so inline
    comments posted by prior runs (PR #23) remain detected for carry-forward, dedupe,
    and resolve. `render_inline_comment` keeps appending the new `INLINE_MARKER` (line
    318) so newly posted comments use the GFM form. Update the affected fixtures and the
    render assertion in tests/test_comments.py: the render test at line 940 must assert
    the body ends with the NEW marker; add a regression test
    `test_legacy_sub_marker_still_detected` asserting `_is_prevue_inline_comment` returns
    True for a body ending in the legacy `<sub>` marker (trusted actor), and a test that
    a body with the new `_posted by Prevue_` marker is detected. Leave the unrelated
    fixtures' marker strings working under the dual detection (they exercise detection,
    not rendering — keep them valid by ensuring legacy detection still passes).
  </action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "marker or inline or render or detect or escalation or scoped or outdated" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'INLINE_MARKER = "_posted by Prevue_"' src/prevue/github/comments.py` matches.
    - `grep -n 'LEGACY_INLINE_MARKER' src/prevue/github/comments.py` matches (legacy detection constant present).
    - `grep -vn '^#' src/prevue/github/comments.py | grep -c '<sub>posted by Prevue</sub>'` is exactly 1 (only the legacy-detection constant retains the HTML form).
    - `_is_prevue_inline_comment` returns True for both a legacy `<sub>...</sub>` body and a new `_posted by Prevue_` body (trusted actor) — asserted by tests.
    - `uv run pytest tests/test_comments.py -q` exits 0.
  </acceptance_criteria>
  <done>Inline footer renders as GFM emphasis; legacy and new markers both detected; carry-forward unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic incremental scope + carried-finding disclaimer (gap #5)</name>
  <files>src/prevue/github/comments.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py lines 357-489 (render_body signature + Review section
      assembly; verbatim `result.summary_markdown` at 484)
    - src/prevue/review.py lines 556-590 (sticky_base_kwargs — where reviewed/not-reviewed
      counts and scope are available to pass into render_body/upsert_sticky)
    - 08-VERIFICATION.md gaps[4] (truth + missing) and Data-Flow row "Sticky Review prose ... STATIC"
    - 08-CONTEXT.md D-11 (verdict reflects all currently-open findings) and D-02 (file-set incremental scope)
  </read_first>
  <behavior>
    - render_body(..., scope="incremental", reviewed_file_count=N, carried_open_count=M)
      → Review section is prefixed with a deterministic disclaimer line/blockquote naming
      incremental scope and, when M>0, that M prior open finding(s) are carried forward
      and may live on files outside this incremental diff; the engine summary follows.
    - render_body(..., scope="full") OR scope omitted → NO disclaimer prefix; Review
      section is the engine summary verbatim (existing behavior unchanged).
    - render_body incremental with carried_open_count == 0 → scope disclaimer present,
      carried-finding clause absent.
    - The disclaimer text is deterministic (not engine-derived) and HTML-neutralization
      of the engine summary is unchanged.
  </behavior>
  <action>
    Add an optional scope signal to `render_body` (e.g. a `scope: str | None = None`
    param plus a `carried_open_count: int = 0` param; do not break existing callers — both
    default to the full/no-disclaimer behavior). When `scope == "incremental"`, prepend a
    deterministic disclaimer to the Review section content before
    `_neutralize_html(result.summary_markdown)` — a short blockquote/line stating the
    review was scoped to files changed since the last reviewed commit and, when
    `carried_open_count > 0`, that N prior open finding(s) are carried forward (some may
    be on files outside this incremental diff). The engine summary remains
    HTML-neutralized and appended after the disclaimer. Thread the scope + carried count
    from `run_review` through `upsert_sticky`/`sticky_base_kwargs` (add to
    sticky_base_kwargs in review.py only if needed to pass them; if review.py is touched
    keep it minimal and additive). Write tests in tests/test_comments.py:
    `test_incremental_review_section_has_scope_disclaimer`,
    `test_incremental_disclaimer_mentions_carried_findings_when_present`,
    `test_full_review_section_has_no_disclaimer`. Follow the pure-render test style
    (build a ReviewResult + GateResult via existing helpers, call render_body, assert on
    substrings). Write the tests FIRST (they must fail), then implement.
  </action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "disclaimer or review_section or render_body" -q</automated>
  </verify>
  <acceptance_criteria>
    - render_body with `scope="incremental"` produces a Review section containing a
      deterministic disclaimer substring (e.g. "scoped to files changed since") — asserted by test.
    - render_body with `scope="incremental"` and carried_open_count>0 includes a
      carried-forward clause; with carried_open_count==0 it does not.
    - render_body with `scope="full"`/default contains NO disclaimer substring (engine summary verbatim).
    - `uv run pytest tests/test_comments.py -q` exits 0.
    - The disclaimer string is a module-level/deterministic literal, not derived from `result.summary_markdown`.
  </acceptance_criteria>
  <done>Sticky Review section honestly reflects incremental scope + carried open findings on incremental runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine summary_markdown → sticky Review | Untrusted LLM prose rendered in the sticky (already `_neutralize_html`-wrapped) |
| live inline comment body → marker detection | Untrusted body checked for the Prevue marker before being trusted as a prior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09-01 | Spoofing | `_is_prevue_inline_comment` marker check | mitigate | Detection still requires BOTH a Prevue marker (legacy or new) AND `_is_trusted_sticky_actor`; broadening to two marker strings does not relax the trusted-actor gate |
| T-08-09-02 | Tampering | render_body disclaimer prepend | mitigate | Disclaimer is a deterministic constant; the engine summary remains `_neutralize_html`-escaped — no new injection surface |
| T-08-09-03 | Tampering | GFM marker breakout | accept | `_posted by Prevue_` is plain GFM emphasis with no executable surface; lower risk than the prior `<sub>` HTML it replaces |
| T-08-09-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in this plan |
</threat_model>

<verification>
- Inline footer renders as `_posted by Prevue_` (GFM), confirmed by render test.
- Legacy `<sub>` marker still detected (PR #23 carry-forward unbroken), confirmed by test.
- Incremental run sticky Review carries deterministic scope + carried-finding disclaimer.
- Full run sticky Review unchanged (no disclaimer).
- `uv run pytest tests/test_comments.py -q` exits 0; ruff clean on comments.py.
</verification>

<success_criteria>
- `uv run pytest tests/test_comments.py -q` exits 0.
- INLINE_MARKER is GFM-safe; both legacy and new markers detected.
- Incremental disclaimer present on incremental runs, absent on full runs.
- Gaps #2 and #5 truths from 08-VERIFICATION.md achievable.
</success_criteria>

<output>
Create `.planning/phases/08-incremental-stateful-review-lifecycle/08-09-SUMMARY.md` when done.
</output>
