---
phase: 04-structured-findings-merge-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_findings_parsing.py
  - tests/test_positions.py
  - tests/test_gate.py
  - tests/test_checks.py
  - .planning/phases/04-structured-findings-merge-gate/04-VALIDATION.md
autonomous: true
requirements: [ENGN-03, OUTP-02, OUTP-03, NOIS-02, NOIS-03]

must_haves:
  truths:
    - "unidiff 0.7.x is installed, locked in uv.lock, and importable from the project venv"
    - "Four RED test scaffolds exist that pin the phase's public API contracts (parsing, positions, gate, checks) and fail only because the implementation modules do not exist yet"
    - "The pre-existing test suite stays green when the four RED scaffolds are excluded"
  artifacts:
    - path: "tests/test_findings_parsing.py"
      provides: "RED contract tests for extract_json_fence/validate_findings (ENGN-03: D-01 last-fence, D-03 strict salvage)"
      contains: "from prevue.engines.parsing import"
      min_lines: 60
    - path: "tests/test_positions.py"
      provides: "RED contract tests for commentable_lines/build_valid_lines (OUTP-02: D-17 hunk validity)"
      contains: "from prevue.github.positions import"
      min_lines: 50
    - path: "tests/test_gate.py"
      provides: "RED contract tests for ReviewConfig/conclude/apply_gate (NOIS-02 D-12/D-16, NOIS-03 D-18/D-19, OUTP-03 D-05/D-06)"
      contains: "from prevue.gate import"
      min_lines: 90
    - path: "tests/test_checks.py"
      provides: "RED contract tests for conclude_review_check/conclude_skip_check (OUTP-03 D-08/D-09/D-10)"
      contains: "from prevue.github.checks import"
      min_lines: 40
    - path: "pyproject.toml"
      provides: "unidiff dependency pinned"
      contains: "unidiff"
  key_links:
    - from: "tests/test_findings_parsing.py"
      to: "src/prevue/engines/parsing.py"
      via: "import of extract_json_fence/validate_findings (created GREEN in 04-02)"
      pattern: "prevue\\.engines\\.parsing"
    - from: "tests/test_gate.py"
      to: "src/prevue/gate.py"
      via: "import of ReviewConfig/conclude/apply_gate (created GREEN in 04-03)"
      pattern: "prevue\\.gate"
---

<objective>
Wave 0 for Phase 4: install the one new dependency (unidiff 0.7.x, registry-audited
and approved in 04-RESEARCH.md Package Legitimacy Audit) and write the four RED test
scaffolds that pin the phase's public API contracts before any implementation exists.
This is the MVP-mode "failing end-to-end test first" step: Plans 02/03/05 turn these
scaffolds GREEN. The scaffolds encode the locked decisions as executable assertions —
D-01 last-fence extraction, D-03 strict salvage, D-05/D-06 conclusion ladder, D-12
defaults, D-16 fail-closed config, D-17 position fallback, D-18 budget cap, D-19
deterministic slot allocation, D-08/D-09/D-10 check-run semantics.

Purpose: interface-first planning — executors of Plans 02-05 receive the contracts
as failing tests, not as prose to interpret. Nyquist rule satisfied for the phase.
Output: unidiff in pyproject.toml/uv.lock; tests/test_findings_parsing.py,
tests/test_positions.py, tests/test_gate.py, tests/test_checks.py (all RED);
04-VALIDATION.md flipped to wave_0_complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-structured-findings-merge-gate/04-CONTEXT.md
@.planning/phases/04-structured-findings-merge-gate/04-RESEARCH.md
@.planning/phases/04-structured-findings-merge-gate/04-PATTERNS.md
@.planning/phases/04-structured-findings-merge-gate/04-VALIDATION.md
@src/prevue/models.py
@tests/conftest.py
@tests/test_copilot_adapter.py
</context>

<phase_goal>
**As a** maintainer gating merges with Prevue, **I want to** receive schema-validated
findings as correctly-placed inline comments plus a pass/fail/neutral check —
severity-filtered and capped by a hard budget — **so that** I can trust the review
gate to never falsely block, flood, or mislead my PRs.
</phase_goal>

<artifacts_this_phase_produces>
Full Phase 4 artifact list (later plans reference this section):

**New modules / symbols (created across Plans 02-05):**
- `src/prevue/engines/parsing.py` — `FENCE_RE`, `extract_json_fence(stdout) -> tuple[str, list | None, str | None]`, `validate_findings(items) -> tuple[list[Finding], int]`
- `src/prevue/gate.py` — `Severity` (Literal alias), `SEVERITY_RANK`, `class ReviewConfig` (fields `min_severity_to_comment`, `min_severity_to_fail`, `max_inline_comments`), `load_review_config(consumer_path=None) -> ReviewConfig`, `conclude(findings, cfg, *, degraded) -> str`, `Placement` (Literal alias), `class PlacedFinding` (fields `finding`, `placement`), `class GateResult` (fields `conclusion`, `severity_counts`, `placed`, `inline`, `config`, `degraded`, `dropped_findings`), `apply_gate(findings, cfg, valid_lines, *, degraded=False, dropped_findings=0) -> GateResult`, `verdict_title(gate) -> str`, `severity_counts_line(gate) -> str`, `thresholds_line(gate) -> str`
- `src/prevue/github/positions.py` — `commentable_lines(path, patch) -> dict[str, set[int]]`, `build_valid_lines(files) -> dict[str, dict[str, set[int]]]`
- `src/prevue/github/checks.py` — `CHECK_NAME = "prevue/review"`, `conclude_review_check(repo, head_sha, gate, *, sticky_url=None)`, `conclude_skip_check(repo, head_sha, dropped_count)`

**Modified symbols:**
- `src/prevue/models.py` — `Finding.severity` tightened to `Literal["error", "warning", "info"]`; `ReviewResult` gains `degraded: bool = False` and `dropped_findings: int = 0`
- `src/prevue/engines/copilot_cli.py` — `_build_prompt()` gains JSON-fence output contract + severity rubric + 4C bar; `review()` gains parse + one-retry-then-degrade loop; new `_invoke()` subprocess helper
- `src/prevue/github/comments.py` — `SEVERITY_BADGES`, `PLACEMENT_BADGES`, `render_inline_comment(finding) -> str`, `render_findings_table(gate) -> str`, `render_finding_details(gate) -> str`, `post_inline_review(pr, gate) -> bool`, `render_body(..., gate=None)` restructured per D-26; `upsert_sticky`/`upsert_skip_note`/`_upsert_marker_comment` return the comment object
- `src/prevue/github/client.py` — `get_repo(ctx) -> Repository`
- `src/prevue/review.py` — post-engine gate stage wired into `run_review()`; `load_review_config()` called before engine spend; skip-path success check; check run after sticky
- `.github/workflows/review.yml` — `checks: write` permission

**New test files (this plan, RED):**
- `tests/test_findings_parsing.py`, `tests/test_positions.py`, `tests/test_gate.py`, `tests/test_checks.py`

**New dependency:** `unidiff==0.7.*` in pyproject.toml + uv.lock
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add unidiff 0.7.x dependency (audited, approved)</name>
  <files>pyproject.toml, uv.lock</files>
  <action>
    Run `uv add "unidiff==0.7.*"` from the repo root. unidiff was registry-audited in
    04-RESEARCH.md § Package Legitimacy Audit: PyPI package, 12-year history, 13.9M
    weekly downloads (pypistats.org), source repo github.com/matiasb/python-unidiff,
    final disposition Approved (the initial SUS flag was solely PyPI's JSON API not
    exposing download counts and was cleared by cross-verification); it is also the
    locked D-17 dependency in STACK.md. No additional human gate is required.
    Verify importability in the project venv: `uv run python -c "from unidiff import
    PatchSet; from unidiff.errors import UnidiffParseError; print('ok')"`. Do NOT
    install anything else; do NOT bump other pins.
  </action>
  <read_first>
    - pyproject.toml (current dependency list and pin style — match `==X.Y.*` pin convention)
    - .planning/phases/04-structured-findings-merge-gate/04-RESEARCH.md (§ Package Legitimacy Audit, § Standard Stack — unidiff row)
  </read_first>
  <acceptance_criteria>
    - `grep -c "unidiff" pyproject.toml` ≥ 1 and the pin is `unidiff==0.7.*`.
    - `uv run python -c "from unidiff import PatchSet; print(PatchSet)"` exits 0.
    - `uv.lock` is updated and committed alongside pyproject.toml.
    - No other dependency lines changed (git diff touches only the unidiff entries + lock).
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from unidiff import PatchSet, errors; print('unidiff ok')" && grep -n "unidiff" pyproject.toml</automated>
  </verify>
  <done>unidiff 0.7.x installed, pinned, locked, importable.</done>
</task>

<task type="auto">
  <name>Task 2: RED test scaffolds pinning the phase API contracts</name>
  <files>tests/test_findings_parsing.py, tests/test_positions.py, tests/test_gate.py, tests/test_checks.py, .planning/phases/04-structured-findings-merge-gate/04-VALIDATION.md</files>
  <action>
    Write four complete test files with real assertions (not stubs) against the exact
    public API pinned in this plan's artifacts section. They MUST fail RED right now
    (collection error: the imported modules `prevue.engines.parsing`, `prevue.gate`,
    `prevue.github.positions`, `prevue.github.checks` do not exist yet). Follow
    tests/test_copilot_adapter.py conventions: module docstring citing requirement IDs,
    `from __future__ import annotations`, module-level fixture-builder helpers,
    class-grouped tests. Construct `Finding` instances only with valid severities
    (`error`/`warning`/`info`) so these tests stay valid after 04-02 tightens the model.

    1. `tests/test_findings_parsing.py` (ENGN-03; targets `extract_json_fence(stdout)
       -> tuple[prose, list | None, error | None]` and `validate_findings(items) ->
       tuple[list[Finding], dropped_count]` from `prevue.engines.parsing`):
       - extracts the LAST json fence when a decoy fence appears earlier in stdout
         (D-01 + RESEARCH Pitfall 5) and returns parsed list with error None
       - returned prose has the extracted fence removed (summary never shows raw JSON)
       - no fence in stdout → (full stdout, None, error mentioning missing fence)
       - malformed JSON inside fence → (prose, None, error) — prose still usable (D-01)
       - top-level JSON object (not array) → (prose, None, error)
       - validate_findings: mixed list keeps valid Finding dicts, drops invalid,
         returns correct dropped count (D-03 salvage)
       - severity "ERROR" (wrong case) and severity "critical" both drop (D-11 Literal)
       - line given as string "12" drops under strict validation (D-03 no coercion,
         RESEARCH Pitfall 6 — strict=True decision)
    2. `tests/test_positions.py` (OUTP-02; targets `commentable_lines(path, patch) ->
       {"RIGHT": set[int], "LEFT": set[int]}` and `build_valid_lines(files) ->
       dict[path, dict[side, set[int]]]` from `prevue.github.positions`):
       - added lines appear in RIGHT set with correct target line numbers
       - context lines appear in RIGHT set (docs: unchanged lines comment on RIGHT)
       - removed lines appear in LEFT set with source line numbers, NOT in RIGHT
       - patch=None → both sets empty (large/binary file → all findings fall back, D-17)
       - malformed patch text → both sets empty, no exception (UnidiffParseError caught)
       - new-file patch with `@@ -0,0 +1,3 @@` header parses; RIGHT contains 1..3
       - a `\ No newline at end of file` marker line is in neither set
       - build_valid_lines maps each ChangedFile.path to its commentable_lines result
       Use realistic GitHub `files[].patch` fragments (hunks only, NO ---/+++ headers)
       as fixtures — the implementation must synthesize headers (RESEARCH Pitfall 1).
    3. `tests/test_gate.py` (NOIS-02, NOIS-03, OUTP-03; targets `prevue.gate`):
       - TestReviewConfig: defaults are min_severity_to_comment="warning",
         min_severity_to_fail=None, max_inline_comments=10 (D-12/D-18); unknown
         severity value raises ValidationError; unknown extra key raises (extra=forbid,
         D-16); negative max_inline_comments raises (ge=0); `load_review_config(None)`
         and a path to a file with no `review:` key both return pure defaults; a
         consumer YAML with `review: {min_severity_to_fail: error,
         max_inline_comments: 3}` overrides those two fields and keeps the comment
         default; a consumer YAML with `review: {min_severity_to_fail: blocker}`
         raises ValidationError (fail-closed before engine spend, D-16)
       - TestConclude (`conclude(findings, cfg, *, degraded)`): no findings + not
         degraded → "success" (D-05); any findings, min_severity_to_fail unset →
         "neutral" (D-05 default nudge); warning finding with min_severity_to_fail=
         "warning" → "failure"; info finding with min_severity_to_fail="warning" →
         "neutral" (below threshold, D-06); degraded=True → "neutral" regardless (D-04)
       - TestApplyGate (`apply_gate(findings, cfg, valid_lines, *, degraded=False,
         dropped_findings=0) -> GateResult`): severity_counts always has all three
         keys and counts ALL findings including sub-threshold and unplaceable ones
         (D-14); info findings get placement "summary-only" under default config
         (D-12) yet still count toward the verdict; a finding whose (path, line, side)
         is not in valid_lines gets placement "position-fallback" (D-17); a finding
         whose path is absent from valid_lines entirely (hallucinated path) gets
         "position-fallback"; with max_inline_comments=2 and 3 placeable errors, only
         the first 2 (engine emission order) are in gate.inline and the third is
         "summary-only" (D-18/D-19); an unplaceable error does NOT consume an inline
         slot — the next placeable warning is promoted (RESEARCH Pattern 2 ordering);
         inline ordering is all errors before warnings, emission order within tier
         (D-19); gate.placed is ordered error→warning→info, emission order within
         tier (D-24 mirror); max_inline_comments=0 → gate.inline empty, everything
         summary-only; degraded=True → conclusion "neutral", inline empty
       - TestVerdictStrings: `verdict_title(gate)` returns "✅ Pass — no findings" for
         success, "⚠️ Findings — not blocking" for neutral-with-findings,
         "⚠️ Structured findings unavailable — not blocking" for degraded neutral,
         "❌ Fail — findings at or above fail threshold" for failure (D-07 one source
         of truth); `severity_counts_line(gate)` formats "2 error · 3 warning · 1
         info"; `thresholds_line(gate)` formats "Thresholds: comment ≥ warning ·
         fail: off" when min_severity_to_fail is None and "Thresholds: comment ≥
         warning · fail ≥ error" when set
    4. `tests/test_checks.py` (OUTP-03; targets `prevue.github.checks`; use
       MagicMock repo objects and assert `repo.create_check_run` call kwargs, the
       test_review_flow.py MagicMock style — no live API):
       - `conclude_review_check(repo, head_sha, gate)` calls create_check_run with
         name="prevue/review" (CHECK_NAME), the given head_sha, status="completed",
         conclusion equal to gate.conclusion (D-08)
       - conclusion passthrough parametrized over success/neutral/failure
       - output dict has "title" containing the verdict title and counts, "summary"
         containing the thresholds line (D-10 compact panel)
       - sticky_url keyword, when given, appears as a markdown link in the summary
       - `conclude_skip_check(repo, head_sha, dropped_count=3)` posts
         conclusion="success" with title containing "no reviewable files" (D-09)

    Finally update `.planning/phases/04-structured-findings-merge-gate/04-VALIDATION.md`
    frontmatter: set `wave_0_complete: true` (the planner already filled the strategy
    body and set nyquist_compliant). Do not edit the body tables except to tick the
    Wave 0 checklist items now satisfied.
  </action>
  <read_first>
    - tests/test_copilot_adapter.py (fixture-builder + class-group + constants conventions to mirror)
    - tests/test_review_flow.py (MagicMock call-kwargs assertion style for test_checks.py)
    - tests/conftest.py (existing fixtures — reuse, do not duplicate)
    - src/prevue/models.py (Finding/ChangedFile/DiffBundle shapes the fixtures construct)
    - .planning/phases/04-structured-findings-merge-gate/04-RESEARCH.md (§ Code Examples 1-6 — the exact target signatures and semantics; § Common Pitfalls 1/2/5/6)
    - .planning/phases/04-structured-findings-merge-gate/04-VALIDATION.md (frontmatter to flip + Wave 0 checklist)
  </read_first>
  <acceptance_criteria>
    - All four test files exist and import ONLY the API names listed in the artifacts section (source assertion: imports match `prevue.engines.parsing`, `prevue.github.positions`, `prevue.gate`, `prevue.github.checks`).
    - `uv run pytest tests/test_findings_parsing.py tests/test_positions.py tests/test_gate.py tests/test_checks.py -q` exits non-zero (RED — collection errors from missing modules, the right reason).
    - `uv run pytest --ignore=tests/test_findings_parsing.py --ignore=tests/test_positions.py --ignore=tests/test_gate.py --ignore=tests/test_checks.py -q` exits 0 (existing suite untouched and green).
    - test_positions.py fixtures use bare hunk fragments starting with `@@` (no `---`/`+++` lines) — grep assertion: no `^---` diff headers inside the fixture strings.
    - 04-VALIDATION.md frontmatter contains `wave_0_complete: true`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_findings_parsing.py tests/test_positions.py tests/test_gate.py tests/test_checks.py -q > /dev/null 2>&1; test $? -ne 0 && uv run pytest --ignore=tests/test_findings_parsing.py --ignore=tests/test_positions.py --ignore=tests/test_gate.py --ignore=tests/test_checks.py -q</automated>
  </verify>
  <done>Four RED scaffolds pin every Phase 4 contract; existing suite green; VALIDATION wave_0_complete flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → project venv | New third-party code (unidiff) enters the build |
| test fixtures → future implementations | Scaffolds define the validation contracts that later bound untrusted engine output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | unidiff pip install (supply chain) | mitigate | Registry-audited in 04-RESEARCH.md Package Legitimacy Audit (12 yr history, 13.9M/wk downloads, github.com/matiasb/python-unidiff, disposition Approved — SUS flag cleared via pypistats cross-check); pinned `==0.7.*`; uv.lock committed; pure-Python wheel, no postinstall scripts |
| T-04-01 | Tampering | contract scaffolds | mitigate | Scaffolds pin strict validation (strict=True, Literal severity, last-fence extraction, path-membership) so later implementations cannot silently weaken the input-validation boundary (ASVS V5) |
</threat_model>

<verification>
- `uv run python -c "from unidiff import PatchSet"` exits 0.
- The four scaffold files fail RED with ModuleNotFoundError/ImportError (not assertion or syntax errors).
- `uv run pytest --ignore=tests/test_findings_parsing.py --ignore=tests/test_positions.py --ignore=tests/test_gate.py --ignore=tests/test_checks.py -q` green.
- `uv run ruff check tests/` passes.
</verification>

<success_criteria>
- unidiff 0.7.x installed, pinned, locked (the phase's only new dependency).
- All Phase 4 public contracts exist as executable RED assertions; Plans 02/03/05 have unambiguous GREEN targets.
- Nyquist rule satisfied: every later implementation task has a pre-existing automated test target.
- Existing suite green; no behavior change to shipped code.
</success_criteria>

<output>
Create `.planning/phases/04-structured-findings-merge-gate/04-01-SUMMARY.md` when done.
</output>
