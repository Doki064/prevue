---
phase: 03-selective-skill-loading
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/prevue/skills/frontend/accessibility.md
  - src/prevue/skills/frontend/component-state.md
  - src/prevue/skills/backend/error-handling.md
  - src/prevue/skills/backend/api-contracts.md
  - src/prevue/skills/data/migrations.md
  - src/prevue/skills/data/sql-safety.md
  - src/prevue/skills/infra/iac-safety.md
  - src/prevue/skills/infra/ci-workflow-hardening.md
  - tests/test_skills_builtin.py
  - tests/test_review_flow.py
autonomous: true
requirements: [SKIL-02]

must_haves:
  truths:
    - "All five built-in bundles (security, frontend, backend, data, infra) exist as SKILL.md-style markdown with applies-to routing metadata"
    - "Every built-in skill parses and validates (no malformed frontmatter ships)"
    - "A backend-only .py PR loads backend + security skills and NOT frontend/data/infra skills, end-to-end through run_review"
    - "The packaged .md skills are readable via importlib.resources after a build (packaging guard)"
  artifacts:
    - path: "src/prevue/skills/frontend/accessibility.md"
      provides: "Frontend a11y review skill"
      contains: "applies-to"
    - path: "src/prevue/skills/backend/error-handling.md"
      provides: "Backend error-handling review skill"
      contains: "applies-to"
    - path: "src/prevue/skills/data/migrations.md"
      provides: "Data migrations review skill"
      contains: "applies-to"
    - path: "src/prevue/skills/infra/iac-safety.md"
      provides: "Infra IaC-safety review skill"
      contains: "applies-to"
  key_links:
    - from: "tests/test_review_flow.py"
      to: "src/prevue/skills/loader.py"
      via: "run_review now assembles non-baseline instructions for a backend .py PR (behavior-change assertion)"
      pattern: "## Skill:|loaded_skills"
    - from: "tests/test_skills_builtin.py"
      to: "src/prevue/skills (packaged tree)"
      via: "importlib.resources reads all 5 bundles; every skill validates"
      pattern: "prevue.skills"
---

<objective>
Broaden the loader from the single security bundle (Plan 02) to all FIVE built-in
bundles, finalize SKIL-02's validation tests (5 bundles / all-valid / packaged-
readable / secrets-present), and apply the planned behavior-change update to
`test_review_flow.py` now that a backend `.py` PR loads real skills.

Purpose: Deliver the full roadmap success criterion — "five built-in skill bundles
exist" and "a backend-only PR gets the backend bundle and not the others." Plan 02
proved the seam on one bundle; this plan completes the content surface and locks the
end-to-end selective-load contract with the all-bundles built-in test suite.

Output: frontend/backend/data/infra skill `.md` content (8 files), a GREEN
`tests/test_skills_builtin.py`, and the updated `tests/test_review_flow.py`
asserting the new (non-baseline) assembled instructions + loaded-skills audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-selective-skill-loading/03-CONTEXT.md
@.planning/phases/03-selective-skill-loading/03-RESEARCH.md
@.planning/phases/03-selective-skill-loading/03-PATTERNS.md
@.planning/phases/03-selective-skill-loading/03-VALIDATION.md
@src/prevue/skills/loader.py
@src/prevue/review.py
@tests/test_review_flow.py
</context>

<phase_goal>
**As a** team relying on Prevue, **I want** the reviewer to load only the bundles my
PR's files trigger across all five domains (security, frontend, backend, data, infra),
**so that** a backend-only PR is reviewed with backend + security depth and zero
irrelevant frontend/data/infra guidance burning context.
</phase_goal>

<artifacts_this_phase_produces>
See 03-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This
plan creates the 8 remaining built-in skill `.md` files (frontend ×2, backend ×2,
data ×2, infra ×2) and the per-bundle package dirs. It finalizes
`tests/test_skills_builtin.py` (GREEN against the now-complete tree) and modifies
`tests/test_review_flow.py` (behavior-change assertion). No NEW source symbols are
introduced — the loader, model, and wiring all exist from Plan 02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the remaining four bundles (frontend, backend, data, infra)</name>
  <files>src/prevue/skills/frontend/accessibility.md, src/prevue/skills/frontend/component-state.md, src/prevue/skills/backend/error-handling.md, src/prevue/skills/backend/api-contracts.md, src/prevue/skills/data/migrations.md, src/prevue/skills/data/sql-safety.md, src/prevue/skills/infra/iac-safety.md, src/prevue/skills/infra/ci-workflow-hardening.md</files>
  <action>
    Create the four remaining bundle directories (`frontend`, `backend`, `data`,
    `infra`) under `src/prevue/skills/`, each a directory of single-skill `.md`
    files (D-01: bundle = directory, one skill per file). Directory names MUST be
    the canonical bundle ids so `canonical_index(bundle)` resolves for D-08 ordering.
    Add a package marker (`__init__.py`) per bundle dir if Plan 02's security bundle
    did (match whatever the build backend needed there). Write the 8 skill `.md`
    files from 03-RESEARCH.md § Skill Content Drafts verbatim-in-structure;
    frontmatter is EXACTLY `name`/`description`/`applies-to` (D-02 — no routing/label
    key) + the drafted checklist body (body wording is tunable per D-11/A2, but the
    globs and the secrets/security-first intent are load-bearing):
    - `frontend/accessibility.md` (`**/*.tsx`/`**/*.jsx`/`**/*.vue`), `frontend/component-state.md` (same globs)
    - `backend/error-handling.md` (`**/*.py`/`**/*.go`/etc.), `backend/api-contracts.md` (adds `**/api/**`, `**/routes/**`)
    - `data/migrations.md` (`**/migrations/**`, `**/*.sql`, `**/schema.prisma`), `data/sql-safety.md` (`**/*.sql`, `**/*repository*`)
    - `infra/iac-safety.md` (`**/*.tf`, `terraform/**`, `**/Dockerfile`, `**/k8s/**`), `infra/ci-workflow-hardening.md` (`.github/workflows/**`, `**/*.yml`, `**/*.yaml`)
    Author every glob in `default_rules.yml` gitignore style (`**/*.py`, never bare
    `*.py`) — Pitfall 4 anchoring. Run `uv sync` after adding files so
    `importlib.resources` sees the new packaged `.md` (Runtime State Inventory note).
  </action>
  <verify>
    <automated>uv run python -c "from prevue.skills.loader import load_skills; ss=load_skills(); print(sorted({s.bundle for s in ss})); assert sorted({s.bundle for s in ss})==['backend','data','frontend','infra','security'], sorted({s.bundle for s in ss}); print(len(ss),'skills loaded')"</automated>
  </verify>
  <read_first>
    - .planning/phases/03-selective-skill-loading/03-RESEARCH.md (§ Skill Content Drafts — the 8 non-security drafts: frontmatter + body + applies-to for each)
    - src/prevue/classify/default_rules.yml (gitignore glob style the applies-to must mirror — Pitfall 4)
    - src/prevue/skills/security/committed-secrets.md (Plan 02's bundle — match the file/frontmatter shape + package-marker convention)
    - src/prevue/classify/models.py (CANONICAL_LABEL_ORDER — bundle dir names must be these ids for D-08)
  </read_first>
  <acceptance_criteria>
    - All four bundle dirs exist with the 8 `.md` files (source assertion: files present).
    - Each new skill's frontmatter is only `name`/`description`/`applies-to` — no routing/label key (D-02).
    - `load_skills()` loads skills from all five bundles without raising (CLI verify prints `['backend','data','frontend','infra','security']`).
    - Every new skill has `applies-to` authored in gitignore style (no bare `*.ext` patterns — grep each file's frontmatter).
    - Bundle directory names are exactly `frontend`/`backend`/`data`/`infra` (canonical ids, D-01 layout).
  </acceptance_criteria>
  <done>All five bundles populated with real-but-lean skills; load_skills() reads the full tree without raising.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize built-in validation tests (SKIL-02) GREEN</name>
  <files>tests/test_skills_builtin.py</files>
  <action>
    Turn the Wave 0 `tests/test_skills_builtin.py` scaffolds GREEN against the now-
    complete five-bundle tree (these tests run against the REAL packaged content, not
    fixtures):
    - `test_all_builtin_skills_valid` (SKIL-02): `load_skills()` returns ≥1 skill per
      bundle and never raises; assert the five canonical bundle names are all present.
    - `test_security_secrets_skill_present` (SKIL-02/D-11): a security skill exists
      whose `applies-to` includes `**/*` and whose name/body concern committed secrets.
    - `test_skills_packaged_and_readable` (Pitfall 2 / A1): assert ≥1 `.md` is readable
      via `importlib.resources.files("prevue.skills")` — proves nested `.md` ships in
      the package (the packaging guard the research flagged `[ASSUMED]`).
    Keep these decoupled from exact body wording (assert on structure/globs/bundle
    presence, not on tunable prose).
  </action>
  <verify>
    <automated>uv run pytest tests/test_skills_builtin.py -x</automated>
  </verify>
  <read_first>
    - tests/test_skills_builtin.py (the Wave 0 RED scaffold from Plan 01 — the exact test names to satisfy)
    - tests/test_comments.py:19-52 (plain-assertion-over-real-artifacts style to mirror)
    - src/prevue/skills/loader.py (load_skills signature + Skill fields to assert on)
    - .planning/phases/03-selective-skill-loading/03-VALIDATION.md (§ Per-Task Verification Map — SKIL-02 rows)
  </read_first>
  <acceptance_criteria>
    - `test_all_builtin_skills_valid`, `test_security_secrets_skill_present`, `test_skills_packaged_and_readable` all PASS.
    - The all-valid test asserts all five canonical bundle names are present in `load_skills()` output (behavior assertion).
    - `uv run pytest tests/test_skills_builtin.py -x` is GREEN.
  </acceptance_criteria>
  <done>SKIL-02 fully verified: 5 bundles exist, all skills valid, secrets skill present, packaged .md readable.</done>
</task>

<task type="auto">
  <name>Task 3: Apply behavior-change update to test_review_flow.py (D-13 end-to-end)</name>
  <files>tests/test_review_flow.py</files>
  <action>
    Update the existing `test_run_review_happy_path_calls_upsert_once` for the
    documented BEHAVIOR CHANGE: its `src/example.py` (backend) PR now matches
    backend + security skills, so `req.instructions == BASELINE_INSTRUCTIONS` is NO
    LONGER true (it was correct in Phase 2; Phase 3 changes it). This is a behavior
    change, not a regression. Replace line 87's equality assertion with:
    - `assert req.instructions.startswith(BASELINE_INSTRUCTIONS)` (baseline stays the
      preamble, D-07), AND
    - `assert "## Skill:" in req.instructions` (delimited skill sections present, D-07).
    Then add a `loaded_skills` audit assertion on the captured `upsert_sticky` call:
    `loaded = mock_upsert.call_args.kwargs.get("loaded_skills")`; assert it is a
    non-empty list and at least one entry contains `(security)` or `(backend)` (D-13).
    Optionally add a focused `test_loaded_skills_in_metadata` if cleaner than
    extending the happy-path test — either satisfies 03-VALIDATION.md's D-13 row.
    Do NOT weaken the other existing assertions (fetch/get_pr/upsert call counts,
    classification labels) — keep the test otherwise intact.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py -x</automated>
  </verify>
  <read_first>
    - tests/test_review_flow.py:53-89 (the happy-path test + capture idiom; line 87 is the assertion to change)
    - .planning/phases/03-selective-skill-loading/03-PATTERNS.md (§ tests/test_review_flow.py MODIFIED — the exact behavior-change instruction)
    - .planning/phases/03-selective-skill-loading/03-RESEARCH.md (§ Validation Architecture heads-up — why instructions no longer equals bare baseline)
    - src/prevue/review.py:50-64 (the wired seam that produces the new instructions + loaded_skills)
  </read_first>
  <acceptance_criteria>
    - `test_run_review_happy_path_calls_upsert_once` no longer asserts `req.instructions == BASELINE_INSTRUCTIONS`; instead asserts baseline is a prefix AND `## Skill:` sections are present (source assertion).
    - A `loaded_skills` kwarg assertion exists on the captured `upsert_sticky` call and asserts a non-empty list (D-13).
    - `uv run pytest tests/test_review_flow.py -x` is GREEN.
    - Full suite: `uv run pytest` is GREEN and `uv run ruff check` passes (phase gate).
  </acceptance_criteria>
  <done>Behavior-change update applied; the backend-PR happy path proves end-to-end selective loading + D-13 audit; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR head content → skill loader | Same SKIL-04 boundary as Plan 02; the 4 new bundles ship inside the framework dir, never the PR |
| skill frontmatter → frontmatter parser | 8 new built-in skills parsed on load; any malformed one is a CI-caught framework bug (D-12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | new built-in skill files (frontend/backend/data/infra) | mitigate | All ship inside the packaged framework dir (SKIL-04 by construction, inherited from Plan 02); loaded only via `importlib.resources.files("prevue.skills")`. |
| T-03-07 | DoS / Tampering | malformed frontmatter in any of the 8 new skills | mitigate | D-12 fail-closed: `test_all_builtin_skills_valid` loads every built-in and fails CI if any is malformed — broken built-in cannot ship. |
| T-03-08 | Information Disclosure | infra/ci-workflow-hardening + iac-safety guidance | accept | These are review-guidance content (instruct the engine what to flag), not mechanisms Prevue executes; no new data path. |
</threat_model>

<verification>
- `uv run pytest` (full suite) is GREEN — all loader, builtin, comments, and review-flow tests pass.
- `uv run python -c "from prevue.skills.loader import load_skills; print(sorted({s.bundle for s in load_skills()}))"` lists all five canonical bundles.
- `uv run ruff check` passes (phase gate).
- A backend `.py` PR's `req.instructions` contains `## Skill:` sections and `upsert_sticky` receives a non-empty `loaded_skills` (D-13 end-to-end).
</verification>

<success_criteria>
- All five built-in bundles exist as SKILL.md-style markdown with applies-to metadata (SKIL-02).
- Every built-in skill parses + validates; no malformed frontmatter ships (D-12 enforced by test_all_builtin_skills_valid).
- Packaged `.md` skills are readable via importlib.resources after a build (A1 packaging guard resolved).
- The backend-only-PR success criterion holds end-to-end: backend + security skills load, frontend/data/infra do not.
- `test_review_flow.py` reflects the documented behavior change and the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/03-selective-skill-loading/03-03-SUMMARY.md` when done.
</output>
