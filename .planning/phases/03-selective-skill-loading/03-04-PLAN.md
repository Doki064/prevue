---
phase: 03-selective-skill-loading
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/prevue/skills/frontend/component-state.md
  - src/prevue/skills/infra/ci-workflow-hardening.md
  - tests/test_skills_builtin.py
  - .planning/phases/03-selective-skill-loading/03-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [SKIL-02]

must_haves:
  truths:
    - "UAT test 7 passes — built-in skills contain genuine lean review guidance with concrete examples, not generic stubs"
    - "The two thinnest skills (component-state, ci-workflow-hardening) match committed-secrets depth pattern: opener + anti-patterns + severity framing"
    - "CI guards against future skill-content regression via automated lean-floor test"
    - "Lean-floor test passes all 11 skills — baseline floor (≥4 bullets) for all; strict committed-secrets bar only for THIN_SKILL_KEYS"
  artifacts:
    - path: "src/prevue/skills/frontend/component-state.md"
      provides: "Enriched React/Vue state review guidance with concrete anti-patterns"
      contains: "**error**"
    - path: "src/prevue/skills/infra/ci-workflow-hardening.md"
      provides: "Enriched CI workflow review guidance with workflow-specific examples"
      contains: "pull_request_target"
    - path: "tests/test_skills_builtin.py"
      provides: "Automated D-11 lean content floor assertion"
      contains: "test_builtin_skill_content_lean_floor"
  key_links:
    - from: "tests/test_skills_builtin.py"
      to: "src/prevue/skills/**/*.md"
      via: "load_skills() reads packaged bodies and asserts lean-floor rubric"
      pattern: "test_builtin_skill_content_lean_floor"
---

<objective>
Close UAT test 7 (minor): built-in skill content quality per D-11.

Purpose: D-11 shipped lean 4–6 bullet checklists that met the minimum floor but user flagged two skills as too generic vs `committed-secrets.md`. Enrich only the thinnest flagged skills; add a lightweight CI floor so content cannot regress to frontmatter-only stubs. Full bundle-wide depth parity and token-budget rubric enforcement deferred to Phase 6 (OUTP-04 token transparency).

Output: enriched `component-state.md` + `ci-workflow-hardening.md`, new `test_builtin_skill_content_lean_floor`, updated D-11 rubric in `03-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-selective-skill-loading/03-UAT.md
@.planning/debug/skill-content-depth.md
@.planning/phases/03-selective-skill-loading/03-CONTEXT.md
@.planning/phases/03-selective-skill-loading/03-VALIDATION.md
@.planning/phases/03-selective-skill-loading/03-PATTERNS.md
@src/prevue/skills/security/committed-secrets.md
@src/prevue/skills/frontend/component-state.md
@src/prevue/skills/infra/ci-workflow-hardening.md
@tests/test_skills_builtin.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lean content-floor test + update validation rubric (RED)</name>
  <files>tests/test_skills_builtin.py, .planning/phases/03-selective-skill-loading/03-VALIDATION.md</files>
  <behavior>
    - Test: `test_builtin_skill_content_lean_floor` — every built-in skill body has ≥4 checklist bullets (baseline D-11 lean floor).
    - Test: `THIN_SKILL_KEYS = {"frontend/component-state.md", "infra/ci-workflow-hardening.md"}` — keyed by `f"{skill.bundle}/{skill.filename}"` — require ALL of: imperative opener (≥1 non-bullet body line before first `-` bullet), ≥2 lines containing backticks, severity footer (`**error**` or `**warning**` in body).
    - Test: non-THIN skills (api-contracts, authn-authz, migrations, sql-safety, etc.) pass on bullets ≥4 alone — no backtick/severity requirement.
    - Test MUST fail on current repo (RED): both THIN skills lack opener + severity footer before Task 2 enrichment.
  </behavior>
  <action>
    Add `test_builtin_skill_content_lean_floor` to `tests/test_skills_builtin.py` using `load_skills()` (same as existing tests). Follow analog in `.planning/phases/03-selective-skill-loading/03-PATTERNS.md` § `tests/test_skills_builtin.py` — plain assertion style over real packaged artifacts via `load_skills()`.

    Rubric logic (helpers inline in test file — no new modules):
    1. Parse body lines; count bullets starting with `-`.
    2. Count lines containing backticks.
    3. Detect severity footer: body contains `**error**` or `**warning**` (case-sensitive markdown bold).
    4. Detect opener: at least one non-empty, non-bullet line before the first `-` bullet.
    5. **All skills:** `bullets >= 4` (baseline lean floor — sufficient for api-contracts, authn-authz, migrations, sql-safety, and the other seven non-THIN skills).
    6. **THIN_SKILL_KEYS only:** require `opener` AND `backtick_lines >= 2` AND `has_severity` (committed-secrets depth pattern).

    Update `03-VALIDATION.md`:
    - Replace the Manual-Only D-11 row test instructions with the two-tier rubric (baseline ≥4 bullets for all; opener + backtick examples + severity footer for THIN_SKILL_KEYS only).
    - Add automated row: `SKIL-02/D-11 | lean content floor | unit | uv run pytest tests/test_skills_builtin.py::test_builtin_skill_content_lean_floor -x`.
    - Add note under Manual-Only: "Full depth parity across all 11 skills + token-budget content packing → Phase 6 (OUTP-04)."

    Do NOT modify skill `.md` files in this task — test-first RED only.
  </action>
  <verify>
    <automated>uv run pytest tests/test_skills_builtin.py::test_builtin_skill_content_lean_floor -x; test $? -ne 0</automated>
  </verify>
  <done>Test exists, asserts two-tier rubric (baseline for all + strict bar for THIN_SKILL_KEYS only), and fails RED on current component-state + ci-workflow-hardening before enrichment.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich thinnest flagged skills (D-11, GREEN)</name>
  <files>src/prevue/skills/frontend/component-state.md, src/prevue/skills/infra/ci-workflow-hardening.md</files>
  <action>
    Rewrite ONLY the two UAT-flagged skills to follow the `committed-secrets.md` pattern (per D-11, per `.planning/debug/skill-content-depth.md`). Do NOT touch the other nine skills — they already meet the baseline lean floor (≥4 bullets).

    **Reference pattern** (`committed-secrets.md`): imperative opener sentence (non-bullet) → checklist bullets with backtick-wrapped concrete anti-patterns → closing "For each:" action line → severity line (`**error**` / `**warning**`).

    **`frontend/component-state.md`** — expand to ~8–10 body lines:
    - Opener: review React/Vue component state + render correctness.
    - Bullets MUST include concrete anti-patterns in backticks, e.g. derived state duplicated into `useState`, inline object/array deps in `useEffect`, array-index list keys (`key={i}`), pointless `useMemo`/`useCallback`, unsanitized `dangerouslySetInnerHTML`.
    - Close with "For each finding: cite file + line." and severity split: infinite re-render / missing effect deps → **error**; memoization nits → **warning**.
    - Keep existing frontmatter unchanged (`name`, `description`, `applies-to` globs).

    **`infra/ci-workflow-hardening.md`** — expand similarly:
    - Opener: review GitHub Actions / CI workflow supply-chain + privilege risks.
    - Bullets MUST include workflow-specific examples in backticks, e.g. mutable action refs (`actions/checkout@v4`, `@main`), missing or overly broad `permissions:`, `pull_request_target` + PR-head checkout, `secrets: inherit`, untrusted `github.event.*` in `run:` scripts.
    - Close with "For each: cite workflow file + step." and severity: `pull_request_target` + PR-head checkout → **error**; unpinned actions on privileged jobs → **warning**.
    - Keep existing frontmatter unchanged.

    Token budget: stay lean (~150–250 words body each). No new dependencies. No loader changes.
  </action>
  <verify>
    <automated>python3 -c "
import re, sys
from pathlib import Path
for rel in ['frontend/component-state.md', 'infra/ci-workflow-hardening.md']:
    text = Path('src/prevue/skills') / rel
    raw = text.read_text()
    parts = raw.split('---', 2)
    body = parts[2] if len(parts) >= 3 else raw
    lines = [l for l in body.strip().splitlines() if l.strip()]
    bullets = [l for l in lines if l.lstrip().startswith('-')]
    backtick_lines = [l for l in lines if '`' in l]
    first_bullet = next((i for i,l in enumerate(lines) if l.lstrip().startswith('-')), len(lines))
    opener = any(not l.lstrip().startswith('-') for l in lines[:first_bullet])
    assert len(bullets) >= 5, f'{rel}: need >=5 bullets, got {len(bullets)}'
    assert len(backtick_lines) >= 2, f'{rel}: need >=2 backtick lines, got {len(backtick_lines)}'
    assert opener, f'{rel}: missing imperative opener'
    assert re.search(r'\*\*error\*\*', body), f'{rel}: missing **error** severity'
    assert 'For each' in body, f'{rel}: missing closing action line'
" && uv run pytest tests/test_skills_builtin.py::test_builtin_skill_content_lean_floor -x && uv run pytest -q && uv run ruff check</automated>
  </verify>
  <done>Both enriched skills have imperative opener, ≥5 bullets with backtick examples, closing "For each" action line, explicit **error**/**warning** severity framing; lean-floor test green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill content → engine prompt | Built-in markdown bodies become review instructions via `assemble_instructions()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | skill `.md` files | accept | Content is framework-maintained trusted ref (SKIL-04); not PR-modifiable |
| T-03-04-02 | Information Disclosure | enriched examples | accept | Examples are illustrative anti-patterns, not real secrets |
| T-03-04-SC | Tampering | npm/pip installs | accept | No new package installs in this plan |
</threat_model>

<verification>
- Task 1 RED: `uv run pytest tests/test_skills_builtin.py::test_builtin_skill_content_lean_floor -x` fails before enrichment
- Task 2 GREEN: same test passes after enrichment
- `uv run pytest -q` full suite green
- Manual spot-check: read enriched `component-state.md` and `ci-workflow-hardening.md` — confirm actionable vs generic
- Re-run UAT test 7 checklist from `03-UAT.md`
</verification>

<success_criteria>
- UAT gap truth satisfied: enriched skills contain genuine lean review guidance with concrete anti-patterns and severity hints
- Automated floor prevents regression to stub-only content; non-THIN skills not falsely flagged
- Phase 6 scope documented for full depth parity (not silently dropped)
</success_criteria>

<output>
Create `.planning/phases/03-selective-skill-loading/03-04-SUMMARY.md` when done
</output>

<!-- gsd-multi-source-audit
SOURCE    | ID      | Feature/Requirement                    | Plan  | Status  | Notes
--------- | ------- | -------------------------------------- | ----- | ------- | -----
GOAL      | —       | Selective skill loading w/ quality     | 04    | COVERED | UAT test 7 gap
REQ       | SKIL-02 | Built-in bundles + D-11 lean content   | 04    | COVERED | Content quality gap
REQ       | SKIL-01 | Per-skill selective load               | 01-03 | COVERED | Prior plans
REQ       | SKIL-04 | Trusted-ref-only loading               | 01-03 | COVERED | Prior plans
RESEARCH  | —       | D-11 lean checklists w/ depth          | 04    | COVERED | Enrich thinnest 2
RESEARCH  | —       | committed-secrets as depth reference   | 04    | COVERED | Pattern in Task 2
CONTEXT   | D-11    | Real but lean checklists               | 04    | COVERED | Task 2 enrichment
CONTEXT   | D-12    | Fail-closed frontmatter                | 01-03 | COVERED | Existing tests
RESEARCH  | —       | Token-budget packing (Phase 6)         | —     | DEFER   | OUTP-04, documented
-->
