---
phase: 03-selective-skill-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/fixtures/skills/security/committed-secrets.md
  - tests/fixtures/skills/security/input-validation.md
  - tests/fixtures/skills/frontend/accessibility.md
  - tests/fixtures/skills/backend/error-handling.md
  - tests/fixtures/skills/malformed/no-applies-to.md
  - tests/conftest.py
  - tests/test_skills_loader.py
  - tests/test_skills_builtin.py
autonomous: false
requirements: [SKIL-01, SKIL-02, SKIL-04]
user_setup:
  - service: python-frontmatter (PyPI package)
    why: "Parse SKILL.md YAML frontmatter + markdown body (D-12 fail-closed validation). Flagged SUS by the legitimacy seam — likely false-positive (long-established eyeseast/python-frontmatter; PyPI repo URL confirmed). Human must approve before install."
    dashboard_config:
      - task: "Confirm package legitimacy at https://pypi.org/project/python-frontmatter and https://github.com/eyeseast/python-frontmatter (version 1.3.*, no postinstall scripts)"
        location: "PyPI / GitHub (browser)"

must_haves:
  truths:
    - "python-frontmatter 1.3.* is an installed project dependency (importable in the venv)"
    - "A reusable skills-fixture tree exists under tests/fixtures/skills/ independent of real built-in content"
    - "Failing test scaffolds exist for the loader (selection/order/dedupe/fallback/assembly/fail-closed/trusted-dir) and built-ins (5 bundles / secrets-skill / packaged-readable)"
  artifacts:
    - path: "tests/fixtures/skills/security/committed-secrets.md"
      provides: "Always-on fixture skill (applies-to **/*) for loader-unit tests"
      contains: "applies-to"
    - path: "tests/test_skills_loader.py"
      provides: "RED scaffold for SKIL-01/D-06/D-08/D-09/D-12/SKIL-04 loader behaviors"
      contains: "def test_"
    - path: "tests/test_skills_builtin.py"
      provides: "RED scaffold for SKIL-02 (5 bundles, secrets skill, packaged-readable)"
      contains: "def test_"
    - path: "pyproject.toml"
      provides: "python-frontmatter dependency declaration"
      contains: "python-frontmatter"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/fixtures/skills/"
      via: "skills_fixture_root pytest fixture returning the fixture tree path"
      pattern: "fixtures/skills"
---

<objective>
Wave 0 scaffold for selective skill loading: add the one new dependency
(`python-frontmatter`) behind a legitimacy checkpoint, create a skills-fixture
tree that loader-unit tests can run against without coupling to real built-in
content, and write the FAILING test scaffolds (RED) that Plans 02–03 turn GREEN.

Purpose: Establish the Nyquist test contract before any production loader code
exists, and clear the supply-chain gate for the new parser dependency. Every
behavior in 03-VALIDATION.md gets a named test here so later plans implement
against a fixed contract, not a moving target.

Output: `python-frontmatter` in pyproject.toml + uv.lock; `tests/fixtures/skills/`
tree; `skills_fixture_root` conftest fixture; `tests/test_skills_loader.py` and
`tests/test_skills_builtin.py` with failing test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-selective-skill-loading/03-CONTEXT.md
@.planning/phases/03-selective-skill-loading/03-RESEARCH.md
@.planning/phases/03-selective-skill-loading/03-PATTERNS.md
@.planning/phases/03-selective-skill-loading/03-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This section lists every NEW symbol/file the WHOLE phase introduces, so the
plan-review source-grounding pass excludes them from drift verification.

**New package + modules (created in Plan 02):**
- `src/prevue/skills/__init__.py`, `src/prevue/skills/security/__init__.py` (and per-bundle markers)
- `src/prevue/skills/models.py` — `Skill` pydantic model (fields: `name`, `description`, `applies_to` aliased `applies-to`, `bundle`, `filename`, `body`)
- `src/prevue/skills/loader.py` — functions `load_skills()`, `select_skills(skills, paths)`, `assemble_instructions(baseline, skills)`, helper `_rank(bundle)`
- `src/prevue/classify/models.py` — NEW shared helper `canonical_index(name)` (lifted; router/comments/loader import it)

**New built-in skill content files (Plan 02 + Plan 03):**
- `src/prevue/skills/security/committed-secrets.md` (MANDATORY, D-11), `security/authn-authz.md`, `security/input-validation.md`
- `src/prevue/skills/frontend/accessibility.md`, `frontend/component-state.md`
- `src/prevue/skills/backend/error-handling.md`, `backend/api-contracts.md`
- `src/prevue/skills/data/migrations.md`, `data/sql-safety.md`
- `src/prevue/skills/infra/iac-safety.md`, `infra/ci-workflow-hardening.md`

**New test files (Plan 01):**
- `tests/test_skills_loader.py`, `tests/test_skills_builtin.py`
- `tests/fixtures/skills/**` fixture tree; `skills_fixture_root` conftest fixture

**Modified existing symbols (Plans 02–03):**
- `src/prevue/review.py` `run_review()` — inserts load/select/assemble; replaces `instructions=BASELINE_INSTRUCTIONS`; adds `loaded_skills=` to `upsert_sticky`
- `src/prevue/github/comments.py` `render_body()` / `upsert_sticky()` — add keyword-only `loaded_skills` param + `Skills:` metadata line
- `tests/test_review_flow.py` — behavior-change update to the instructions assertion

**New dependency:** `python-frontmatter==1.3.*` (this plan).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify legitimacy of python-frontmatter, then install</name>
  <what-built>
    Nothing automated yet — this is the supply-chain gate REQUIRED by
    03-RESEARCH.md § Package Legitimacy Audit. `python-frontmatter` was flagged
    SUS by the legitimacy seam (too-new / unknown-downloads / no-repository).
    The researcher assessed this as a likely false-positive: the SUS signal
    reflects the 1.3.0 release date (2026-05-20), not package age — the project
    (eyeseast/python-frontmatter) has been on PyPI for years, and PyPI metadata
    DOES expose `project_urls.repository = github.com/eyeseast/python-frontmatter`.
    It is the STACK.md-mandated parser. Per protocol the SUS verdict still
    requires a blocking human verification before install (this checkpoint is
    NOT auto-approvable — `workflow.auto_advance` is ignored).
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/python-frontmatter/ — confirm the latest is
       version `1.3.x`, the maintainer is `eyeseast`, and the project page links
       to `github.com/eyeseast/python-frontmatter`.
    2. Open https://github.com/eyeseast/python-frontmatter — confirm it is a
       long-lived, real project (not a typosquat created recently).
    3. Confirm PyPI wheels have no postinstall scripts (Python wheels cannot run
       install hooks; this is structural, just sanity-confirm it is a wheel dist).
    4. Reply "approved" to authorize the install, or describe concerns to abort.
  </how-to-verify>
  <action>
    After the human approves, run `uv add "python-frontmatter==1.3.*"` to add it
    to `pyproject.toml` `[project].dependencies` (alongside the existing
    pathspec/pydantic/pygithub/pyyaml pins) and update `uv.lock`. Then run
    `uv pip show python-frontmatter` to record the resolved version. Do NOT pin
    an exact patch — match the `==1.3.*` style of the existing dependency pins.
    If the human aborts, STOP the plan and report.
  </action>
  <read_first>
    - pyproject.toml (the `dependencies` list — match the `==X.Y.*` pin style)
    - .planning/phases/03-selective-skill-loading/03-RESEARCH.md (§ Package Legitimacy Audit — the SUS verdict context + recommended verify command)
  </read_first>
  <acceptance_criteria>
    - Human reply recorded as "approved" before any install ran.
    - `pyproject.toml` `[project].dependencies` contains a `python-frontmatter==1.3.*` entry.
    - `uv.lock` updated; `uv run python -c "import frontmatter; print(frontmatter.__name__)"` prints `frontmatter` (CLI output check).
    - `uv pip show python-frontmatter` reports a `1.3.x` version.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install, or describe concerns to abort.</resume-signal>
  <done>python-frontmatter 1.3.* installed and importable; uv.lock committed-ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create skills-fixture tree + conftest fixture</name>
  <files>tests/fixtures/skills/security/committed-secrets.md, tests/fixtures/skills/security/input-validation.md, tests/fixtures/skills/frontend/accessibility.md, tests/fixtures/skills/backend/error-handling.md, tests/fixtures/skills/malformed/no-applies-to.md, tests/conftest.py</files>
  <action>
    Build a SMALL fixture skills tree under `tests/fixtures/skills/` that loader-
    unit tests run against WITHOUT depending on the real built-in content (so
    Plan 02's loader tests stay decoupled from Plan 03's bundle authoring). Create
    these fixture skill files, each with valid `name` + `description` + `applies-to`
    frontmatter (use python-frontmatter `---`-delimited YAML, mirroring the
    Skill Content Drafts in 03-RESEARCH.md) and a one-line body:
    - `security/committed-secrets.md` → `applies-to: ["**/*"]` (always-on; the
      determinism + dedupe anchor)
    - `security/input-validation.md` → `applies-to: ["**/*.py"]`
    - `frontend/accessibility.md` → `applies-to: ["**/*.tsx"]`
    - `backend/error-handling.md` → `applies-to: ["**/*.py"]`
    - `malformed/no-applies-to.md` → frontmatter with `name` + `description` but
      NO `applies-to` key (drives the D-12 fail-closed RED test). The `malformed`
      bundle dir is deliberately NOT a canonical bundle name.
    Author globs in `default_rules.yml` gitignore style (`**/*.py`, never bare
    `*.py`) per 03-RESEARCH.md Pitfall 4.
    Add a `skills_fixture_root` pytest fixture to `tests/conftest.py` returning
    `pathlib.Path(__file__).parent / "fixtures" / "skills"`. Append it next to the
    existing `fake_engine` fixture; do not remove or alter existing fixtures.
  </action>
  <verify>
    <automated>uv run python -c "import frontmatter, pathlib; p = pathlib.Path('tests/fixtures/skills'); ok = [frontmatter.loads(f.read_text()).metadata for f in p.rglob('*.md')]; print(len(ok), 'fixture skills parsed')"</automated>
  </verify>
  <read_first>
    - tests/conftest.py (the existing `fake_engine` fixture style + imports — append, do not rewrite)
    - src/prevue/classify/default_rules.yml (authoritative glob-authoring style: `**/*.py`, `terraform/**`, `.github/workflows/**` — Pitfall 4 anchoring)
    - .planning/phases/03-selective-skill-loading/03-RESEARCH.md (§ Skill Content Drafts — frontmatter shape `name`/`description`/`applies-to`)
  </read_first>
  <acceptance_criteria>
    - 5 fixture `.md` files exist under `tests/fixtures/skills/` (4 valid in security/frontend/backend, 1 malformed under `malformed/`).
    - Each valid fixture parses via `frontmatter.loads(...)` with `name`, `description`, `applies-to` present in `.metadata` (CLI verify command exits 0, prints `5 fixture skills parsed` or `4 fixture skills parsed` depending on whether the malformed one parses — both load via frontmatter; malformed only lacks `applies-to`).
    - `malformed/no-applies-to.md` `.metadata` has NO `applies-to` key.
    - `tests/conftest.py` defines a `skills_fixture_root` fixture; existing `fake_engine` fixture is unchanged (`uv run pytest tests/test_smoke.py -x` still passes).
  </acceptance_criteria>
  <done>Fixture tree + conftest fixture exist; all valid fixtures parse via python-frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Write failing loader + builtin test scaffolds (RED)</name>
  <files>tests/test_skills_loader.py, tests/test_skills_builtin.py</files>
  <action>
    Create the two RED test files with the EXACT test function names from
    03-VALIDATION.md § Per-Task Verification Map (later plans turn them GREEN).
    These tests import from `prevue.skills.loader` / `prevue.skills.models` and
    `prevue.skills` — modules that do not exist yet — so they fail at collection/
    import (legitimate RED). Mirror the assertion idioms in `tests/test_review_flow.py`
    (`patch` + capture-dict) and `tests/test_comments.py` (plain assertions).

    `tests/test_skills_loader.py` — use the `skills_fixture_root` fixture (and/or
    monkeypatch the loader's `importlib.resources.files` to the fixture tree) for
    the selection/order/dedupe/fallback/assembly tests so they do NOT depend on
    real built-in content. Define:
    - `test_backend_only_pr_selects_backend_not_frontend` (SKIL-01): a `.py`-only
      path list selects backend/security skills, NOT the frontend `.tsx` skill.
    - `test_dedupe_by_path` (D-09): a skill whose globs both match a path appears once.
    - `test_canonical_then_filename_order` (D-08): matched skills ordered by
      `CANONICAL_LABEL_ORDER` bundle rank, then filename alpha.
    - `test_no_match_falls_back_to_baseline` (D-06): paths matching nothing →
      `assemble_instructions` returns the bare baseline.
    - `test_assemble_sections` (D-07): output is `baseline` + `## Skill: <name>`
      delimited sections.
    - `test_missing_applies_to_raises` (D-12): loading the `malformed/` skill (or
      validating its metadata) raises `pydantic.ValidationError`.
    - `test_loads_from_packaged_framework_dir` (SKIL-04): assert the loader resolves
      its root via `importlib.resources.files("prevue.skills")` (the trusted packaged
      dir), NOT a `__file__`-relative or PR-head path.

    `tests/test_skills_builtin.py` — these run against the REAL built-in tree (Plan
    02/03 content). Define:
    - `test_all_builtin_skills_valid` (SKIL-02): every `.md` under the packaged
      `prevue.skills` tree parses + validates into a `Skill` (no raise).
    - `test_security_secrets_skill_present` (SKIL-02/D-11): a security skill named
      for committed secrets exists and its `applies-to` includes `**/*`.
    - `test_skills_packaged_and_readable` (Pitfall 2): at least one `.md` is readable
      via `importlib.resources.files("prevue.skills")` (packaging guard).
    Add a brief module docstring on each file noting it is the Wave 0 RED scaffold.
  </action>
  <verify>
    <automated>uv run pytest tests/test_skills_loader.py tests/test_skills_builtin.py --co -q 2>&1 | tail -5; echo "expect collection errors (RED) — modules not yet implemented"</automated>
  </verify>
  <read_first>
    - tests/test_review_flow.py:53-89 (patch + capture-dict idiom) and :182-194 (pytest.raises idiom for fail-closed)
    - tests/test_comments.py:19-52 (plain-assertion render style)
    - .planning/phases/03-selective-skill-loading/03-VALIDATION.md (§ Per-Task Verification Map — the EXACT test names to use)
    - .planning/phases/03-selective-skill-loading/03-RESEARCH.md (§ Validation Architecture + Code Examples — loader signatures `load_skills()`, `select_skills(skills, paths)`, `assemble_instructions(baseline, skills)`)
  </read_first>
  <acceptance_criteria>
    - `tests/test_skills_loader.py` defines all 7 named tests above; `tests/test_skills_builtin.py` defines all 3.
    - Both files FAIL at collection or assertion (RED) because `prevue.skills.*` does not exist yet — `uv run pytest tests/test_skills_loader.py tests/test_skills_builtin.py --co -q` reports collection errors / import errors, not zero tests silently.
    - Every test name matches 03-VALIDATION.md exactly (e.g. `test_backend_only_pr_selects_backend_not_frontend`, `test_missing_applies_to_raises`, `test_loads_from_packaged_framework_dir`).
    - `uv run ruff check tests/test_skills_loader.py tests/test_skills_builtin.py` passes (imports may reference not-yet-existing modules; ruff F401/F821 must still be clean — guard with the import at top so it is a real ImportError, not a lint error).
  </acceptance_criteria>
  <done>Both RED test files exist with the exact named tests; they fail because the loader package is unbuilt (intended).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer machine / CI | New dependency `python-frontmatter` crosses the supply-chain boundary at install time |
| fixture frontmatter → frontmatter parser | YAML parsed at test time (trusted fixtures, but SafeLoader still applies) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `uv add python-frontmatter` (supply chain) | mitigate | Blocking human-verify checkpoint (Task 1) before install per RESEARCH legitimacy audit; pin `==1.3.*`; commit `uv.lock`. Not auto-approvable. |
| T-03-01 | Tampering / RCE | YAML in fixture frontmatter | accept | python-frontmatter uses `yaml.SafeLoader` by default; fixtures are repo-trusted test data, no untrusted input here. |
</threat_model>

<verification>
- `uv run python -c "import frontmatter"` succeeds (dependency installed).
- `uv run pytest tests/test_smoke.py -x` passes (conftest change did not break collection).
- `uv run pytest tests/test_skills_loader.py tests/test_skills_builtin.py --co -q` shows RED (collection/import errors) — proves the contract precedes implementation.
- `uv run ruff check tests/` passes.
</verification>

<success_criteria>
- python-frontmatter 1.3.* is an installed, importable dependency, approved through the legitimacy checkpoint.
- `tests/fixtures/skills/` tree + `skills_fixture_root` conftest fixture exist and parse via python-frontmatter.
- `tests/test_skills_loader.py` (7 tests) and `tests/test_skills_builtin.py` (3 tests) exist as RED scaffolds with the exact 03-VALIDATION.md names.
- The full pre-existing suite (`uv run pytest --ignore=tests/test_skills_loader.py --ignore=tests/test_skills_builtin.py`) stays green.
</success_criteria>

<output>
Create `.planning/phases/03-selective-skill-loading/03-01-SUMMARY.md` when done.
</output>
