---
phase: 09-classification-skill-loading-multi-call-review
plan: 04
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/prevue/review.py
  - src/prevue/skills/loader.py
  - tests/test_review_flow.py
autonomous: true
requirements: [SKIL-01, ROUT-01, CLSF-03]
must_haves:
  truths:
    - "classify() runs on the full filtered changed set (reduced.files) BEFORE packing, not on packed_files"
    - "After classify+route, skills from routed bundles are unioned into the loaded set via hybrid selection and appear in assembled instructions before engine.review()"
    - "LLM fallback labels trigger the same bundle-scoped selection as deterministic labels (one code path)"
    - "Path-glob select behavior is unchanged for bundles NOT present in result_cls.bundles"
    - "gap-demo-sandbox regression: a packed path NOT matching a skill's applies-to, but whose bundle is routed, loads that skill's body into instructions (GAP-DEMO-SKILL-LOADED present)"
    - "Post-union re-trim + byte-limit guard run after selection; still-over-budget → neutral skip"
    - "noop and incremental paths still work; SKIL-04 base-ref guard and SECR-01 fork guard remain before the classify block"
  artifacts:
    - path: "src/prevue/review.py"
      provides: "run_review() reordered to classify-first → route → hybrid select → pack → assemble → re-trim → byte guard"
      contains: "select_skills_hybrid"
  key_links:
    - from: "src/prevue/review.py"
      to: "src/prevue/skills/selection.py"
      via: "select_skills_hybrid(skills, paths, diff_text, result_cls.bundles, llm_skill_names=...)"
      pattern: "select_skills_hybrid"
    - from: "src/prevue/review.py"
      to: "src/prevue/classify/router.py"
      via: "route() output feeds selection, not just sticky"
      pattern: "result_cls.bundles"
---

<objective>
Reorder `run_review()` to classify-first (D-01) and wire the hybrid skill selection from Plan 09-02 so routed bundles drive which skills load — closing the SKIL-01 gap. Add the gap-demo-sandbox regression (D-12) proving a routed bundle's skill loads even when no packed path matches its applies-to glob.

Purpose: This is the Thread 1 integration. Today `run_review()` packs first, selects skills by glob against packed paths, then classifies the packed set with the bundles used only for sticky metadata (review.py:546/562/674/713). The fix moves classify+route ahead of select/pack and feeds `result_cls.bundles` into `select_skills_hybrid`, so classification finally drives loading. Keep the single-call path byte-identical so the 98 KB `test_review_flow.py` stays green as a regression backstop.
Output: Reordered `run_review()`, hybrid selection wired with LLM double-duty, post-union budget guard, and integration tests including the gap regression.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-CONTEXT.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-RESEARCH.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-02-SUMMARY.md
@src/prevue/skills/selection.py
</context>

<artifacts_this_phase_produces>
This plan modifies (no new symbols beyond local reorder):
- `src/prevue/review.py` — `run_review()` reordered; consumes `select_skills_hybrid` and `llm_select_skills`/double-duty names from 09-02
- `src/prevue/skills/loader.py` — only if a shared selection seam needs exposing (e.g. re-export); prefer importing selection.py directly
New tests in `tests/test_review_flow.py`: `test_classify_runs_on_full_set_not_packed`, `test_routed_bundle_skill_loads_via_union`, `test_llm_fallback_label_triggers_bundle_selection`, `test_non_routed_bundle_glob_unchanged`, `test_post_union_budget_neutral_skip`, `test_gap_demo_skill_loaded`
All other Phase 9 symbols (importscan, multicall, ReviewConfig caps, comments.py audit) are produced by sibling plans — see 09-01 artifacts section.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorder run_review to classify-first + wire hybrid selection (D-01/D-02/D-03)</name>
  <files>src/prevue/review.py, src/prevue/skills/loader.py</files>
  <read_first>
    - src/prevue/review.py (FULL run_review, especially: load_skills+weight+pack at 476-545; select_skills+trim+readmit cascade at 546-611; byte-limit probe at 646-672; classify+llm_classify+route at 674-714; ReviewRequest+engine.review at 716-732; sticky kwargs at 782-808). Note the noop path (_finish_noop_review, 316-365) and incremental scope (436-453, delta_paths 618-644) that must remain intact.
    - src/prevue/skills/selection.py (select_skills_hybrid + keyword_score signatures from 09-02)
    - src/prevue/classify/llm_fallback.py (llm_classify + llm_select_skills double-duty return from 09-02)
    - src/prevue/skills/loader.py (select_skills, assemble_instructions — the union/dedupe ordering to preserve)
    - src/prevue/classify/classifier.py (classify signature: classify(files, label_rules) → ClassificationResult)
    - src/prevue/classify/router.py (route(labels, routing_map) → bundle ids)
    - src/prevue/pack.py (make_file_weight, pack_files, trim_packed_files, readmit_files)
    - .planning/phases/09-RESEARCH.md (System Architecture Diagram target pipeline; Pitfalls 1, 2, 7; Anti-Patterns)
  </read_first>
  <action>
    Restructure run_review() to the D-01 order WITHOUT breaking noop/incremental/fork/skip/budget paths:
    1. Move classify+route ABOVE the pack/select block. Call `classify(reduced.files, ruleset.label_rules)` on the FULL filtered set (Pitfall 1 — NOT packed_files; the status-quo bug at review.py:674 passes packed_files). Keep `result_cls.dropped_count = len(dropped)`.
    2. Keep the LLM fallback (fallback_cfg.enabled and unmatched) but run it on the pre-pack unmatched set (Pitfall 2 — CONTEXT D-01 accepts LLM still sees only the small unmatched set; bill classify_tokens via estimate_classify_tokens on the pre-pack unmatched set). Extend the fallback to also obtain relevant skill names (double-duty via llm_select_skills / extended llm_classify from 09-02) — capture them as `llm_skill_names`.
    3. `result_cls.bundles = route(list(result_cls.labels.keys()), ruleset.routing_map)` — now available BEFORE selection.
    4. Replace BOTH `select_skills(skills, [f.path for f in packed_files])` call sites (review.py:546, 562, 583, 599) with `select_skills_hybrid(skills, paths, diff_text, result_cls.bundles, llm_skill_names=llm_skill_names)` where `paths` = packed-file paths and `diff_text` = concatenated patch text of packed files (the keyword-floor content signal). For bundles NOT in result_cls.bundles, hybrid selection must reduce to the existing glob behavior (verified by the non-routed test) — confirm select_skills_hybrid honors this (09-02 contract).
    5. Preserve the full trim → readmit → second-trim cascade and the `_skill_ratios`/`assemble_instructions` refresh after each matched-set change (review.py:546-611) — each refresh now calls select_skills_hybrid, not select_skills.
    6. Keep the existing MAX_PROMPT_BYTES probe (review.py:646-672) as the post-selection byte guard; since selection now happens before classify-LLM no longer mutates instructions afterward, ensure the probe runs AFTER the final matched/instructions are set (D-04 post-select byte-limit guard → neutral skip). Remove/replace the now-stale comment at review.py:717-719 ("no re-check is needed") to reflect the new order.
    7. weight/pack: continue using make_file_weight(ruleset.label_rules, skills=skills); packing still ranks by labels+skill globs. The reorder does not change pack inputs except that classify/route now precede it.
    8. Sticky kwargs: `loaded_skills` and `skill_ratios` come from the final post-union matched set (unchanged shape; Plan 09-06 adds source provenance). Do NOT regress the existing sticky audit (CLSF-03).
    Guardrails to NOT break (assert via existing tests staying green): fork guard (384-385), should_skip (398-407), noop (423-434), empty-reduced incremental noop (442-454), engine resolution (460-471), consumer skills base-ref guard (_consumer_skills_root 476), load_skills fail-closed (479-505).
    If a small shared helper improves clarity, add it to loader.py and import from review.py — do not duplicate dedupe/sort.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py -x -q && uv run ruff check src/prevue/review.py src/prevue/skills/loader.py</automated>
  </verify>
  <acceptance_criteria>
    - `classify(` in review.py is called with `reduced.files` (full set), NOT `packed_files` — assert via a test where a security file is dropped by packing yet the security bundle is still routed.
    - `select_skills(` direct calls in review.py replaced by `select_skills_hybrid(` (grep: `grep -c "select_skills_hybrid" src/prevue/review.py` >= 1; `grep -vE '^\s*#' src/prevue/review.py | grep -c "select_skills(" ` == 0).
    - `route(` is called before the first skill-selection call (classify-first order).
    - Existing test_review_flow.py tests pass unchanged (single-call regression backstop green).
    - New tests pass: test_classify_runs_on_full_set_not_packed, test_routed_bundle_skill_loads_via_union, test_llm_fallback_label_triggers_bundle_selection, test_non_routed_bundle_glob_unchanged, test_post_union_budget_neutral_skip.
    - noop and incremental integration tests still pass.
  </acceptance_criteria>
  <done>run_review classifies the full set first, routes, hybrid-selects skills from routed bundles into instructions before review, preserves the budget cascade and all guard paths, and keeps the single-call flow green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gap-demo-sandbox gap regression (D-12)</name>
  <files>tests/test_review_flow.py</files>
  <read_first>
    - tests/test_review_flow.py (CaptureEngine pattern at line 391: captures req.instructions; test_run_review_filtered_diff_and_classification_metadata at line 363 as the analog)
    - tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md (the gap-shape fixture from 09-01)
    - src/prevue/review.py (post-reorder run_review from Task 1)
    - tests/conftest.py (gap_shape_skill fixture, responses_activated, event_json)
  </read_first>
  <action>
    Add `test_gap_demo_skill_loaded` to test_review_flow.py reproducing the live gap shape (CONTEXT domain section): a packed path `src/pages/Checkout.jsx` (no `auth` segment, so it does NOT match the gap-demo skill's `applies-to: **/auth/**`), with classification routing the `security` bundle (either deterministic label rule assigning `security` to the changed path, or a patched llm_classify returning `security`). Use a CaptureEngine to capture `req.instructions`. Assert:
    - `GAP-DEMO-SKILL-LOADED` (the fixture body token) IS present in `captured["req"].instructions` — the routed-bundle skill loaded despite no glob match.
    - The gap-demo skill name appears in the sticky `loaded_skills` audit (read the upserted sticky body via the responses mock or the upsert spy).
    Add the LLM-fallback variant `test_llm_fallback_label_triggers_bundle_selection` (if not already covered in Task 1): patch llm_classify to add a `data` label on an unmatched path and return the data-bundle skill name; assert a data-bundle skill body loads despite no path glob match (D-03).
    Wire PREVUE_CONSUMER_ROOT (or the loader's consumer-root mechanism) so the gap-demo consumer fixture is loaded as a base-ref consumer skill (SKIL-04 path), mirroring existing consumer-skill tests.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py::test_gap_demo_skill_loaded tests/test_review_flow.py::test_llm_fallback_label_triggers_bundle_selection -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_gap_demo_skill_loaded` passes and would FAIL against the pre-reorder code (regression guard for SKIL-01).
    - The test path `src/pages/Checkout.jsx` provably does NOT match `**/auth/**` (glob assertion in the test or comment), yet the skill body loads — proving bundle routing, not glob, drove loading.
    - GAP-DEMO-SKILL-LOADED appears in captured engine instructions AND in the sticky loaded_skills audit.
    - LLM-fallback variant proves recovered-label bundles trigger the same selection path (D-03).
  </acceptance_criteria>
  <done>The gap-demo-sandbox gap shape is a passing regression test: routed bundle skill loads without a path glob match, via both deterministic and LLM-fallback labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff/paths → classify+select | Untrusted diff content drives classification and (via keyword_score) skill selection |
| consumer skills (base ref) → instructions | Base-ref skill bodies assembled into the engine prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Elevation of Privilege | SKIL-04 base-ref skill loading during reorder | mitigate | Reorder keeps _consumer_skills_root base-ref guard (review.py:476/77-104) and load_skills fail-closed BEFORE the classify/select block — V1 architecture control preserved; tested by existing SKIL-04 tests staying green |
| T-09-12 | Tampering (prompt injection) | assembled instructions from selected skill bodies + diff | mitigate | Diff stays fenced via build_prompt UNTRUSTED DATA + INSTRUCTION_REASSERTION (prompt.py); skill bodies are trusted base-ref content; reorder does not change prompt fencing |
| T-09-13 | Denial of Service | post-union over-budget prompt | mitigate | D-04 post-select trim_packed_files + MAX_PROMPT_BYTES guard → neutral skip; test_post_union_budget_neutral_skip enforces |
| T-09-14 | Spoofing/EoP | fork PR reaching review | mitigate | SECR-01 ForkPrUnsupported guard (review.py:384) remains the first check, unchanged |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; `uv sync --locked` |
</threat_model>

<verification>
- `uv run pytest tests/test_review_flow.py -x -q` green (existing + new).
- `uv run ruff check src/prevue/review.py src/prevue/skills/loader.py` clean.
- grep confirms classify on reduced.files, route before selection, select_skills_hybrid replaces select_skills in review.py.
- gap-demo-sandbox regression passes and is order-sensitive (fails on pre-reorder code).
</verification>

<success_criteria>
- SC-1: routed-bundle skills unioned into loaded set + assembled instructions before engine.review() (SKIL-01).
- SC-2: glob select unchanged for non-routed bundles (ROUT-01 scoping).
- SC-3: LLM fallback labels trigger the same union (D-03).
- SC-4: post-union re-trim + byte guard prevent overrun; neutral skip if still too large (D-04).
- SC-6: gap-demo-sandbox regression covers classified-bundle ≠ glob-path (D-12).
- CLSF-03 sticky audit preserved through the reorder.
</success_criteria>

<output>
Create `.planning/phases/09-classification-skill-loading-multi-call-review/09-04-SUMMARY.md` when done
</output>
