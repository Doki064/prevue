---
phase: 09-classification-skill-loading-multi-call-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/prevue/gate.py
  - src/prevue/config.py
  - tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md
  - tests/conftest.py
  - tests/test_gate.py
  - tests/test_config.py
  - tests/test_selection.py
  - tests/test_importscan.py
  - tests/test_multicall.py
  - tests/test_skills_loader.py
  - tests/test_review_flow.py
autonomous: true
requirements: [ENGN-05, ENGN-06, ENGN-07, SKIL-01]
must_haves:
  truths:
    - "All five new ReviewConfig caps parse from prevue.yml review: block with backward-compatible defaults (max_review_calls=1, review_concurrency=1)"
    - "The gap-demo-sandbox gap-shape fixture exists: a consumer skill whose applies-to does NOT match the test path but whose bundle is routable"
    - "RED test scaffolds for selection, importscan, and multicall exist and fail (xfail/skip-free) pending implementation"
  artifacts:
    - path: "src/prevue/gate.py"
      provides: "ReviewConfig with max_review_calls, review_concurrency, max_tokens_per_call, max_total_run_tokens, guardrail_skills fields"
      contains: "max_review_calls"
    - path: "tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md"
      provides: "Gap-shape skill fixture (applies-to **/auth/** + GAP-DEMO-SKILL-LOADED body token)"
      contains: "GAP-DEMO-SKILL-LOADED"
    - path: "tests/test_selection.py"
      provides: "RED unit scaffold for hybrid skill selection"
    - path: "tests/test_importscan.py"
      provides: "RED unit scaffold for import extraction"
    - path: "tests/test_multicall.py"
      provides: "RED unit scaffold for split/execute/merge"
  key_links:
    - from: "src/prevue/config.py"
      to: "src/prevue/gate.py"
      via: "ReviewConfig.model_validate(raw['review'])"
      pattern: "ReviewConfig"
---

<objective>
Lay the Wave 0 foundation for Phase 9: add the five new multi-call/run caps to `ReviewConfig` (D-09) with backward-compatible defaults, create the gap-demo-sandbox gap-shape test fixture (D-12), and write RED test scaffolds for the three new modules built in later waves (selection, importscan, multicall).

Purpose: Pin the config contract and test API surface up front so Wave 2+ executors implement against fixed signatures (interface-first ordering). The new caps must default to today's single-call behavior so the large existing `tests/test_review_flow.py` stays green as a regression backstop.
Output: Extended `ReviewConfig`, the gap-shape skill fixture + conftest wiring, and failing test scaffolds that later plans turn green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-CONTEXT.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This section lists every symbol Phase 9 creates so source-grounding drift checks exclude them.

New modules (created in later plans, scaffolded here):
- `src/prevue/skills/selection.py` — `keyword_score()`, `select_skills_hybrid()`, `KEYWORD_THRESHOLD` (Plan 09-02)
- `src/prevue/importscan.py` — `extract_imports()`, `referenced_paths()` (Plan 09-03)
- `src/prevue/multicall.py` — `CallGroup` (dataclass), `split_into_calls()`, `execute_calls()`, `merge_findings()` (Plan 09-05)

New `ReviewConfig` fields (this plan):
- `max_review_calls: int = 1` (ENGN-05)
- `review_concurrency: int = 1` (ENGN-07)
- `max_tokens_per_call: int = 120000` (D-09)
- `max_total_run_tokens: int = 480000` (D-09)
- `guardrail_skills: list[str] = []` (D-07 always-on per-call skill keys)

Extended functions (later plans):
- `llm_classify()` / new `llm_select_skills()` double-duty return (Plan 09-02)
- `select_skills` hybrid path in loader (Plan 09-02/09-04)
- review.py `run_review()` classify-first reorder + multi-call loop (Plan 09-04/09-05)
- `render_body()` / `upsert_sticky()` skill-source + per-call token meta + budget alert kwargs (Plan 09-06)

New test files: `tests/test_selection.py`, `tests/test_importscan.py`, `tests/test_multicall.py`
New fixture: `tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add five multi-call/run caps to ReviewConfig (D-09)</name>
  <read_first>
    - src/prevue/gate.py (ReviewConfig class, lines 19-41; note model_config extra="forbid" and _validate_token_budget)
    - src/prevue/config.py (load_config maps raw["review"] → ReviewConfig.model_validate, line 179)
    - tests/test_gate.py (existing ReviewConfig field tests to mirror)
    - tests/test_config.py (existing review-block parse tests)
  </read_first>
  <action>
    Add five fields to `ReviewConfig` in gate.py, all flat (per RESEARCH Open Question 2 — lean flat, parity with existing knobs under extra="forbid"):
    - `max_review_calls: int = Field(default=1, ge=1)` — ENGN-05; default 1 keeps single-call path.
    - `review_concurrency: int = Field(default=1, ge=1)` — ENGN-07; default 1 = sequential.
    - `max_tokens_per_call: int = Field(default=120000, ge=1, le=250_000)` — D-09; mirror the max_input_tokens bound so a passing per-call pack implies invoke success.
    - `max_total_run_tokens: int = Field(default=480000, ge=1)` — D-09 whole-run ceiling (classify + Σ review calls). 4× max_input_tokens starting point (RESEARCH A3).
    - `guardrail_skills: list[str] = Field(default_factory=list)` — D-07 always-on per-call skill keys as `bundle/filename` (e.g. `security/committed-secrets.md`); empty default = no forced guardrails, preserving current behavior.
    Extend the `_validate_token_budget` model_validator (or add a second after-validator) to assert `max_tokens_per_call <= max_total_run_tokens` (a per-call ceiling above the run ceiling is incoherent) and `output_reserve_tokens <= max_tokens_per_call`. Keep the existing output_reserve_tokens <= max_input_tokens check. Update the ReviewConfig docstring to cite D-09/ENGN-05/ENGN-06/ENGN-07.
    Do NOT change load_config — `ReviewConfig.model_validate(raw["review"])` already picks up new fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_gate.py tests/test_config.py -x -q && uv run python -c "from prevue.gate import ReviewConfig; c=ReviewConfig(); assert c.max_review_calls==1 and c.review_concurrency==1 and c.max_tokens_per_call==120000 and c.max_total_run_tokens==480000 and c.guardrail_skills==[]; print('caps OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ReviewConfig().max_review_calls == 1` and `review_concurrency == 1` (single-call/sequential defaults).
    - `ReviewConfig.model_validate({"max_review_calls": 0})` raises ValidationError (ge=1).
    - `ReviewConfig.model_validate({"max_tokens_per_call": 999999, "max_total_run_tokens": 100})` raises ValidationError (per-call > run ceiling).
    - `ReviewConfig.model_validate({"unknown_cap": 1})` still raises (extra="forbid" preserved).
    - Existing test_gate.py / test_config.py pass unchanged.
  </acceptance_criteria>
  <done>Five new caps parse with backward-compatible defaults; coherence validators reject incoherent combinations; extra="forbid" intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add config.py parse tests for new caps + nested vs flat decision record</name>
  <read_first>
    - src/prevue/config.py (load_config, ReviewConfig.model_validate at line 179)
    - tests/test_config.py (existing review-block YAML parse test pattern)
  </read_first>
  <action>
    In tests/test_config.py add tests proving the new caps flow through `load_config` from a prevue.yml `review:` block:
    - A YAML file with `review: {max_review_calls: 3, review_concurrency: 2, max_tokens_per_call: 60000, max_total_run_tokens: 200000, guardrail_skills: ["security/committed-secrets.md"]}` loads into `config.review` with those exact values.
    - A YAML file with no `review:` block yields all five defaults (1, 1, 120000, 480000, []).
    - A `review: {max_review_calls: 0}` block raises ValidationError through load_config.
    Use the existing tmp-path + resolve_consumer_config_path + load_config helper pattern already in test_config.py. Do not introduce a nested `multicall:` sub-model — record the flat-field decision in a one-line code comment citing RESEARCH Open Question 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "cap or multicall or review_block" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - At least three new test functions assert the caps round-trip through load_config.
    - Invalid cap value test asserts ValidationError is raised via the public load_config path, not just direct model_validate.
  </acceptance_criteria>
  <done>load_config surfaces the new caps from prevue.yml; defaults and validation proven through the public loader.</done>
</task>

<task type="auto">
  <name>Task 3: Gap-shape fixture + RED scaffolds for selection/importscan/multicall</name>
  <read_first>
    - tests/conftest.py (fixture style: skills_fixture_root, FakeEngine, sample_request)
    - tests/fixtures/skills/consumer/security/consumer-only-rule.md (consumer skill frontmatter format to copy)
    - tests/test_skills_loader.py (loader unit test style; fixture_skills usage)
    - src/prevue/skills/models.py (Skill fields: name, description, applies_to alias applies-to, bundle, filename, body, source)
    - .planning/phases/09-classification-skill-loading-multi-call-review/09-RESEARCH.md (Wave 0 Gaps + Phase Requirements → Test Map sections)
  </read_first>
  <action>
    1. Create `tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md` (the gap-demo-sandbox gap shape, D-12): frontmatter `name: Gap Demo Auth Guard`, `description: Verify authorization guards on checkout and payment flows.`, `applies-to: ["**/auth/**"]`; body MUST contain the distinctive token `GAP-DEMO-SKILL-LOADED`. This skill's bundle is `security` (path segment) but its applies-to does NOT match `src/pages/Checkout.jsx`.
    2. Add a conftest fixture `gap_shape_skill` returning a `Skill` instance for this shape (bundle="security", applies_to=["**/auth/**"], body contains GAP-DEMO-SKILL-LOADED) for unit tests that need it without disk I/O.
    3. Create RED scaffolds (functions that import the not-yet-existing symbols and assert intended behavior, expected to fail with ImportError until later waves):
       - `tests/test_selection.py`: import `keyword_score`, `select_skills_hybrid`, `KEYWORD_THRESHOLD` from `prevue.skills.selection`; one test per Plan 09-02 behavior (keyword floor selects high-score skill; below-threshold routed skill escalates not drops; non-routed below-threshold skill drops). Mark module-level `pytest.importorskip("prevue.skills.selection")` is FORBIDDEN — these must FAIL (RED), not skip. Use a try/except guard only to produce a clear failure message, or let the ImportError surface.
       - `tests/test_importscan.py`: import `extract_imports`, `referenced_paths` from `prevue.importscan`; tests for Python ast extraction, JS/TS regex extraction, and graceful degrade on unparseable content (Plan 09-03).
       - `tests/test_multicall.py`: import `split_into_calls`, `execute_calls`, `merge_findings`, `CallGroup` from `prevue.multicall`; tests for single-call when max_review_calls=1, bundle grouping, sequential execution, fail-soft, fingerprint merge/dedupe with severity tie-break (Plan 09-05).
    Each scaffold test body should encode the EXPECTED contract from RESEARCH (Patterns 1-4), so later implementers code to a fixed signature. Keep assertions minimal but real (not `assert True`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_selection.py tests/test_importscan.py tests/test_multicall.py -q 2>&1 | grep -qE "error|failed" && echo "RED scaffolds fail as expected" && grep -q "GAP-DEMO-SKILL-LOADED" tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md && echo "fixture OK"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/skills/consumer/security/gap-demo-auth-guard.md` exists, has valid Skill frontmatter (loadable via frontmatter.loads → Skill.model_validate), applies-to=["**/auth/**"], body contains GAP-DEMO-SKILL-LOADED.
    - `tests/test_selection.py`, `tests/test_importscan.py`, `tests/test_multicall.py` exist and FAIL (collection or assertion error) because target modules do not yet exist — NOT skipped.
    - No `pytest.importorskip` or `@pytest.mark.skip` masking the RED state.
    - `uv run pytest` (full suite) shows ONLY the three new scaffold files failing; all pre-existing tests still pass.
  </acceptance_criteria>
  <done>Gap-shape fixture in place; three RED scaffolds encode the target contracts and fail pending implementation; existing suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer prevue.yml → ReviewConfig | Consumer-supplied cap values cross into runtime budget/concurrency control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Denial of Service | `review_concurrency`, `max_review_calls` caps | mitigate | `ge=1` floors prevent negative/zero; consumer can raise concurrency but each call is still subprocess-bound and bounded by `max_workers`; whole-run ceiling `max_total_run_tokens` caps total spend (enforced in Plan 09-05) |
| T-09-02 | Tampering | prevue.yml unknown keys | mitigate | `extra="forbid"` on ReviewConfig rejects typo'd/injected cap keys at validation; coherence validators reject incoherent per-call > run ceiling |
| T-09-03 | Elevation of Privilege | consumer prevue.yml read from PR head | accept | Existing SKIL-04 sentinel (config.py resolve_consumer_config_path) already fails closed to framework defaults when base ref unavailable in Actions — unchanged by this plan |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable); `uv sync --locked` only |
</threat_model>

<verification>
- `uv run pytest tests/test_gate.py tests/test_config.py -x -q` green (caps parse + validate).
- `uv run pytest` shows exactly the three new RED scaffold files failing, everything else green.
- `uv run ruff check src/prevue/gate.py src/prevue/config.py` clean.
- Gap-shape fixture loadable as a Skill and contains GAP-DEMO-SKILL-LOADED.
</verification>

<success_criteria>
- Five new ReviewConfig caps with backward-compatible defaults, coherence-validated, extra="forbid" intact (D-09, ENGN-05/06/07).
- prevue.yml round-trips the caps through load_config (CLSF-03 audit-config parity).
- gap-demo-sandbox gap-shape fixture + conftest helper exist (D-12).
- Three RED scaffolds pin the selection/importscan/multicall contracts for later waves.
- Existing full suite stays green (single-call default preserved).
</success_criteria>

<output>
Create `.planning/phases/09-classification-skill-loading-multi-call-review/09-01-SUMMARY.md` when done
</output>
