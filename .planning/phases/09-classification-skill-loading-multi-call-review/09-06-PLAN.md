---
phase: 09-classification-skill-loading-multi-call-review
plan: 06
type: execute
wave: 5
depends_on: ["09-04", "09-05"]
files_modified:
  - src/prevue/github/comments.py
  - src/prevue/review.py
  - tests/test_comments.py
  - tests/test_review_flow.py
  - docs/ARCHITECTURE.md
  - docs/configuration.md
autonomous: false
requirements: [OUTP-04, CLSF-03]
must_haves:
  truths:
    - "Sticky Metadata reflects the final post-selection loaded skills, distinguishing routed/keyword/LLM sources where useful"
    - "Multi-call runs show per-bundle/per-call token meta in the sticky"
    - "skill_ratios denominators use the full merged load_skills() set"
    - "The whole-run budget-reached alert is prominent (a visible section/line), not a buried metadata line"
    - "docs/ARCHITECTURE.md pipeline diagram matches the classify-first + multi-call implementation"
    - "A live gap-demo-sandbox PR re-run shows GAP-DEMO-SKILL-LOADED when security routes"
  artifacts:
    - path: "src/prevue/github/comments.py"
      provides: "render_body skill-source provenance + per-call token meta + prominent budget-reached alert"
      contains: "run token budget"
    - path: "docs/ARCHITECTURE.md"
      provides: "updated classify-first + multi-call pipeline steps"
      contains: "classify"
  key_links:
    - from: "src/prevue/review.py"
      to: "src/prevue/github/comments.py"
      via: "sticky_base_kwargs threads per-call token meta + skill sources + budget alert"
      pattern: "upsert_sticky"
---

<objective>
Finalize the audit and documentation for Phase 9 (OUTP-04/CLSF-03/D-10/D-11): extend the sticky to disclose final loaded skills with their selection source, per-bundle/per-call token meta on multi-call runs, and a PROMINENT whole-run budget-reached alert; update the architecture/configuration docs to match the classify-first + multi-call pipeline; and run the end-of-phase live verification on the gap-demo-sandbox PR.

Purpose: The reorder (09-04) and multi-call (09-05) changed what the sticky must report. D-11 requires the audit to reflect post-selection skills and per-call token breakdown; D-10 requires the budget-reached alert to be impossible to miss (user emphasis). The docs pipeline diagram currently says route() is "metadata only (not skill gating)" — now false. This plan closes the audit + docs + live-proof loop.
Output: Sticky audit extensions (TDD), updated docs, and a passing live UAT (human-verify checkpoint) confirming the gap closure on a real PR.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-CONTEXT.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-RESEARCH.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-04-SUMMARY.md
@.planning/phases/09-classification-skill-loading-multi-call-review/09-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan modifies (no new modules):
- `src/prevue/github/comments.py` — `render_body`/`upsert_sticky` gain kwargs: skill-source provenance (e.g. `skill_sources: dict[str,str]` mapping skill key → "routed"/"keyword"/"llm"), per-call token meta rendering (consumes token_meta["per_call"]), and a prominent budget-reached alert (rendered as a visible section near Verdict/Coverage, not inside the collapsed Metadata details)
- `src/prevue/review.py` — thread the new kwargs into sticky_base_kwargs from data produced by 09-05
- `docs/ARCHITECTURE.md`, `docs/configuration.md` — pipeline + routing/config doc updates
New tests: `tests/test_comments.py::test_render_body_skill_source_provenance`, `::test_render_body_per_call_token_meta`, `::test_render_body_prominent_budget_alert`; `tests/test_review_flow.py::test_sticky_multicall_token_meta`
All other Phase 9 symbols are produced by sibling plans — see 09-01 artifacts section.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sticky skill-source provenance + per-call token meta + prominent budget alert (D-10/D-11)</name>
  <files>src/prevue/github/comments.py, src/prevue/review.py, tests/test_comments.py</files>
  <read_first>
    - src/prevue/github/comments.py (render_body 442-594: Skills line 494-497; token_meta block 520-532; skill_ratios 534-544; not_reviewed coverage 546-551; coverage_section 560-571; section ordering in the final return 581-594)
    - src/prevue/review.py (sticky_base_kwargs 782-808: loaded_skills, skill_ratios, token_meta; the per-call data produced by 09-05)
    - tests/test_comments.py (render_body test style; existing token_meta/skill_ratios assertions)
    - .planning/phases/09-RESEARCH.md (Pitfalls 3 union skills audit, 5 sum review tokens; D-10 prominent alert; A4 sticky schema)
    - .planning/phases/09-CONTEXT.md (D-11 routed vs keyword-vs-LLM source disclosure; D-10 prominent budget-reached alert — "not a buried metadata line")
  </read_first>
  <action>
    Extend render_body (and upsert_sticky passthrough) in comments.py:
    1. Skill-source provenance (D-11): accept an optional `skill_sources` kwarg mapping skill key/name → source ("routed", "keyword", "llm"). When present, annotate the existing `Skills:` metadata line (or add a compact "Skill sources:" line) showing how each loaded skill was selected. Keep the existing `loaded_skills` builtin/consumer suffix intact. When absent, render exactly as today (backward compatible).
    2. Per-call token meta (D-11/Pitfall 5): when token_meta carries a `per_call` breakdown (list of {bundle/label, review tokens}), render a per-bundle/per-call token line under the existing Tokens line. Aggregate review tokens already summed in 09-05; this adds the breakdown only. Single-call runs (no per_call key) render unchanged.
    3. PROMINENT budget-reached alert (D-10, user emphasis): when the whole-run cap forced files to be skipped (a new kwarg e.g. `run_budget_reached: bool` + count/reason from 09-05), render a VISIBLE alert — a bold line or its own section placed near the Verdict/Coverage area (NOT inside the collapsed `<details>` Metadata block). Reuse/extend the existing Coverage section pattern (comments.py:560-571) but ensure the run-budget case is distinguishable from the ordinary over-budget packing case and is not collapsed by default. The alert text must contain "run token budget reached" and the N-files-not-reviewed count.
    4. Thread the new kwargs through review.py sticky_base_kwargs from the data 09-05 produced (union skills for loaded_skills/skill_ratios per Pitfall 3; skill_sources map; per_call token meta; run_budget_reached + count).
    TDD: write tests in test_comments.py first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>uv run pytest tests/test_comments.py -k "skill_source or per_call or budget_alert" -x -q && uv run ruff check src/prevue/github/comments.py src/prevue/review.py</automated>
  </verify>
  <acceptance_criteria>
    - `test_render_body_skill_source_provenance` passes: a routed-but-low-keyword skill is shown with source "routed"/"llm"; a keyword-floor skill shows "keyword".
    - `test_render_body_per_call_token_meta` passes: a token_meta with per_call breakdown renders a per-bundle token line; without it, the Tokens line is unchanged.
    - `test_render_body_prominent_budget_alert` passes: when run_budget_reached, the alert text contains "run token budget reached" + the count AND is NOT inside the `<details>` Metadata block (assert it appears before the Metadata heading in the body string).
    - Backward compatible: existing test_comments.py render_body tests pass unchanged (new kwargs default to None/False → today's output).
    - skill_ratios denominators use the full merged load_skills set (Pitfall 3) — assert via a multi-call sticky test.
  </acceptance_criteria>
  <done>Sticky discloses skill selection source, per-call token meta, and a prominent (non-collapsed) whole-run budget-reached alert; review.py threads the data; backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-call sticky integration test + docs pipeline update (CLSF-03)</name>
  <files>tests/test_review_flow.py, docs/ARCHITECTURE.md, docs/configuration.md</files>
  <read_first>
    - tests/test_review_flow.py (sticky-assertion test pattern; multi-call tests from 09-05)
    - docs/ARCHITECTURE.md (pipeline steps 8-12 at lines 58-62: step 10 "Select skills", step 11 "Classification ... route for metadata" — both now wrong; mermaid at 19-21 and ASCII layer map at 40-42)
    - docs/configuration.md (routing section ~line 70: "Does not gate skill loading" — now wrong for routed bundles)
    - .planning/phases/09-RESEARCH.md (System Architecture Diagram target pipeline — the canonical new order)
  </read_first>
  <action>
    1. Add `test_sticky_multicall_token_meta` to test_review_flow.py: a 2-call run asserts the sticky body contains the per-bundle/per-call token breakdown and the union loaded_skills (skills from both calls), and the skill_ratios coverage reflects the full load_skills denominator.
    2. Update docs/ARCHITECTURE.md to the classify-first + multi-call pipeline (match RESEARCH target diagram):
       - Reorder the numbered pipeline steps so classify+route precede skill selection and packing: filter → classify(full set) → route → select skills (hybrid keyword floor + LLM escalation within routed bundles) → label-weighted pack → assemble → re-trim/byte-guard → multi-call split (bundle + import co-location, default 1) → execute (sequential/parallel) → merge+dedupe (fingerprint) → one gate → publish.
       - Update the mermaid graph (lines 19-21) and the ASCII layer map (lines 40-42) so route is shown driving skill selection, not "metadata only".
       - Add a short subsection documenting multi-call (max_review_calls, review_concurrency, max_tokens_per_call, max_total_run_tokens, guardrail_skills) and the whole-run budget-reached behavior.
    3. Update docs/configuration.md: remove "Does not gate skill loading" for routed bundles; clarify path-glob for non-routed bundles + bundle-scoped hybrid selection for routed; document the five new review caps with defaults and the SKIL-01-now-means-routed note (Deferred Ideas: SKIL-01 wording amend).
    Keep doc grep-checkable: the pipeline must mention classify before select; the configuration must mention the new caps.
  </action>
  <verify>
    <automated>uv run pytest tests/test_review_flow.py::test_sticky_multicall_token_meta -x -q && grep -vE '^\s*#|^\s*<!--' docs/ARCHITECTURE.md | grep -qi "classify" && grep -q "max_review_calls" docs/configuration.md && echo "docs OK"</automated>
  </verify>
  <acceptance_criteria>
    - `test_sticky_multicall_token_meta` passes (union skills + per-call token meta in sticky).
    - docs/ARCHITECTURE.md no longer states route is "metadata only / not skill gating"; pipeline shows classify-first driving selection (grep: the phrase "metadata only" or "not skill gating" absent from the routing/classify step).
    - docs/ARCHITECTURE.md documents the five multi-call caps and whole-run budget behavior.
    - docs/configuration.md documents max_review_calls + the other four caps with defaults and no longer claims routing does not gate skill loading.
  </acceptance_criteria>
  <done>Multi-call sticky integration test passes; ARCHITECTURE.md and configuration.md match the classify-first + multi-call implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live gap-demo-sandbox gap-closure + multi-call UAT</name>
  <what-built>
    Phase 9 closes the SKIL-01 classify/route → skill-loading gap (classify-first reorder + hybrid B+D selection) and adds configurable multi-call review (split/merge/dedupe/optional parallel). The deterministic gap closure is unit/integration-tested (gap-demo-sandbox regression in 09-04). This checkpoint confirms the fix end-to-end on a real PR with a live engine, and exercises a multi-call run.
  </what-built>
  <how-to-verify>
    1. Re-run Prevue review on gap-demo-sandbox PR #25 (or #26) where `src/pages/Checkout.jsx` is changed and the consumer skill `gap-demo-auth-guard` (applies-to **/auth/**, bundle security) exists at the base ref.
       - Expected: the sticky `Bundles:` includes `security` (classify routes it), the sticky `Skills:` audit lists the gap-demo skill, and the review surfaces the `GAP-DEMO-SKILL-LOADED` evidence (a finding or behavior the skill instructs) — proving the routed-bundle skill loaded despite no path glob match.
       - Before Phase 9 this PR loaded only a generic security skill via broad glob and showed `Labels: frontend` only, no gap-demo skill.
    2. Trigger a multi-call run on a larger PR (or set `review: {max_review_calls: 2}` in the sandbox prevue.yml) and confirm:
       - The sticky shows per-bundle/per-call token meta.
       - Findings from both calls appear merged/deduped (no duplicate same-title findings on the same path).
       - One pass/fail check and one sticky comment (not N).
    3. (Optional) Force the whole-run cap (lower max_total_run_tokens) on a large PR and confirm the PROMINENT "run token budget reached — N files not reviewed" alert appears outside the collapsed Metadata, with a neutral check (not a red fail).
    Any auth/CLI errors during the live run create a dynamic auth gate — authenticate and retry.
  </how-to-verify>
  <verify>
    <human-check>Confirm: (a) gap-demo skill loads when security routes on PR #25/#26, (b) multi-call sticky shows per-call token meta + merged findings + one check/one sticky, (c) whole-run budget alert is prominent and neutral. Type "approved" or describe issues.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
  <done>Live PR confirms gap closure (routed-bundle skill loads) and multi-call behaves (per-call meta, merged findings, one check/sticky, prominent neutral budget alert).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selection/token data → sticky markdown | Internal audit data rendered into the public PR comment |
| skill names / classification labels → sticky | Label `matched` value can be an untrusted PR path after LLM fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Tampering (markdown injection) | new sticky lines (skill sources, per-call meta, budget alert) | mitigate | Reuse existing _escape_table_cell / _escape_path_code / _neutralize_html (comments.py) for any value that can carry untrusted content (skill names, label matched-paths); new lines must escape exactly as the adjacent existing lines do |
| T-09-20 | Repudiation / false-green | budget alert hidden, run appears clean | mitigate | D-10 alert rendered OUTSIDE the collapsed Metadata details with neutral (partial) conclusion; test asserts the alert precedes the Metadata heading so reviewers cannot miss skipped coverage |
| T-09-21 | Information Disclosure | per-call token meta leaking paths/content | accept | Token meta is integer counts + bundle labels only (no diff content); bundle labels are framework-controlled canonical labels |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase; live UAT uses already-pinned Copilot CLI on the runner |
</threat_model>

<verification>
- `uv run pytest tests/test_comments.py tests/test_review_flow.py -x -q` green.
- `uv run ruff check src/prevue/github/comments.py src/prevue/review.py` clean.
- Docs grep checks pass (classify-first pipeline; caps documented; no "metadata only" routing claim).
- Live UAT approved (human checkpoint).
- Full suite green: `uv run pytest -q`.
</verification>

<success_criteria>
- SC-5: sticky Metadata reflects final loaded skills post-selection; docs pipeline diagram matches implementation (D-11/CLSF-03).
- Multi-call per-bundle/per-call token meta in sticky (OUTP-04/D-11).
- Prominent whole-run budget-reached alert, neutral (partial) — not buried (D-10).
- skill_ratios denominators use full merged load_skills set (Pitfall 3).
- Live gap-demo-sandbox PR proves gap closure end-to-end (D-12 live validation).
</success_criteria>

<output>
Create `.planning/phases/09-classification-skill-loading-multi-call-review/09-06-SUMMARY.md` when done
</output>
