---
phase: 10-boundary-contracts
plan: 03
type: tdd
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/prevue/engines/usage.py
  - src/prevue/pricing/__init__.py
  - src/prevue/pricing/model_prices.json
  - src/prevue/engines/flow.py
  - src/prevue/engines/tokens.py
  - tests/test_usage_capture.py
  - tests/test_pricing.py
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "Claude stdout-json usage is captured as real input/output/cache tokens with estimated=False"
    - "Cursor and Antigravity fall back to bytes/4 with estimated=True (per-engine, not global)"
    - "Copilot OTEL JSONL is parsed and summed into real tokens with estimated=False"
    - "compute_cost applies the cache-aware formula from the vendored pricing table; unknown model → no cost, labeled"
    - "engine.pricing override (passed in) takes precedence over the bundled snapshot"
    - "When the engine returns total_cost_usd (Claude), it is preferred over recomputation"
    - "Switching Claude to stdout-json does not break fence extraction (Pitfall 3) — result field is parsed before extract_json_fence"
    - "tokens.py bytes/4 remains as the explicitly labeled fallback only"
    - "prevue.pricing is a PACKAGE: __init__.py re-exports compute_cost/load_pricing_table; model_prices.json lives inside the package dir"
    - "Every pytest test injects the small sample_prices.json via table= — no pytest test loads the full vendored snapshot"
  artifacts:
    - path: "src/prevue/engines/usage.py"
      provides: "capture_usage(spec, stdout, otel_path) per-strategy parser"
      contains: "def capture_usage"
    - path: "src/prevue/pricing/__init__.py"
      provides: "compute_cost + load_pricing_table (pure compute; package re-exports), reads vendored JSON from package dir"
      contains: "def compute_cost"
    - path: "src/prevue/pricing/model_prices.json"
      provides: "vendored pinned LiteLLM pricing snapshot inside the pricing package (D-06a)"
  key_links:
    - from: "src/prevue/engines/flow.py"
      to: "src/prevue/engines/usage.py"
      via: "capture_usage called in token-meta builders"
      pattern: "capture_usage"
    - from: "src/prevue/engines/usage.py"
      to: "src/prevue/engines/tokens.py"
      via: "estimate_tokens fallback when capture returns None"
      pattern: "estimate_tokens"
    - from: "src/prevue/pricing/__init__.py"
      to: "src/prevue/pricing/model_prices.json"
      via: "importlib.resources.files('prevue.pricing') / 'model_prices.json' (never fetched — D-06a)"
      pattern: "model_prices.json"
---

<objective>
Deliver real per-engine token accounting (PERF-03 / D-04) and dollar-cost computation (D-05/D-06). Add a per-strategy `usage.capture_usage` dispatched by `spec.usage_capture` (stdout-json | otel-jsonl | none), wire it into `flow.py`'s two token-meta builders so the `estimated` flag is set per-engine, vendor the pinned LiteLLM pricing snapshot (D-06a) INSIDE a `prevue.pricing` PACKAGE, and add a pure `compute_cost`. `bytes/4` in `tokens.py` is demoted to the labeled fallback. This is added ONCE against the generic adapter (the reason ENGN-10 landed first).

Purpose: Turn OUTP-04's "~est tokens" into measured tokens + cost where the CLI reports them, with honest per-engine estimate labeling otherwise.
Output: `usage.py`, the `prevue.pricing` package (`__init__.py` + `model_prices.json`), updated `flow.py`/`tokens.py`, GREEN PERF-03 tests.
</objective>

<artifacts_this_phase_produces>
NEW symbols:
- `prevue.engines.usage.capture_usage(spec, stdout, otel_path=None) -> dict | None` — returns `{"input","output","cache_read","cache_creation","cost_usd"?,"estimated":False}` for stdout-json/otel-jsonl, or `None` for "none" (caller falls back to bytes/4).
- `prevue.engines.usage._parse_copilot_otel(path)` (internal OTEL JSONL summer).
- `prevue.pricing.compute_cost(engine, model, usage, override=None, table=None) -> float | None` (defined in and re-exported from `src/prevue/pricing/__init__.py`).
- `prevue.pricing.load_pricing_table() -> dict` (reads the vendored JSON from inside the `prevue.pricing` package; cached).
- NEW PACKAGE `src/prevue/pricing/` containing `__init__.py` (the compute module) and `model_prices.json` (pinned LiteLLM snapshot, D-06a). `prevue.pricing` is a PACKAGE, NOT a module file — there is no `src/prevue/pricing.py`.
MODIFIED: `flow._token_meta` / `flow._retry_token_meta` accept a captured-usage dict + the spec's `usage_capture`; `extract_json_fence` runs on the Claude envelope's `result` field for stdout-json (Pitfall 3). `tokens.estimate_tokens` docstring marks it the labeled fallback.
Extended `engine_meta["tokens"]` shape: `{"review", "input"?, "output"?, "cache_read"?, "cache_creation"?, "cost_usd"?, "estimated": bool}`.

> Import-stability note: the Plan-01 RED tests import `from prevue.pricing import compute_cost, load_pricing_table`. Because `prevue.pricing` is a package whose `__init__.py` defines/re-exports those symbols, that import path is UNCHANGED — no Plan-01 edit is required.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-boundary-contracts/10-RESEARCH.md
@.planning/phases/10-boundary-contracts/10-PATTERNS.md
@.planning/phases/10-boundary-contracts/10-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Vendor pricing snapshot as a package + pure compute_cost (GREEN test_pricing)</name>
  <read_first>
    - tests/test_pricing.py (the RED tests from Plan 01 — the contract this task turns GREEN; they import `from prevue.pricing import compute_cost, load_pricing_table`)
    - tests/fixtures/pricing/sample_prices.json (unit-test fixture — EVERY pytest test injects this via `table=`, NOT the 500 KB snapshot)
    - src/prevue/engines/tokens.py (nearest pure-module convention)
    - src/prevue/config.py (`path.read_text` + parse convention at load_config:164 — pricing uses stdlib `json` instead of yaml)
    - pyproject.toml (build backend / package-data config — confirm the pricing package + its JSON ship in the wheel, A5)
    - 10-RESEARCH.md § "Cost Computation" (formula + field names: input_cost_per_token, output_cost_per_token, cache_read_input_token_cost, cache_creation_input_token_cost; tiered above_200k fields; prefer total_cost_usd; alias/normalization for model→row; unknown model → no cost labeled) and § "Standard Stack — Vendored data" (note: snapshot committed under `src/prevue/`)
    - 10-PATTERNS.md § "src/prevue/pricing.py" (the compute pattern — apply it inside the pricing PACKAGE `__init__.py`, not a module file) and § "No Analog Found" (vendored JSON ships in the wheel)
  </read_first>
  <behavior>
    - `compute_cost(engine, model, usage, override, table)` = input*input_cost_per_token + output*output_cost_per_token + cache_read*cache_read_input_token_cost + cache_creation*cache_creation_input_token_cost, summing only fields present in the row.
    - `usage` carrying a non-None `cost_usd` (Claude's `total_cost_usd`) → return that value verbatim (authoritative for that vendor).
    - `override` (the D-06c `engine.pricing` map) row for `model` shadows the table row.
    - Unknown model (absent from override and table) → returns None (no cost; caller labels it).
    - `load_pricing_table()` reads `model_prices.json` from INSIDE the `prevue.pricing` package, never fetches (D-06a); result cached.
    - The pricing table is injectable: `compute_cost(..., table=<dict>)` uses the passed table; tests always pass the small `sample_prices.json` so no pytest test loads the full snapshot.
  </behavior>
  <action>
    Create the `prevue.pricing` PACKAGE (NOT a `pricing.py` module file — that would collide with the `pricing/` directory the JSON must live in):
    - `src/prevue/pricing/__init__.py` — defines (and is the public surface for) `load_pricing_table()` and `compute_cost(...)`. Record the LiteLLM source URL + pin date in a top-of-file comment here.
    - `src/prevue/pricing/model_prices.json` — the vendored pinned snapshot.
    Vendor the snapshot: download the current LiteLLM `model_prices_and_context_window.json` to `src/prevue/pricing/model_prices.json` (the ONLY network touch — a one-time vendoring commit, NOT a runtime fetch; `compute_cost` never fetches). `load_pricing_table()` resolves the JSON via `importlib.resources.files("prevue.pricing") / "model_prices.json"` (preferred) OR `Path(__file__).parent / "model_prices.json"` — NEVER `Path(__file__).parent / "pricing" / "model_prices.json"` (that path is wrong under the package layout). Cache via `functools.lru_cache`. Implement the pure `compute_cost(...)` per the behavior block, including a small model→row normalization (lowercase / strip provider prefix) with `engine.pricing` override precedence and a `table=` parameter (defaults to `load_pricing_table()` when None).
    Confirm the build backend ships `src/prevue/pricing/model_prices.json` as package data (A5) — run a built-wheel check; add explicit package-data config in `pyproject.toml` only if the JSON is missing from the wheel.
    Turn the Plan-01 `tests/test_pricing.py` GREEN, ensuring EVERY pytest test passes `table=<loaded sample_prices.json>` (the small fixture) — no pytest test calls the zero-arg `load_pricing_table()` against the full snapshot (WARNING 2). The full-snapshot check stays a standalone CLI acceptance below, not a pytest test. If a Plan-01 RED assertion encoded a wrong field name, fix the test to match 10-RESEARCH field names and note it in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pricing.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `prevue.pricing` is a PACKAGE, not a module file: `test -d src/prevue/pricing && test -f src/prevue/pricing/__init__.py && test -f src/prevue/pricing/model_prices.json && ! test -f src/prevue/pricing.py`.
    - The import path is unchanged for downstream: `uv run python -c "from prevue.pricing import compute_cost, load_pricing_table; print('ok')"`.
    - `tests/test_pricing.py` is GREEN.
    - WARNING 2 — no pytest test loads the full snapshot: every test injects the fixture. Enforce with `grep -q 'sample_prices' tests/test_pricing.py` AND `! grep -nE 'load_pricing_table\(\s*\)' tests/test_pricing.py` (no zero-arg full-table load inside pytest; the only zero-arg load lives in production + the standalone CLI check below).
    - `compute_cost` never performs network I/O: `grep -REq 'requests|urlopen|httpx|urllib.request' src/prevue/pricing/ ; test $? -ne 0` (zero matches).
    - Standalone CLI acceptance (NOT a pytest test) — vendored snapshot exists and is valid + large: `uv run python -c "import prevue.pricing as p; t=p.load_pricing_table(); assert isinstance(t,dict) and len(t)>50; print(len(t))"`.
    - Unknown model returns None: `uv run python -c "from prevue.pricing import compute_cost; print(compute_cost('x','no-such-model',{'input':10,'output':10}))"` prints `None`.
  </acceptance_criteria>
  <done>prevue.pricing is a package (__init__.py + model_prices.json, no pricing.py); compute_cost is pure + cache-aware + override-aware + table-injectable; pytest tests use the small fixture only; test_pricing green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Per-strategy usage capture + flow seam (GREEN test_usage_capture)</name>
  <read_first>
    - tests/test_usage_capture.py (Plan 01 RED tests — the contract to turn GREEN)
    - tests/fixtures/usage/{claude_envelope.json,cursor_envelope.json,copilot_otel.jsonl,antigravity_text.txt}
    - src/prevue/engines/flow.py (`_token_meta` lines 15-19, `_retry_token_meta` lines 22-34, the `_tokens()` closure lines 132-135, `extract_json_fence(stdout)` call at line 90, engine_meta assembly lines 144-162)
    - src/prevue/engines/tokens.py (estimate_tokens — the fallback)
    - src/prevue/engines/spec.py (the `usage_capture` field, Plan 02)
    - src/prevue/engines/cli_adapter.py (where review() obtains stdout — usage capture must thread spec + stdout into flow; for otel, the OTEL log path env must be readable post-run)
    - 10-RESEARCH.md § "Pattern 2: Per-engine usage capture strategy", § "Per-Engine Token Reporting Matrix", Pitfall 1 (uniform-stdout assumption) + Pitfall 3 (Claude JSON breaks fence parsing)
    - 10-PATTERNS.md § "src/prevue/engines/usage.py" and § "src/prevue/engines/flow.py"
  </read_first>
  <behavior>
    - `capture_usage(spec, stdout, otel_path)`:
      - `usage_capture=="stdout-json"`: parse the Claude envelope, read `usage.{input_tokens,output_tokens,cache_read_input_tokens,cache_creation_input_tokens}` + `total_cost_usd`, return them with `estimated=False`. The review text for fence extraction is the envelope's `result` field, not raw stdout (Pitfall 3).
      - `usage_capture=="otel-jsonl"`: read + sum token attributes across the JSONL at `otel_path`, return real tokens with `estimated=False`.
      - `usage_capture=="none"`: return None → caller uses `estimate_tokens` with `estimated=True`.
    - `flow` token-meta: when capture returns a dict, carry input/output/cache + cost_usd + `estimated=False`; when None, keep bytes/4 with `estimated=True`. Set `estimated` per-engine, never globally.
    - For stdout-json engines, `extract_json_fence` runs on the parsed `result`, so Claude reviews still produce findings (not all-degraded).
    - For otel engines, when `COPILOT_OTEL_FILE_EXPORTER_PATH` / `otel_path` is unset or empty, capture returns None and the engine falls back to bytes/4 (`estimated=True`) — this is the EXPECTED state until the workflow sets the env in Plan 05 (cross-wave dependency, see WARNING 3 note in Plan 05).
  </behavior>
  <action>
    Create `src/prevue/engines/usage.py` with `capture_usage` + `_parse_copilot_otel` per the behavior block, importing `estimate_tokens` only as the documented fallback boundary (the fallback itself stays in flow). Modify `flow.py`: have `review_with_retry` (and the generic via it) pass `spec.usage_capture` + the raw stdout (and, for otel, the OTEL log path) into the token-meta builders; for `stdout-json`, parse the envelope and run `extract_json_fence` on `result` (Pitfall 3) — guard against a non-JSON stdout by degrading to the existing fence path. Extend the `engine_meta["tokens"]` dict (lines 144-162) with input/output/cache/cost_usd/estimated WITHOUT changing the `ReviewResult` construction shape. Update `tokens.py`'s `estimate_tokens` docstring to state it is the labeled fallback used only when an engine cannot report usage (no logic change). Thread the OTEL log path: `cli_adapter` reads `COPILOT_OTEL_FILE_EXPORTER_PATH` for copilot so capture can read it post-run (env value comes from the workflow in Plan 05; capture MUST no-op safely when the path is unset/empty → fall back to estimate). Add a code comment noting Copilot reports `estimated:true` until Plan 05 wires the env (WARNING 3).
    Turn `tests/test_usage_capture.py` GREEN. Add a regression assertion that a Claude stdout-json envelope still yields findings via `extract_json_fence(result)` (Pitfall 3) — extend the test if the Plan-01 scaffold did not cover it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_usage_capture.py tests/test_engine_flow.py tests/test_engine_contract.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_usage_capture.py` GREEN: Claude→real tokens estimated:false; Cursor/Antigravity→estimated:true; Copilot OTEL summed→estimated:false (with otel_path set in the test); Copilot with unset otel_path→None→estimate (WARNING 3 fallback covered).
    - Claude JSON path still extracts findings (Pitfall 3 regression): a test asserts a stdout-json envelope with a fenced `result` yields ≥1 finding, not degraded.
    - `test_engine_contract.py` + `test_engine_flow.py` stay GREEN (token-meta shape change is additive).
    - Per-engine estimated flag (not global): `grep -q 'estimated' src/prevue/engines/usage.py` and the cursor/antigravity capture returns None (estimate path).
  </acceptance_criteria>
  <done>usage.capture_usage dispatches per strategy; flow sets per-engine estimated + real tokens; Claude fence parsing preserved; Copilot OTEL no-ops to estimate when env unset; PERF-03 capture tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored pricing JSON → cost compute | Pricing data is committed inside the pricing package, base-ref-controlled; never fetched at review time (D-06a). |
| engine stdout / OTEL log → usage parser | The CLI's stdout/OTEL file is untrusted output parsed for numbers only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | runtime pricing fetch poisoning cost | mitigate | `compute_cost`/`load_pricing_table` read only the vendored package JSON via `importlib.resources` — no network call (grep gate asserts zero network imports under `src/prevue/pricing/`). The scheduled-PR bump is Plan 06 (human-reviewed, never auto-merge). |
| T-10-07 | Denial of Service | malformed engine stdout crashes usage parser | mitigate | `capture_usage` wraps `json.loads`/OTEL parse in try/except and returns None (falls back to bytes/4) rather than raising — Pitfall 1 warning sign covered by the fallback test. |
| T-10-08 | Information Disclosure | secret in stdout/OTEL leaking into engine_meta | mitigate | Capture extracts only numeric token fields + cost; no raw stdout stored in `engine_meta`. Existing `sanitize_stderr` path unchanged. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (per 10-RESEARCH Package Legitimacy Audit). The pricing JSON is vendored data inside the package, not a dependency; one-time download committed + reviewed in this PR. |
</threat_model>

<verification>
- `uv run pytest tests/test_usage_capture.py tests/test_pricing.py tests/test_engine_flow.py tests/test_engine_contract.py -x -q` green.
- `uv run pytest -q` green (token-meta extension is additive; existing review-flow tests unaffected).
- `prevue.pricing` is a package (not a `pricing.py` file): `test -f src/prevue/pricing/__init__.py && ! test -f src/prevue/pricing.py`.
- `grep -REq 'requests|urlopen|httpx|urllib' src/prevue/pricing/ ; test $? -ne 0` (no runtime fetch).
- No pytest test loads the full snapshot (WARNING 2): `! grep -nE 'load_pricing_table\(\s*\)' tests/test_pricing.py`.
- Built-wheel check confirms `src/prevue/pricing/model_prices.json` is packaged (A5).
</verification>

<success_criteria>
- PERF-03 satisfied: real tokens for Claude (stdout-json) + Copilot (OTEL, once env wired in Plan 05); honest labeled estimates for Cursor + Antigravity; per-engine `estimated` flag.
- `prevue.pricing` is a clean package: `__init__.py` (compute) + `model_prices.json` (data), no module/dir collision.
- Cost computed from the vendored pricing table with cache discounts + override precedence; Claude `total_cost_usd` preferred.
- Pytest suite never loads the 500 KB snapshot — small fixture injected via `table=`.
- Claude JSON-output parse path preserved (no all-degraded regression).
- `bash scripts/ci-local.sh` green before handoff.
</success_criteria>

<output>
Create `.planning/phases/10-boundary-contracts/10-03-SUMMARY.md` when done
</output>
