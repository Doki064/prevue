---
phase: 10-boundary-contracts
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_config_precedence.py
  - tests/test_usage_capture.py
  - tests/test_pricing.py
  - tests/test_raw_args.py
  - tests/test_model_roles.py
  - tests/test_output_contract.py
  - tests/fixtures/pricing/sample_prices.json
  - tests/fixtures/usage/claude_envelope.json
  - tests/fixtures/usage/cursor_envelope.json
  - tests/fixtures/usage/copilot_otel.jsonl
  - tests/fixtures/usage/antigravity_text.txt
autonomous: true
requirements: [WKFL-05, PERF-03, ENGN-08, ENGN-09, OUTP-05]
must_haves:
  truths:
    - "Six new test files exist and collect under pytest"
    - "Each new test is RED (xfail/skip-pending or asserting a not-yet-existent import) — never silently green"
    - "Usage + pricing fixtures exist so capture/cost tests do not load the 500 KB production snapshot"
  artifacts:
    - path: "tests/test_config_precedence.py"
      provides: "WKFL-05 input>yml>default precedence matrix (RED)"
      contains: "PREVUE_ENGINE"
    - path: "tests/test_usage_capture.py"
      provides: "PERF-03 per-strategy capture RED tests"
      contains: "usage_capture"
    - path: "tests/test_pricing.py"
      provides: "PERF-03 cost compute + cache discount + override precedence RED tests"
      contains: "compute_cost"
    - path: "tests/test_raw_args.py"
      provides: "ENGN-08 list-form append + base-ref-only RED tests"
      contains: "raw_args"
    - path: "tests/test_model_roles.py"
      provides: "ENGN-09 per-role resolution + deterministic merge RED tests"
      contains: "models"
    - path: "tests/test_output_contract.py"
      provides: "OUTP-05 compact + full + schema_version RED tests"
      contains: "schema_version"
    - path: "tests/fixtures/pricing/sample_prices.json"
      provides: "small pricing fixture for unit tests"
    - path: "tests/fixtures/usage/claude_envelope.json"
      provides: "sample Claude stdout-json usage envelope"
  key_links:
    - from: "tests/test_usage_capture.py"
      to: "tests/fixtures/usage/"
      via: "fixture file load"
      pattern: "fixtures/usage"
    - from: "tests/test_pricing.py"
      to: "tests/fixtures/pricing/sample_prices.json"
      via: "fixture file load"
      pattern: "fixtures/pricing"
---

<objective>
Create the Wave-0 RED test scaffolds and fixtures for every new Phase 10 contract (WKFL-05, PERF-03, ENGN-08, ENGN-09, OUTP-05). These tests pin the exact contract shape before any production code exists, satisfying the Nyquist rule for downstream plans: each later task points at a concrete `<automated>` command that already exists and is RED.

Purpose: Lock the behavioral contracts as executable assertions first (TDD RED). Downstream plans (03/04/05) turn these RED tests GREEN against the spec-driven generic — never against copy-pasted code.
Output: 6 new test files + a pricing fixture + 4 usage fixtures, all collecting and RED.
</objective>

<artifacts_this_phase_produces>
This plan (Wave 0) creates ONLY test scaffolds and fixtures. The production symbols they reference (created by later plans) are:
- `prevue.engines.usage.capture_usage` (Plan 03)
- `prevue.pricing.compute_cost`, `prevue.pricing.load_pricing_table` (Plan 03)
- `prevue.config.EngineConfig` with fields `name`, `model`, `models`, `raw_args`, `pricing`; `_resolve_model`, `_resolve_engine_models` (Plan 04)
- `prevue.review` per-role model resolution + OUTP-05 emit (`emit_machine_output`) (Plans 04/05)
- `schema_version` on the serialized OUTP-05 shape (Plan 05)
New test files: the six listed in frontmatter. New fixture dirs: `tests/fixtures/pricing/`, `tests/fixtures/usage/`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-boundary-contracts/10-RESEARCH.md
@.planning/phases/10-boundary-contracts/10-VALIDATION.md
@.planning/phases/10-boundary-contracts/10-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Usage-capture + pricing fixtures and RED tests (PERF-03)</name>
  <read_first>
    - tests/fixtures/ (existing fixture layout for convention)
    - src/prevue/engines/flow.py (the `_token_meta` dict shape `{"review", "estimated"}` these tests extend — lines 15-34)
    - 10-RESEARCH.md § "Per-Engine Token Reporting Matrix" (field names per engine) and § "Cost Computation" (formula + JSON field names: input_cost_per_token, output_cost_per_token, cache_read_input_token_cost, cache_creation_input_token_cost)
    - 10-PATTERNS.md § "src/prevue/engines/usage.py" and § "src/prevue/pricing.py"
  </read_first>
  <behavior>
    - Claude stdout-json fixture (`claude_envelope.json`) has `usage.{input_tokens,output_tokens,cache_read_input_tokens,cache_creation_input_tokens}` + `total_cost_usd` + a `result` text field; `capture_usage(spec, stdout=<envelope>)` returns input/output/cache counts with `estimated=False`.
    - Cursor fixture (`cursor_envelope.json`) has `type/result/session_id` but NO token fields; capture returns `None` so caller falls back to bytes/4 with `estimated=True`.
    - Copilot OTEL fixture (`copilot_otel.jsonl`) has ≥2 JSONL lines with per-call input/output/cache-read token attributes; capture sums them, `estimated=False`.
    - Antigravity fixture (`antigravity_text.txt`) is plain review text (no JSON); capture returns `None` → estimate fallback.
    - `compute_cost(engine, model, usage, override=None, table=<sample_prices>)` = input*input_cost_per_token + output*output_cost_per_token + cache_read*cache_read_input_token_cost + cache_creation*cache_creation_input_token_cost. Cache discount applied when present. Unknown model → no cost (None), labeled. `override` map (D-06c) takes precedence over the table for matching model. When the engine already returned `total_cost_usd` (Claude), prefer it over recomputation.
  </behavior>
  <action>
    Write `tests/fixtures/pricing/sample_prices.json` — a tiny pricing table (3-4 model keys, one with cache fields, one tiered with `input_cost_per_token_above_200k_tokens`) mirroring the LiteLLM `model_prices_and_context_window.json` field names per 10-RESEARCH § Cost Computation. Write the four usage fixtures under `tests/fixtures/usage/` per the behavior block.
    Write `tests/test_usage_capture.py` with RED tests `test_claude_stdout_json`, `test_fallback_estimated` (cursor + antigravity), `test_copilot_otel` — each imports `from prevue.engines.usage import capture_usage` (does not exist yet → ImportError/RED) and asserts the per-strategy shape. Write `tests/test_pricing.py` with RED tests covering the cache-aware formula, unknown-model→None, `engine.pricing` override precedence (D-06c), and prefer-`total_cost_usd`-when-present — importing `from prevue.pricing import compute_cost, load_pricing_table`.
    Use `pytest.importorskip` or a module-level import that fails so the file collects but tests are RED; do NOT mark them xfail-silent — they must show as errors/failures until Plan 03 lands. NEVER inline production logic here.
  </action>
  <verify>
    <automated>uv run pytest tests/test_usage_capture.py tests/test_pricing.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_usage_capture.py tests/test_pricing.py --collect-only -q` lists the named tests (collection succeeds).
    - Running the tests is RED (ImportError on `prevue.engines.usage` / `prevue.pricing`) — proven by `uv run pytest tests/test_pricing.py -x 2>&1 | grep -qi "No module named 'prevue.pricing'\|ImportError\|ModuleNotFoundError"`.
    - `ls tests/fixtures/pricing/sample_prices.json tests/fixtures/usage/claude_envelope.json tests/fixtures/usage/cursor_envelope.json tests/fixtures/usage/copilot_otel.jsonl tests/fixtures/usage/antigravity_text.txt` all exist.
  </acceptance_criteria>
  <done>Five fixtures + two RED test files exist; both collect; both fail on missing production modules.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Config precedence + raw_args + model-roles RED tests (WKFL-05 / ENGN-08 / ENGN-09)</name>
  <read_first>
    - src/prevue/config.py (`_resolve_engine` lines 141-151; `FallbackConfig`/`SkipConfig` `ConfigDict(extra="forbid")` pattern lines 23-49; `resolve_consumer_config_path` base-ref-only sentinel lines 80-126, `NO_CONSUMER_CONFIG_SENTINEL` line 20; `load_config` lines 154-195)
    - tests/test_config.py (existing config test conventions, monkeypatch of env + tmp prevue.yml)
    - 10-RESEARCH.md § "Config Override-Knob Inventory" (3 override knobs: engine, model, fallback model) and § "ENGN-09 Config Shape" (the `engine.models {classify, review, consolidate}` block + single `model:` fallback; resolution `models.<role>` else `model` else default)
    - 10-PATTERNS.md § "src/prevue/config.py" and § "src/prevue/multicall.py + src/prevue/review.py"
  </read_first>
  <behavior>
    - WKFL-05 (D-07): for `engine`, `PREVUE_ENGINE` env > `engine.name` in yml > `DEFAULT_ENGINE`. For `model`, `PREVUE_MODEL` (then `COPILOT_MODEL`) env > `engine.model` in yml > None. For fallback model, env (if any) > `classification.fallback.model` > None. A precedence matrix test asserts each of the 3 knobs at each layer.
    - ENGN-08 (D-10): `EngineConfig.raw_args` is a `list[str]`; a non-list (shell string) is rejected by the pydantic validator (`extra="forbid"` + type). raw_args read only from the base-ref path — when only a PR-head prevue.yml differs (Actions, no `PREVUE_CONSUMER_ROOT`), `resolve_consumer_config_path` returns the sentinel and raw_args is empty (Pitfall 4).
    - ENGN-09 (D-11): `EngineConfig.models` resolves per role — `_resolve_engine_models(config)` yields `{classify, review, consolidate}` where each unset role falls back to `engine.model` then engine default. The `consolidate` slot resolves (D-13) but no test asserts it is consumed.
  </behavior>
  <action>
    Write `tests/test_config_precedence.py` — a parametrized matrix asserting input>yml>default for engine, model, and fallback model, mirroring the `_resolve_engine` ladder. Reference `prevue.config._resolve_engine` (exists) plus `_resolve_model` and `_resolve_engine_models` (do NOT exist yet → RED).
    Write `tests/test_raw_args.py` — assert `EngineConfig` validates a `raw_args: list[str]`, rejects a string, and that a PR-head-only prevue.yml under simulated Actions (set `GITHUB_ACTIONS=1`, unset `PREVUE_CONSUMER_ROOT`) yields no raw_args because `resolve_consumer_config_path` returns `NO_CONSUMER_CONFIG_SENTINEL`. Import `from prevue.config import EngineConfig` (RED).
    Write `tests/test_model_roles.py` — assert `_resolve_engine_models` returns the per-role mapping with `models.<role>` precedence over `model` over default, and that the deterministic `merge_findings` (import from `prevue.multicall`) is unchanged (a quick assertion that two identical-fingerprint findings merge with higher severity winning — Pitfall: do NOT touch merge). Import the not-yet-existent resolver (RED).
    Keep all three files collecting; RED until Plan 04. No production logic inlined.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_precedence.py tests/test_raw_args.py tests/test_model_roles.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Collection succeeds for all three files.
    - RED proven: `uv run pytest tests/test_raw_args.py -x 2>&1 | grep -qi "EngineConfig\|ImportError\|cannot import"`.
    - `grep -q "NO_CONSUMER_CONFIG_SENTINEL\|GITHUB_ACTIONS" tests/test_raw_args.py` (base-ref-only Pitfall 4 covered).
    - The model-roles file asserts merge determinism is preserved: `grep -q "merge_findings" tests/test_model_roles.py`.
  </acceptance_criteria>
  <done>Three RED test files exist, collect, and fail on the missing `EngineConfig`/`_resolve_model`/`_resolve_engine_models` symbols.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Output contract RED test (OUTP-05)</name>
  <read_first>
    - src/prevue/models.py (`ReviewResult` lines 45-50 — `model_dump(mode="json")` is the serialization path)
    - src/prevue/review.py (end of `run_review` lines 1244-1253 — where gate.conclusion + severity counts + tokens are available for emit)
    - 10-RESEARCH.md § "Pattern 4: Versioned both-form output" (compact keys: schema_version, conclusion, error_count, warning_count, info_count, tokens, cost_usd; full = schema_version + ReviewResult.model_dump) and Pitfall 6 (1 MB job-output limit → full goes to artifact)
    - 10-PATTERNS.md § "src/prevue/models.py" and § ".github/workflows/prevue-review.yml"
  </read_first>
  <behavior>
    - A compact-output builder produces a dict with `schema_version="1.0"`, `conclusion`, `error_count`, `warning_count`, `info_count`, `tokens`, `cost_usd`.
    - A full-output builder produces `{"schema_version": "1.0", **ReviewResult.model_dump(mode="json")}` — valid JSON, parseable, includes findings.
    - Writing the compact form to `$GITHUB_OUTPUT` escapes newlines / uses a safe key=value form (no raw multiline values).
  </behavior>
  <action>
    Write `tests/test_output_contract.py` — RED tests: `test_compact_has_schema_version_and_counts`, `test_full_is_parseable_json_with_findings`, `test_github_output_lines_are_safe`. Import the not-yet-existent emit helper(s) from `prevue.review` (e.g. `from prevue.review import build_compact_output, build_full_output` — RED) and a `GateResult`/severity-counts fixture mirroring `prevue.gate`. Assert `schema_version == "1.0"` on both forms, that `json.loads(build_full_output(...))` round-trips, and that compact `$GITHUB_OUTPUT` rendering contains no embedded newline in a single value (use a heredoc-style or escaped form). RED until Plan 05.
  </action>
  <verify>
    <automated>uv run pytest tests/test_output_contract.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - Collection succeeds.
    - RED proven: `uv run pytest tests/test_output_contract.py -x 2>&1 | grep -qi "build_compact_output\|build_full_output\|ImportError\|cannot import"`.
    - `grep -q "schema_version" tests/test_output_contract.py` and `grep -q "1.0" tests/test_output_contract.py`.
  </acceptance_criteria>
  <done>OUTP-05 RED test file exists, collects, and fails on the missing emit helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → unit tests | Fixtures are repo-controlled data; no untrusted input. No runtime boundary crossed by scaffolds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | usage/pricing fixtures used by later tests | accept | Fixtures are committed test data, reviewed in PR; no runtime trust impact. The real trust controls (base-ref-only reads) are tested in Plan 04, not here. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (test files + JSON fixtures only); per 10-RESEARCH Package Legitimacy Audit the phase installs zero new packages. |
</threat_model>

<verification>
- `uv run pytest --collect-only -q` succeeds for the whole suite (no collection errors introduced).
- Each new test file is RED when run (missing production module), never silently passing.
- Fixtures exist and are valid JSON / JSONL / text.
</verification>

<success_criteria>
- Six new test files collect and are RED.
- Pricing + usage fixtures exist (small, not the 500 KB production snapshot).
- No production source files modified (scaffolds only).
- `bash scripts/ci-local.sh` is NOT expected green yet (RED tests are intentional) — Wave 0 gate is "collects + RED", full-green is reached as later plans land.
</success_criteria>

<output>
Create `.planning/phases/10-boundary-contracts/10-01-SUMMARY.md` when done
</output>
