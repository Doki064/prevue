---
phase: 07-customization-hardening
plan: 07
type: execute
wave: 5
depends_on: []
files_modified:
  - docs/examples/prevue.yml
  - docs/consumer-setup.md
autonomous: true
gap_closure: true
requirements: [WKFL-03]
user_setup: []
must_haves:
  truths:
    - "Consumer can copy a single starter .github/prevue.yml from repo docs without assembling fragments"
    - "consumer-setup.md links the starter to configuration.md for knob details"
  artifacts:
    - path: "docs/examples/prevue.yml"
      provides: "Copy-paste starter config with review, skills, classification, engine, skip sections"
    - path: "docs/consumer-setup.md"
      provides: "Starter config section with fenced block or link to docs/examples/prevue.yml"
  key_links:
    - from: "docs/consumer-setup.md"
      to: "docs/examples/prevue.yml"
      via: "link or inline copy-paste block pointing at examples/prevue.yml"
      pattern: "prevue\\.yml|examples/prevue"
    - from: "docs/consumer-setup.md"
      to: "docs/configuration.md"
      via: "See Configuration for details"
      pattern: "configuration\\.md"
---

<objective>
UAT Gap 2 closure: ship a copy-paste starter `.github/prevue.yml` so consumers adopting skills.exclude and other knobs do not have to assemble config from section fragments.

Purpose: Test 3 passed functionally but user created config manually; docs lacked a single starter file. Minimal docs-only fix.

Output: docs/examples/prevue.yml starter + consumer-setup.md section with copy-paste guidance.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-customization-hardening/07-UAT.md
@docs/consumer-setup.md
@docs/configuration.md
@src/prevue/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add docs/examples/prevue.yml starter template</name>
  <files>docs/examples/prevue.yml</files>
  <action>
    Create docs/examples/prevue.yml as a valid, commented starter consumers copy to `.github/prevue.yml` on their base branch. Include all sections from configuration.md with documented defaults: review (max_input_tokens, output_reserve_tokens, min_severity_to_comment, min_severity_to_fail, max_inline_comments), skills (exclude with example entry like security/committed-secrets.md commented out, max_skill_bytes, max_total_consumer_bytes, max_consumer_skills), classification.fallback (enabled, model), engine.name, and skip.review_bots (empty list default with one commented example bot login). Top-of-file comment: copy to `.github/prevue.yml` on default branch; read from base ref at review time. Values must match PrevueConfig defaults in config.py — no invented keys (extra=forbid). Keep YAML minimal but complete enough to enable skills.exclude without reading multiple doc pages.
  </action>
  <verify>
    <automated>test -f docs/examples/prevue.yml && grep -q 'skills:' docs/examples/prevue.yml && grep -q 'exclude:' docs/examples/prevue.yml && grep -q 'review:' docs/examples/prevue.yml && python -c "import yaml; yaml.safe_load(open('docs/examples/prevue.yml'))"</automated>
  </verify>
  <done>docs/examples/prevue.yml exists, parses as YAML, includes review/skills/classification/engine/skip with skills.exclude example.</done>
</task>

<task type="auto">
  <name>Task 2: Wire starter into consumer-setup.md</name>
  <files>docs/consumer-setup.md</files>
  <action>
    Add a "## Starter config (prevue.yml)" section to docs/consumer-setup.md after the minimal caller snippet (before Required permissions). State that config lives at `.github/prevue.yml` on the base branch (reuse existing config-path note). Provide either (a) a fenced copy of docs/examples/prevue.yml contents OR (b) a short block plus link: "Full starter: [docs/examples/prevue.yml](./examples/prevue.yml)". Link to [configuration.md](./configuration.md) for knob semantics and [skills.md](./skills.md) for custom skills. Mention skills.exclude takes bundle/filename keys (e.g. security/committed-secrets.md). Do not duplicate the entire configuration.md — starter + links only. Match existing doc tone and structure.
  </action>
  <verify>
    <automated>grep -q 'Starter config' docs/consumer-setup.md && grep -q 'examples/prevue.yml' docs/consumer-setup.md && grep -q 'configuration.md' docs/consumer-setup.md && grep -q 'skills.exclude\|exclude:' docs/consumer-setup.md</automated>
  </verify>
  <done>consumer-setup.md has starter section linking examples/prevue.yml and configuration.md; skills.exclude documented for copy-paste adoption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → consumer repo | Example config is trusted documentation; loaded from base ref per SKIL-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-gap2-01 | Information Disclosure | example YAML | accept | No secrets in template; placeholder comments only |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `test -f docs/examples/prevue.yml` and YAML parses
- consumer-setup.md contains Starter config section with links
- `ruff check` N/A (docs only)
</verification>

<success_criteria>
- UAT test 3 doc enhancement closed: consumer can adopt prevue.yml from documented starter without manual assembly
- Example file keys align with PrevueConfig schema
</success_criteria>

<output>
Create `.planning/phases/07-customization-hardening/07-07-SUMMARY.md` when done.
</output>
