---
phase: 07-customization-hardening
plan: 06
type: tdd
wave: 5
depends_on: ["07-04", "07-03"]
files_modified:
  - src/prevue/skills/models.py
  - src/prevue/skills/loader.py
  - src/prevue/review.py
  - src/prevue/github/comments.py
  - tests/test_skills_merge.py
  - tests/test_comments.py
  - tests/test_review_flow.py
autonomous: true
gap_closure: true
requirements: [SKIL-03, OUTP-04]
user_setup: []
must_haves:
  truths:
    - "Sticky Metadata Engine line reflects the adapter actually used (e.g. cursor-cli), not a hardcoded copilot-cli"
    - "Consumer-loaded skills show a consumer source tag in the sticky Skills line so overrides are auditable"
    - "Built-in skills render without a consumer tag (name + bundle only)"
  artifacts:
    - path: "src/prevue/skills/models.py"
      provides: "Skill.source field (builtin | consumer)"
      contains: "source"
    - path: "src/prevue/skills/loader.py"
      provides: "source set from is_consumer during merge"
    - path: "src/prevue/review.py"
      provides: "engine.name written to result.engine_meta; loaded_skills formatted with source"
    - path: "src/prevue/github/comments.py"
      provides: "Engine line reads engine_meta.engine"
  key_links:
    - from: "src/prevue/review.py"
      to: "result.engine_meta"
      via: "result.engine_meta['engine'] = engine.name after engine.review(req)"
      pattern: "engine_meta.*engine"
    - from: "src/prevue/github/comments.py"
      to: "result.engine_meta"
      via: "render_body metadata uses engine_meta.get('engine')"
      pattern: "engine_meta.get\\(.engine"
    - from: "src/prevue/review.py"
      to: "Skill.source"
      via: "loaded_skills list includes consumer tag when source=='consumer'"
      pattern: "consumer"
---

<objective>
UAT Gap 1 closure: sticky Metadata must truthfully report the active engine and distinguish consumer vs built-in loaded skills so consumers can verify overrides from the review comment alone.

Purpose: Test 1 failed because render_body hardcoded `Engine: copilot-cli` and loaded_skills omitted skill provenance. Closes the major UAT gap without changing merge or selection behavior.

Output: Skill.source on model + loader, engine_meta.engine populated in run_review, render_body reads engine from meta, Skills line tags consumer skills.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-customization-hardening/07-UAT.md
@.planning/phases/07-customization-hardening/07-04-SUMMARY.md
@src/prevue/skills/models.py
@src/prevue/skills/loader.py
@src/prevue/review.py
@src/prevue/github/comments.py
@tests/test_skills_merge.py
@tests/test_comments.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Skill.source field + loader provenance + merge tests</name>
  <read_first>
    - src/prevue/skills/models.py (Skill fields: name, bundle, filename, body)
    - src/prevue/skills/loader.py (_load_from_tree is_consumer flag; by_key merge at load_skills)
    - tests/test_skills_merge.py (test_override_replaces_builtin, test_custom_adds_alongside)
  </read_first>
  <behavior>
    - Skill model has source: Literal["builtin", "consumer"] defaulting to "builtin"
    - Skills loaded from builtin root have source=="builtin"
    - Skills loaded from consumer root (including overrides at same bundle/filename) have source=="consumer"
    - test_override_replaces_builtin asserts override.source == "consumer" and body still contains CONSUMER OVERRIDE
    - test_custom_adds_alongside asserts consumer-only skill has source=="consumer" and builtin skills have source=="builtin"
  </behavior>
  <action>
    Add source field to Skill in models.py as Literal["builtin", "consumer"] with default "builtin". In loader.py _load_from_tree, after Skill.model_validate and bundle/filename/body assignment, set skill.source = "consumer" if is_consumer else "builtin" on each Skill instance. Extend test_skills_merge.py: test_override_replaces_builtin must assert override.source == "consumer"; test_custom_adds_alongside must assert the consumer-only file has source consumer and at least one remaining builtin skill has source builtin. Do not change merge keying, exclude, or cap logic.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_skills_merge.py -x -q</automated>
  </verify>
  <done>Skill.source persisted through loader merge; override and custom skills tagged consumer; builtin skills tagged builtin; merge tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: engine_meta.engine + sticky render + integration tests</name>
  <read_first>
    - src/prevue/review.py (engine = adapter or get_adapter; result = engine.review; loaded_skills at line 259)
    - src/prevue/github/comments.py (render_body metadata line at 129 hardcodes copilot-cli)
    - src/prevue/engines/base.py (EngineAdapter.name)
    - tests/test_comments.py (test_render_body_loaded_skills pattern)
    - tests/test_review_flow.py (mock upsert_sticky kwargs assertions)
  </read_first>
  <behavior>
    - After engine.review(req) in run_review, result.engine_meta["engine"] equals engine.name (e.g. cursor-cli)
    - render_body metadata uses result.engine_meta.get("engine", "unknown") — never hardcodes copilot-cli
    - loaded_skills passed to upsert_sticky: builtin skills as "Name (bundle)"; consumer skills as "Name (bundle, consumer)"
    - test_render_body_engine_from_meta: body with engine_meta engine=cursor-cli contains "Engine: cursor-cli"
    - test_render_body_loaded_skills_consumer_tag: loaded_skills entry with ", consumer)" renders in Skills line
  </behavior>
  <action>
    In review.py immediately after result = engine.review(req), set result.engine_meta["engine"] = engine.name. Replace the hardcoded metadata engine string in comments.py render_body (line 129) with result.engine_meta.get("engine", "unknown"). Update loaded_skills construction in review.py: for each matched skill, format f"{s.name} ({s.bundle})" when s.source == "builtin" else f"{s.name} ({s.bundle}, consumer)". Add test_render_body_engine_from_meta in test_comments.py asserting Engine: cursor-cli when engine_meta includes engine. Update test_render_body_loaded_skills to keep builtin format; add test_render_body_skill_consumer_source with loaded_skills=["Custom Rule (security, consumer)"]. If test_review_flow asserts loaded_skills strings, update expectations to match new format where consumer skills appear. Run full comments + review_flow subset.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_comments.py tests/test_review_flow.py tests/test_skills_merge.py -x -q</automated>
  </verify>
  <done>Sticky shows correct engine name; consumer skills tagged in Skills line; no hardcoded copilot-cli in render_body; related tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sticky Metadata → consumer reader | Display-only provenance strings; no untrusted input interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-gap1-01 | Tampering | engine_meta.engine | mitigate | Value sourced from adapter.name set in orchestration, not from PR diff |
| T-07-gap1-02 | Spoofing | Skill.source tag | accept | Tag reflects loader merge truth (consumer root overwrite); not security boundary — audit aid only |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_skills_merge.py tests/test_comments.py tests/test_review_flow.py -x -q` green
- `grep -n 'copilot-cli' src/prevue/github/comments.py` shows no hardcoded Engine line (only comments if any)
- `ruff check src/prevue/skills src/prevue/github/comments.py src/prevue/review.py` clean
</verification>

<success_criteria>
- UAT test 1 truth achievable: consumer override visible in sticky Skills line with consumer tag
- Engine line matches configured adapter (cursor-cli, claude-code-cli, etc.)
- Prior phase 7 test suite unbroken
</success_criteria>

<output>
Create `.planning/phases/07-customization-hardening/07-06-SUMMARY.md` when done.
</output>
