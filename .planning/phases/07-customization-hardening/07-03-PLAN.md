---
phase: 07-customization-hardening
plan: 03
type: tdd
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/prevue/engines/flow.py
  - src/prevue/github/comments.py
  - src/prevue/review.py
  - tests/test_comments.py
  - tests/test_review_flow.py
autonomous: true
requirements: [OUTP-04]
user_setup: []
must_haves:
  truths:
    - "Every review's sticky summary shows tokens used, split into review tokens and (when LLM fallback fired) classify tokens, marked ~est"
    - "The summary shows a per-bundle skills-loaded-vs-skipped ratio in the exact compact shape (e.g. 'Skills: 3/13 loaded — security 2/3 · frontend 1/4 · ...')"
    - "Transparency is computed on the PACKED (reviewed) set; the summary states classification/skills reflect only reviewed files and that N files were not reviewed"
  artifacts:
    - path: "src/prevue/engines/flow.py"
      provides: "engine_meta['tokens'] = {review, estimated} on all result paths"
      contains: "tokens"
    - path: "src/prevue/github/comments.py"
      provides: "token line + per-bundle ratio line + coverage statement in render_body"
      contains: "loaded"
    - path: "src/prevue/review.py"
      provides: "packed-set skill selection + ratio denominators + classify-token sum threaded to sticky"
  key_links:
    - from: "src/prevue/engines/flow.py"
      to: "src/prevue/engines/tokens.py"
      via: "estimate_tokens(prompt) + estimate_tokens(stdout)"
      pattern: "estimate_tokens"
    - from: "src/prevue/review.py"
      to: "src/prevue/github/comments.py"
      via: "render_body(skill_ratios=..., token_meta=..., reviewed/skipped counts)"
      pattern: "skill_ratios|token_meta"
    - from: "src/prevue/review.py"
      to: "src/prevue/skills/loader.py"
      via: "select_skills(skills, [f.path for f in packed.files])"
      pattern: "packed.files"
---

<objective>
OUTP-04 vertical slice: every review proves the token-efficiency thesis. The sticky summary reports tokens used (review + classify, hybrid actual-else-estimate, marked ~est) and a per-bundle skills-loaded-vs-skipped ratio — all computed on the PACKED set produced by DIFF-03, with an explicit statement that the numbers reflect only reviewed files.

Purpose: The transparency line is the user-visible proof of Prevue's core value. It MUST read the packed set (Plan 02) so a partial review never over-reports coverage (D-16). Depends on packing being wired first.

Output: token counts in engine_meta, token + per-bundle-ratio lines in the sticky, packed-set skill selection with correct denominators.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-customization-hardening/07-CONTEXT.md
@.planning/phases/07-customization-hardening/07-RESEARCH.md
@.planning/phases/07-customization-hardening/07-PATTERNS.md
@.planning/phases/07-customization-hardening/07-VALIDATION.md
@src/prevue/engines/flow.py
@src/prevue/github/comments.py
@src/prevue/review.py
@src/prevue/skills/loader.py
@src/prevue/classify/models.py
@src/prevue/classify/llm_fallback.py
</context>

## Phase Goal

**As a** consumer team adopting Prevue, **I want to** see transparent token budgets and which skills loaded versus skipped on every review, **so that** I can trust the token-efficiency claim and audit what context the review actually used.

## Acceptance Criteria (this plan, OUTP-04)
- [ ] estimator bytes/4 backs the token line (test_estimate_tokens already green from Plan 02)
- [ ] token line shows review + classify, marked ~est (D-13/14)
- [ ] per-bundle ratio line matches the exact compact shape (D-15)
- [ ] ratios + coverage computed on the PACKED set; coverage statement names N not-reviewed files (D-16)
- [ ] Plan 01 RED tests test_token_line_estimated, test_per_bundle_ratio_line turn GREEN

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: token counts in engine_meta (RED then GREEN)</name>
  <read_first>
    - src/prevue/engines/flow.py (engine_meta built at lines 29-34, 86-92, 100-104; prompt at 47, stdout at 57)
    - src/prevue/engines/tokens.py (estimate_tokens from Plan 02)
    - src/prevue/models.py (ReviewResult.engine_meta dict)
    - .planning/phases/07-customization-hardening/07-PATTERNS.md (section flow.py — token meta on all three result paths)
  </read_first>
  <behavior>
    - On a successful review, engine_meta["tokens"] == {"review": estimate_tokens(prompt) + estimate_tokens(stdout), "estimated": True} (D-13)
    - The degraded-result and parse-degrade paths ALSO carry engine_meta["tokens"] with estimated=True (consistency across all three constructions)
    - estimated is always True in v1 (no engine emits real usage — research State of the Art); the render layer marks it ~est
  </behavior>
  <action>
    In flow.py, compute the review token estimate where both prompt (flow.py:47) and stdout (flow.py:57) are in scope and add a "tokens" key {"review": estimate_tokens(prompt) + estimate_tokens(stdout), "estimated": True} to ALL THREE engine_meta constructions (success at flow.py:100-104, the inline degraded ReviewResult at flow.py:86-92, and _degraded_result at flow.py:29-34) — per D-13/14. For the _degraded_result path, thread prompt+stdout (or the bytes already known) so the estimate is non-zero where possible; if stdout is unavailable in a branch, estimate review from the prompt alone. Do NOT add a classify token key here — classify tokens are summed in review.py (Task 3). Extend tests/test_review_flow.py or test the flow directly to assert tokens present on success + degrade paths.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review_flow.py -k "token or degrad" -x -q</automated>
  </verify>
  <done>engine_meta carries tokens.review + estimated=True on success, inline-degrade, and _degraded_result paths.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: token line + per-bundle ratio + coverage statement in render_body (RED then GREEN)</name>
  <read_first>
    - src/prevue/github/comments.py (render_body lines 98-163; Metadata block 107-152; upsert_sticky kwargs 214-231)
    - src/prevue/classify/models.py (canonical_index, CANONICAL_LABEL_ORDER for ordering)
    - tests/test_comments.py (test_token_line_estimated, test_per_bundle_ratio_line targets)
    - .planning/phases/07-customization-hardening/07-PATTERNS.md (section comments.py — exact ratio shape D-15; Pitfall 2 denominators; Pitfall 3 coverage from packed)
  </read_first>
  <behavior>
    - Token line: reads result.engine_meta["tokens"]; renders "Tokens: review ~est N" and, when a classify token count is passed and non-zero, " · classify ~est M" (D-13/14). When estimated is True the ~est marker is present.
    - Per-bundle ratio line EXACT shape (D-15): "Skills: 3/13 loaded — security 2/3 · frontend 1/4 · backend 0/3 · data 0/2 · infra 0/1", ordered by canonical_index, non-canonical bundles last, zero-loaded bundles still shown.
    - Numerator = count of MATCHED skills per bundle; denominator = count of ALL merged available skills per bundle (Pitfall 2 — denominator from the merged load_skills output, not the matched list).
    - Coverage statement (D-16): when files were skipped by packing, the metadata explicitly states classification/skills reflect only the reviewed files and that N files were not reviewed (couples to the DIFF-03 disclosure already rendered in Plan 02).
  </behavior>
  <action>
    Add skill_ratios (dict bundle -> (loaded, total)), token_meta (the engine_meta tokens dict plus an optional classify token int), and reviewed/not-reviewed counts as kwargs to render_body and upsert_sticky in comments.py. Render the Token line from token_meta marking ~est when estimated (D-13/14). Render the per-bundle ratio line in the EXACT D-15 shape, iterating bundles in canonical_index order with non-canonical bundles appended last, showing every available bundle including 0-loaded ones; the headline numerator/denominator is the sum across bundles. Keep the existing "Skills: <names>" line (loaded skill names stay listed per D-15). Add the coverage statement (D-16) when not-reviewed count > 0. Reuse the existing Metadata-append style (string concatenation onto metadata). Add tests/test_comments.py::test_token_line_estimated and ::test_per_bundle_ratio_line.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_comments.py::test_token_line_estimated tests/test_comments.py::test_per_bundle_ratio_line -x -q</automated>
  </verify>
  <done>Token line + exact-shape per-bundle ratio + coverage statement render; denominators from merged set; named RED tests green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: wire packed-set selection + classify-token sum + ratios into run_review (RED then GREEN)</name>
  <read_first>
    - src/prevue/review.py (classify 96; llm_classify 98-103; load_skills 130; select_skills 131 — switch to packed.files; upsert_sticky 176-183)
    - src/prevue/classify/llm_fallback.py (llm_classify signature; CLASSIFY_BATCH_SIZE batching — keep batched, scope to packed per D-19, D-22)
    - src/prevue/skills/loader.py (load_skills, select_skills; Counter denominators per Pitfall 2)
    - .planning/phases/07-customization-hardening/07-RESEARCH.md (Pitfall 1 classify/pack sequencing; Pitfall 3 packed-set substitution; D-19, D-22 scoping)
    - tests/test_review_flow.py (test_fallback_only_on_packed target)
  </read_first>
  <behavior>
    - select_skills runs on the PACKED set: select_skills(skills, [f.path for f in packed.files]) (Pitfall 3, D-19) — NOT reduced.files
    - paid llm_classify is scoped to unmatched ∩ packed paths only (D-19, D-22): files dropped by the budget do NOT incur a classify() call; the call stays batched (CLASSIFY_BATCH_SIZE) — assert no llm_classify on a skipped path
    - classify tokens are estimated (estimate_tokens of the classify prompt(s), summed across batches) and passed to render_body as token_meta classify count (D-14)
    - per-bundle ratios: numerator = Counter(s.bundle for s in matched); denominator = Counter(s.bundle for s in all merged skills); threaded to upsert_sticky as skill_ratios (Pitfall 2)
    - reviewed count = len(packed.files); not-reviewed count = len(skipped) threaded for the coverage statement (D-16)
  </behavior>
  <action>
    In review.py, after packing (Plan 02), change select_skills to operate on packed.files (Pitfall 3 / D-19). Scope the paid llm_classify input to the intersection of result_cls.unmatched with the packed paths so dropped files never trigger a classify call (D-19, D-22) — keep the existing batched llm_classify call, just narrow its input list. Estimate classify tokens by summing estimate_tokens over the classify prompt(s) actually sent (use build_classify_prompt for the batched paths, or sum the per-batch prompt sizes) and pass that as the classify count in token_meta. Compute skill_ratios from Counter(s.bundle for s in matched) over Counter(s.bundle for s in all_merged_skills) (Pitfall 2). Thread skill_ratios, token_meta (result.engine_meta["tokens"] + classify count), reviewed=len(packed.files), not_reviewed=len(skipped) into upsert_sticky. Add tests/test_review_flow.py::test_fallback_only_on_packed asserting llm_classify is not invoked for a budget-skipped unmatched path.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review_flow.py::test_fallback_only_on_packed -x -q</automated>
  </verify>
  <done>Skill selection + ratios + classify scope all read the packed set; classify tokens summed and displayed; dropped files never classified; named RED test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packed set → transparency line | Coverage/ratio numbers must describe ONLY reviewed files — never the full changed set |
| classify fallback → packed scope | Paid LLM classify must not run on files excluded from review (cost + over-reporting) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure (false coverage) | per-bundle ratio + coverage line | mitigate | Ratios computed on the PACKED set with an explicit "reflects only reviewed files; N not reviewed" statement (D-16) — a partial review cannot over-report coverage |
| T-07-07 | Denial of Service (cost) | paid llm_classify scope | mitigate | Classify fallback scoped to unmatched ∩ packed only (D-19, D-22); stays batched at CLASSIFY_BATCH_SIZE — dropped files incur zero classify tokens |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new package installs (07-RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_comments.py -k "token or ratio" -x -q` green
- `.venv/bin/python -m pytest tests/test_review_flow.py -k "token or fallback_only" -x -q` green
- `.venv/bin/python -m pytest -q` full suite green
- `ruff check src/prevue tests` clean
- Manual trace: an 11-skill repo with a backend-only packed set shows "Skills: 1/11 loaded — security 0/3 · backend 1/2 · ..." and "Tokens: review ~est N"
</verification>

<success_criteria>
- token line (review + classify, ~est) renders from engine_meta
- exact-shape per-bundle ratio with merged-set denominators
- ratios/coverage/classify-scope all read the packed set (D-16/19)
- All OUTP-04 RED tests from Plan 01 green; prior suite unbroken
</success_criteria>

<output>
Create `.planning/phases/07-customization-hardening/07-03-SUMMARY.md` when done.
</output>
