---
phase: 01-walking-skeleton-review-loop
plan: 03
type: tdd
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/prevue/models.py
  - tests/test_models.py
autonomous: true
requirements: [ENGN-01]

must_haves:
  truths:
    - "`ReviewRequest` and `ReviewResult` validate and round-trip JSON"
    - "`ReviewResult.findings` defaults to an empty list (D-02)"
    - "`DiffBundle` carries NO PR title/body field (D-07 injection posture)"
  artifacts:
    - path: "src/prevue/models.py"
      provides: "pydantic v2 contract: ChangedFile, DiffBundle, ReviewRequest, Finding, ReviewResult"
      contains: "class ReviewResult(BaseModel)"
    - path: "tests/test_models.py"
      provides: "contract tests: validation, findings=[] invariant, JSON round-trip, no title/body field"
  key_links:
    - from: "src/prevue/models.py"
      to: "all downstream components"
      via: "shared pydantic contract import (`from prevue.models import ...`)"
      pattern: "from prevue.models import"
---

## Phase Goal

**As a** developer who opens a pull request, **I want to** automatically receive an AI-generated review summary comment, **so that** I get feedback without anyone running a review manually.

## Acceptance Criteria (this slice)
- [ ] The locked `EngineAdapter` data contract (ENGN-01 final shape, D-02) exists and is the single source of truth all later components import.
- [ ] `findings` is present but defaults `[]` (Phase 4 fills it without reshaping the models).
- [ ] No PR title/body field is surfaced anywhere in the diff/request models (D-07).

<objective>
Establish the pydantic v2 adapter contract — the data shape of the engine seam — to its final ENGN-01 form now (D-02), so Phase 4 can fill `findings` without breaking the API. This is the foundation every other component imports.

Purpose: Lock the cross-component contract first (interface-first ordering). Everything downstream (diff fetch, adapter, comments, orchestration) depends on these types.
Output: `src/prevue/models.py` with `ChangedFile`, `DiffBundle`, `ReviewRequest`, `Finding`, `ReviewResult`; full contract tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-review-loop/01-PATTERNS.md
</context>

## Artifacts this phase produces

This plan creates `src/prevue/models.py` (`ChangedFile`, `DiffBundle`, `ReviewRequest`, `Finding`, `ReviewResult`) and `tests/test_models.py`. Full phase inventory in `01-SKELETON.md`.

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — contract tests for the adapter models</name>
  <files>tests/test_models.py</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§"Pattern 1" code, lines 205–242 — the canonical model definitions; §"Validation Architecture → TDD Units" lines 531–539)
    - .planning/phases/01-walking-skeleton-review-loop/01-PATTERNS.md (P1 pydantic v2 contract, lines 36–41)
  </read_first>
  <behavior>
    - `ReviewResult(summary_markdown="x")` validates with `findings == []` and `engine_meta == {}` defaults (D-02).
    - `ReviewRequest` requires a `DiffBundle` + `instructions`; `budget_seconds` defaults 300 (D-10); `model` defaults None (D-03).
    - `ChangedFile` accepts `patch=None` (large/binary files); `DiffBundle` holds `pr_number`, `base_sha`, `head_sha`, `files`.
    - `DiffBundle` and `ReviewRequest` have NO `title`/`body`/`pr_title`/`pr_body` field (D-07) — assert via `model_fields`.
    - A populated `ReviewResult` (with one `Finding`) survives a `model_dump_json()` → `model_validate_json()` round-trip.
  </behavior>
  <action>Write `tests/test_models.py` covering the behaviors above. Assert `ReviewResult().findings == []`, default `budget_seconds == 300`, `model is None`, the JSON round-trip equality, and `"title" not in DiffBundle.model_fields and "body" not in DiffBundle.model_fields` (and same for `ReviewRequest`). Run the suite and confirm it FAILS because `prevue.models` does not yet exist (RED).</action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_models.py` exists with the five behavior groups above
    - The suite fails on import of `prevue.models` (RED state confirmed)
  </acceptance_criteria>
  <done>Failing contract tests describe the locked model shape before any implementation.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement the pydantic v2 contract</name>
  <files>src/prevue/models.py</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§"Pattern 1" code, lines 205–242)
    - tests/test_models.py (from Task 1)
  </read_first>
  <action>Create `src/prevue/models.py` using pydantic v2 (`BaseModel`, `Field(default_factory=...)`, `str | None` unions — never v1 validators). Define `ChangedFile` (`path`, `status`, `additions`, `deletions`, `patch: str | None`), `DiffBundle` (`pr_number`, `base_sha`, `head_sha`, `files: list[ChangedFile]` — deliberately no title/body, D-07), `ReviewRequest` (`diff: DiffBundle`, `instructions: str`, `budget_seconds: int = 300`, `model: str | None = None`), `Finding` (`path`, `line`, `side="RIGHT"`, `severity`, `title`, `body`, `suggestion: str | None = None` — present now, empty in v1 per D-02), and `ReviewResult` (`summary_markdown: str`, `findings: list[Finding] = Field(default_factory=list)`, `engine_meta: dict = Field(default_factory=dict)`). Run tests to GREEN.</action>
  <verify>
    <automated>uv run pytest tests/test_models.py -x && uv run ruff check src/prevue/models.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_models.py -x` exits 0 (GREEN)
    - `src/prevue/models.py` contains `class ReviewResult(BaseModel)` and `class DiffBundle(BaseModel)`
    - `ruff check` clean on the file
  </acceptance_criteria>
  <done>The locked adapter contract is implemented and all contract tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter contract surface | the only typed data shape crossing between fetch → engine → post |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Tampering / Elevation | prompt-injection surface | mitigate | `DiffBundle`/`ReviewRequest` carry NO PR title/body field — attacker-writable text structurally cannot reach the engine (D-07); enforced by `model_fields` assertion test |
| T-05 | Input Validation | malformed engine/fetch I/O | mitigate | pydantic v2 boundary validation on every model; `patch: str | None` tolerated explicitly |
</threat_model>

<verification>
- `uv run pytest tests/test_models.py -x` GREEN.
- `grep -q "class ReviewResult(BaseModel)" src/prevue/models.py`.
- No `title`/`body` field on `DiffBundle`/`ReviewRequest` (asserted in tests).
</verification>

<success_criteria>
- ENGN-01 contract exists in final shape; `findings` defaults `[]`; JSON round-trips; no title/body leak path.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-review-loop/01-03-SUMMARY.md` when done.
</output>
