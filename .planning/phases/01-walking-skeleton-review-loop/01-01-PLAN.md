---
phase: 01-walking-skeleton-review-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/spike-copilot.yml
autonomous: false
requirements: [ENGN-02]
user_setup:
  - service: github-copilot
    why: "Copilot CLI auth on the runner (live engine call)"
    env_vars:
      - name: COPILOT_GITHUB_TOKEN
        source: "Fine-grained, user-owned PAT with Copilot Requests permission; owner holds an active Copilot seat. Add as a repo Actions secret (D-13)."

must_haves:
  truths:
    - "A throwaway workflow runs `copilot -p ... -s --no-ask-user` on a clean ubuntu-latest runner"
    - "The spike resolves whether zero-tool Copilot returns usable prose, the exact accepted PAT prefix, and cold-start timing"
  artifacts:
    - path: ".github/workflows/spike-copilot.yml"
      provides: "D-12 throwaway spike: workflow_dispatch, npm i @github/copilot@1.0.60, bare copilot prose call"
      contains: "workflow_dispatch"
  key_links:
    - from: ".github/workflows/spike-copilot.yml"
      to: "secrets.COPILOT_GITHUB_TOKEN"
      via: "env on the copilot step"
      pattern: "COPILOT_GITHUB_TOKEN"
---

<objective>
De-risk the phase's single biggest unknown (D-12, flagged in STATE.md): GitHub Copilot CLI behavior on a clean Actions runner — auth via a user-owned PAT, zero-tool prose viability, output stability, and cold-start timing — BEFORE any adapter/pipeline code is written. Findings feed the adapter design in Plan 05.

Purpose: Resolve RESEARCH.md Open Questions 1–3 and Assumptions A1/A2/A3 with live evidence so the adapter is built once, correctly.
Output: A throwaway `spike-copilot.yml` workflow and recorded findings (auth behavior, accepted PAT prefix, whether `--allow-tool` is needed, install + first-call timing vs the 300s budget).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-review-loop/01-CONTEXT.md
@.planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md
@.planning/research/STACK.md
</context>

## Artifacts this phase produces

This plan creates `.github/workflows/spike-copilot.yml` (throwaway, deleted after findings recorded). Full phase inventory in `01-SKELETON.md`.

<tasks>

<task type="auto">
  <name>Task 1: Author the throwaway Copilot CLI spike workflow</name>
  <files>.github/workflows/spike-copilot.yml</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§"Spike workflow (D-12)" lines 445–456; §"Common Pitfalls" Pitfall 7 lines 292–295; Open Questions lines 479–491)
    - .planning/research/STACK.md (Copilot CLI 1.0.x install + headless invocation facts)
  </read_first>
  <action>Create a `workflow_dispatch`-only workflow named for the spike on `ubuntu-latest`. Steps: install the engine with `npm install -g @github/copilot@1.0.60` (pinned), then run a single bare prose call `copilot -p "Reply with exactly: PREVUE_SPIKE_OK" -s --no-ask-user` with `COPILOT_GITHUB_TOKEN: ${{ secrets.COPILOT_GITHUB_TOKEN }}` as step env and NO `--allow-tool` flags (tests the zero-tool posture, A1). Add a second diagnostic step that echoes the masked token prefix length and the copilot version, and wrap the call with shell `time` (or record start/end) so cold-start + first-call timing is observable in logs. Do NOT add `pull_request`/`pull_request_target` triggers, no checkout, no extra permissions. This file is throwaway and will be removed once findings are recorded.</action>
  <verify>
    <automated>test -f .github/workflows/spike-copilot.yml && grep -q "workflow_dispatch" .github/workflows/spike-copilot.yml && grep -q "@github/copilot@1.0.60" .github/workflows/spike-copilot.yml && ! grep -q "pull_request_target" .github/workflows/spike-copilot.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/spike-copilot.yml` exists and is valid YAML
    - Contains `workflow_dispatch`, `@github/copilot@1.0.60`, `-s`, `--no-ask-user`, and `COPILOT_GITHUB_TOKEN`
    - Contains NO `pull_request_target`, NO `actions/checkout` of any PR ref, NO `--allow-tool`
  </acceptance_criteria>
  <done>The spike workflow is committed and ready to dispatch from the Actions tab.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the spike and record live findings (de-risk gate)</name>
  <what-built>A `workflow_dispatch` spike that runs the Copilot CLI headless on a clean runner with the real `COPILOT_GITHUB_TOKEN`.</what-built>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (Open Questions lines 479–491; Assumptions A1/A2/A3 lines 472–474)
  </read_first>
  <how-to-verify>
    1. Ensure `COPILOT_GITHUB_TOKEN` (fine-grained, user-owned PAT with Copilot Requests; owner has a Copilot seat) is set as a repo Actions secret (D-13).
    2. GitHub → Actions → the spike workflow → "Run workflow" (workflow_dispatch).
    3. Open the run logs and record: (a) did the bare zero-tool call return usable prose / `PREVUE_SPIKE_OK` (A1), or did it error needing a tool? (b) the exact accepted PAT prefix — confirm `github_pat_` vs other (A2); (c) total install + first-call wall time vs the 300s call budget (A3); (d) any auth/entitlement or token-shadowing errors (Pitfall 7).
  </how-to-verify>
  <resume-signal>Reply "approved" plus the four findings (prose-viability, PAT prefix, timing, auth notes). If zero-tool fails, note it so Plan 05 grants only `--allow-tool=read` with a file-based prompt.</resume-signal>
  <acceptance_criteria>
    - Spike run completed; the four findings are recorded in the plan SUMMARY
    - A1 (zero-tool prose) resolved YES or fallback noted; A2 (PAT prefix) confirmed; A3 (timing) within budget or mitigation noted
  </acceptance_criteria>
  <done>Adapter design inputs are confirmed with live evidence; Plan 05 can proceed without guessing CLI behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner → Copilot service | `COPILOT_GITHUB_TOKEN` secret crosses into the CLI subprocess here |
| npm registry → runner | third-party install of `@github/copilot` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install -g @github/copilot` | mitigate | Pin `@github/copilot@1.0.60` (RESEARCH Package Legitimacy Audit: first-party GitHub publisher, Approved — no `[ASSUMED]`/`[SUS]`) |
| T-01-LEAK | Information Disclosure | Copilot token in logs | mitigate | Rely on Actions secret masking; echo only token *length*/prefix-class, never the value; no `set -x` around auth |
| T-01-TRIG | Elevation of Privilege | spike trigger | accept→mitigate | `workflow_dispatch` only (manual, maintainer-gated); no `pull_request_target`, no checkout — nothing untrusted runs |
</threat_model>

<verification>
- `grep -q workflow_dispatch .github/workflows/spike-copilot.yml` and `! grep -q pull_request_target ...`
- Manual: spike run logs show prose output (or documented fallback), PAT prefix, and timing.
</verification>

<success_criteria>
- Throwaway spike workflow authored and run on a real runner.
- D-12 findings (A1/A2/A3 + Pitfall 7 auth behavior) recorded and ready to inform Plan 05.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-review-loop/01-01-SUMMARY.md` capturing the spike findings (prose viability, PAT prefix, timing, auth notes) when done.
</output>
