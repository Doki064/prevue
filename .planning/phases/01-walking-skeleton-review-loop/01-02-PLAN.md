---
phase: 01-walking-skeleton-review-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/prevue/__init__.py
  - src/prevue/github/__init__.py
  - src/prevue/engines/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/fixtures/pulls_files.json
  - tests/fixtures/event_pull_request.json
  - .github/workflows/ci.yml
  - .gitignore
autonomous: true
requirements: [SECR-01]

must_haves:
  truths:
    - "`uv run pytest -x -q` runs (zero or more tests collected) against an installed `prevue` package"
    - "Recorded GitHub REST fixtures and shared test fixtures exist for mocking the API and the runner event"
    - "CI runs pytest + ruff + actionlint + zizmor on every push/PR"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed package `prevue` with runtime deps (PyGithub 2.9.*, pydantic 2.13.*), dev deps (pytest 9.*, pytest-cov 7.*, responses 0.26.*, ruff 0.15.*), pytest/ruff config, `prevue` script entrypoint"
      contains: "[project]"
    - path: "tests/conftest.py"
      provides: "shared fixtures: responses activation, fake engine runner, sample event JSON loader"
    - path: "tests/fixtures/pulls_files.json"
      provides: "recorded /pulls/{n}/files payload for diff-fetch tests"
    - path: "tests/fixtures/event_pull_request.json"
      provides: "sample GITHUB_EVENT_PATH payload (pull_request.number, head.repo/base.repo)"
    - path: ".github/workflows/ci.yml"
      provides: "pytest + ruff + actionlint + zizmor gate"
      contains: "zizmor"
  key_links:
    - from: "pyproject.toml"
      to: "src/prevue"
      via: "uv package src layout + [project.scripts] prevue"
      pattern: "prevue"
    - from: ".github/workflows/ci.yml"
      to: ".github/workflows"
      via: "zizmor static scan of workflow YAML"
      pattern: "zizmor"
---

<objective>
Establish the Wave 0 foundation every later plan depends on: the `uv`-managed `prevue` package skeleton, the pytest/ruff/responses test infrastructure with recorded fixtures, and a CI workflow that runs the test + lint + workflow-security gate (including zizmor — the SECR-01 enforcement arm that blocks `pull_request_target`/broad permissions from re-entering).

Purpose: Greenfield repo has no scaffold; without it no TDD plan can run a test. CI's zizmor/actionlint gate makes the SECR-01 trust posture self-enforcing from day one.
Output: `pyproject.toml` + `uv.lock`, `src/prevue/` package namespaces, `tests/` with `conftest.py` + two JSON fixtures, `.github/workflows/ci.yml`, `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md
@.planning/phases/01-walking-skeleton-review-loop/01-PATTERNS.md
@.planning/phases/01-walking-skeleton-review-loop/01-VALIDATION.md
@.planning/research/STACK.md
</context>

## Artifacts this phase produces

This plan creates `pyproject.toml`, `uv.lock`, `src/prevue/` (+`github/`,`engines/`) package namespaces, `tests/conftest.py`, `tests/fixtures/{pulls_files,event_pull_request}.json`, `.github/workflows/ci.yml`, `.gitignore`. Full phase inventory in `01-SKELETON.md`.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the uv package and dependency/tooling config</name>
  <files>pyproject.toml, uv.lock, src/prevue/__init__.py, src/prevue/github/__init__.py, src/prevue/engines/__init__.py, .gitignore</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§Installation lines 112–121; §"Recommended Project Structure" lines 175–199; §Standard Stack lines 80–104)
    - .planning/research/STACK.md (pinned versions: PyGithub 2.9.1, pydantic 2.13.4, pytest 9.0.3, pytest-cov 7.1.0, responses 0.26.1, ruff 0.15.16, uv 0.11.20)
  </read_first>
  <action>Run `uv init --package prevue` to create the `src/prevue/` layout. Add runtime deps with `uv add 'PyGithub==2.9.*' 'pydantic==2.13.*'` and dev deps with `uv add --dev 'pytest==9.*' 'pytest-cov==7.*' 'responses==0.26.*' 'ruff==0.15.*'`. In `pyproject.toml` set `requires-python = ">=3.12"`, declare `[project.scripts] prevue = "prevue.cli:main"` (entrypoint wired in Plan 06), add `[tool.pytest.ini_options]` with `testpaths = ["tests"]`, and a `[tool.ruff]` section. Create empty `src/prevue/github/__init__.py` and `src/prevue/engines/__init__.py` import namespaces. Add a Python `.gitignore` (`.venv/`, `__pycache__/`, `*.egg-info`, `.pytest_cache/`). Commit `uv.lock`.</action>
  <verify>
    <automated>uv sync --locked && uv run python -c "import prevue, prevue.github, prevue.engines" && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync --locked` succeeds and `uv.lock` is committed
    - `pyproject.toml` contains `[project]`, `requires-python = ">=3.12"`, `[project.scripts] prevue = "prevue.cli:main"`, `[tool.pytest.ini_options]`
    - `uv run python -c "import prevue"` exits 0; `uv run pytest -q` exits 0 (no tests yet collected is acceptable)
  </acceptance_criteria>
  <done>The `prevue` package installs and imports; pytest runs against it.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared test fixtures and recorded GitHub REST payloads</name>
  <files>tests/__init__.py, tests/conftest.py, tests/fixtures/pulls_files.json, tests/fixtures/event_pull_request.json</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§"Code Examples — Diff Fetch" lines 307–333; §"Wave 0 Gaps" lines 546–552; §"Validation Architecture" lines 506–539)
    - .planning/phases/01-walking-skeleton-review-loop/01-VALIDATION.md (Wave 0 Requirements lines 60–67)
  </read_first>
  <action>Create `tests/conftest.py` with reusable pytest fixtures: a `responses`-activated fixture for mocking the GitHub REST API, a `fake_engine` fixture providing a stub `EngineAdapter` whose `review()` returns a canned `ReviewResult` (used by orchestration tests in Plan 06), and an `event_json` fixture that loads `tests/fixtures/event_pull_request.json`. Create `tests/fixtures/pulls_files.json` — a realistic recorded `GET /repos/{owner}/{repo}/pulls/{n}/files` response array with at least two files: one with a unified-diff `patch` string and one with `patch` omitted (large/binary), each carrying `filename`, `status`, `additions`, `deletions`. Create `tests/fixtures/event_pull_request.json` — a sample `GITHUB_EVENT_PATH` payload containing `pull_request.number`, `pull_request.base.repo.full_name`, `pull_request.head.repo.full_name`, `pull_request.base.sha`, `pull_request.head.sha` (same-repo, non-fork by default).</action>
  <verify>
    <automated>uv run python -c "import json; a=json.load(open('tests/fixtures/pulls_files.json')); e=json.load(open('tests/fixtures/event_pull_request.json')); assert any(f.get('patch') for f in a) and any(f.get('patch') is None for f in a); assert e['pull_request']['number']"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines `fake_engine` and `event_json` fixtures and a responses-activation fixture
    - `pulls_files.json` contains ≥2 files, ≥1 with a `patch` and ≥1 with `patch` null/absent
    - `event_pull_request.json` exposes `pull_request.number`, `head.repo.full_name`, `base.repo.full_name`
  </acceptance_criteria>
  <done>Tests in later plans can mock the GitHub REST API and the runner event without live calls.</done>
</task>

<task type="auto">
  <name>Task 3: Add the CI workflow (pytest + ruff + actionlint + zizmor)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-walking-skeleton-review-loop/01-RESEARCH.md (§"Sampling Rate" lines 542–544; §"Development Tools" lines 97–103; §"Workflow YAML" lines 417–443 for pinning/permissions conventions)
    - .planning/phases/01-walking-skeleton-review-loop/01-PATTERNS.md (P4 thin-workflow lines 58–63)
  </read_first>
  <action>Create `.github/workflows/ci.yml` triggered on `push` and `pull_request` with top-level `permissions: { contents: read }` only. Job on `ubuntu-latest` using pinned-SHA actions: `actions/checkout`, `astral-sh/setup-uv@v8`, then `uv sync --locked`, `uv run pytest --cov=prevue`, `uv run ruff check`, `uv run ruff format --check`. Add a workflow-lint step running `actionlint` and a `zizmor .github/workflows` step (the SECR-01 static gate that fails CI on `pull_request_target`, broad permissions, or unpinned actions). Pin all action SHAs. Keep the YAML thin (no business logic).</action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q "zizmor" .github/workflows/ci.yml && grep -q "actionlint" .github/workflows/ci.yml && grep -q "setup-uv" .github/workflows/ci.yml && ! grep -q "pull_request_target" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `ci.yml` runs pytest, ruff (check + format --check), actionlint, and `zizmor .github/workflows`
    - Top-level `permissions` is `contents: read`; no `pull_request_target`
    - Action references are SHA-pinned
  </acceptance_criteria>
  <done>Every push/PR runs the test + lint + workflow-security gate; zizmor enforces SECR-01 posture automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → PyPI/npm/Marketplace | dependency + action installs cross here |
| repo → CI permissions | `GITHUB_TOKEN` scope granted to CI jobs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | uv deps + pinned actions | mitigate | Commit `uv.lock`; `uv sync --locked`; SHA-pin all actions (RESEARCH Package Legitimacy Audit — all Approved, no `[ASSUMED]`) |
| T-03 | Elevation of Privilege | future workflow YAML drift | mitigate | `zizmor .github/workflows` + `actionlint` in CI fail on `pull_request_target`/broad perms/unpinned actions (SECR-01 enforcement arm) |
| T-04 | Information Disclosure | CI token scope | mitigate | CI top-level `permissions: { contents: read }` only — least privilege |
</threat_model>

<verification>
- `uv sync --locked` + `uv run pytest -q` green.
- `grep -q zizmor .github/workflows/ci.yml`; `! grep -q pull_request_target .github/workflows/ci.yml`.
- Fixtures parse and expose required keys (Task 2 automated check).
</verification>

<success_criteria>
- `prevue` package installs and imports; pytest infrastructure runs.
- Recorded REST + event fixtures available for all downstream TDD plans.
- CI gate (pytest + ruff + actionlint + zizmor) active and enforcing SECR-01.
</success_criteria>

<output>
Create `.planning/phases/01-walking-skeleton-review-loop/01-02-SUMMARY.md` when done.
</output>
