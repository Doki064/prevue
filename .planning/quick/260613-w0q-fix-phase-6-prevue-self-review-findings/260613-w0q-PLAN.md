---
quick_id: 260613-w0q
slug: fix-phase-6-prevue-self-review-findings
date: 2026-06-13
status: in-progress
---

# Quick Task 260613-w0q: Fix Phase 6 Prevue self-review findings

## Context

PR #13 (Phase 6) merged with 5 warning + 2 info findings from Prevue's own review.
Post-merge remediation on a fresh branch. Fixes must not violate project constraints
(reusable `workflow_call` delivery, minimal token scopes, no `pull_request_target`,
no `secrets: inherit`, forks skip in v1, hybrid deterministic-first classification,
zero-config consumer adoption).

Finding #5 (inline upsert mixed state) recurred across all 10 PR review iterations
because two tests (`test_create_failure_skips_stale_delete`,
`test_create_failure_skips_existing_inline_edit`) **encode the buggy behavior** —
green-test pressure restored the defect each time. The fix rewrites those tests.

## Tasks

### Task 1 — #5 resilient inline upsert (real bug)
- **files:** `src/prevue/github/comments.py`, `tests/test_comments.py`
- **action:** In `post_inline_review`, run existing-comment edits FIRST (best-effort,
  non-fatal), then creates, then ALWAYS attempt stale-comment cleanup. Track an
  aggregate `ok` flag; return it instead of early-returning `False` before cleanup.
- **tests:** rewrite the two bug-encoding tests to assert resilient behavior
  (stale delete still runs on create failure; edits run before create); add a new
  test that an edit failure is non-fatal and stale cleanup still runs.
- **done:** edit→create→always-delete ordering; no early abort strands stale comments.

### Task 2 — #2 reusable workflow fork-PR job guard
- **files:** `.github/workflows/prevue-review.yml`, `tests/test_reusable_workflow_yaml.py`
- **action:** Extend job `if:` to also require
  `github.event.pull_request.head.repo.full_name == github.repository`
  (defense-in-depth; honors v1 "forks skip" constraint even if a caller forgets its guard).
- **done:** new YAML guard test passes; draft guard retained.

### Task 3 — #3 consumer-setup fork note
- **files:** `docs/consumer-setup.md`
- **action:** Add one-line note that fork PRs are unsupported in v1 and skipped
  automatically by the reusable workflow.
- **done:** docs reflect fork behavior.

### Task 4 — #4 missing-config visible signal
- **files:** `src/prevue/config.py`
- **action:** Emit a stderr warning when the resolved config path is absent
  (defaults + `classification.fallback.enabled=true` in effect). No behavior change —
  preserves zero-config adoption.
- **done:** warning printed; `test_absent_file_all_defaults` still green.

### Task 5 — #1 cursor install hardening + risk disclosure
- **files:** `.github/workflows/prevue-review.yml`, `docs/consumer-setup.md`
- **action:** Download installer to a file then exec (avoids partial-exec on truncated
  pipe), keep `cursor.com/install`. No upstream npm pkg / published checksum exists to
  pin against — document the residual supply-chain risk in consumer-setup.
- **done:** `test_cursor_install_uses_official_curl_not_npm_impostor` still green; risk documented.

### Task 6 — #6 review_bots semantics (doc only)
- **files:** `docs/consumer-setup.md`
- **action:** Document that `skip.review_bots` is an allowlist of bots TO review
  (empty default = all bots skipped). No rename (breaking config change).
- **done:** semantics documented.

## Deferred
- #7 (multi-label classify vs skill loading) — needs skill-loader inspection; info-level. Not in this task.

## Verification
- `uv run ruff check .` clean
- `uv run pytest` green (incl. rewritten + new upsert tests)
