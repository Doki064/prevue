# UAT fixture: infra classification

Dummy Terraform for Phase 2 multi-domain upstream testing.

Paired with `examples/uat/frontend/*.tsx` on the same PR. Prevue should classify as both `frontend` and `infra`.

Delete after UAT completes.
